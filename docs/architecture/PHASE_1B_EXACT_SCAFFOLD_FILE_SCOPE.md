# Phase 1B Exact Scaffold File Scope

**Document status:** `ENGINEERING-RECOMMENDED`. `NOT YET AUTHOR-APPROVED`. `NOT YET IMPLEMENTED`. `NOT PRODUCTION-APPROVED`. **This document creates no repository scaffold, no source file, no test file, no configuration file, no manifest file, no CI workflow, and installs no technology.** It proposes the exact file-level scope that a future, separately-authorized implementation task would create.

---

## 1. Purpose

Define, at exact file-path granularity, the proposed initial repository scaffold implied by the Phase 1A architecture and the Phase 1B author-approved technology and policy decisions — so that the author can review and approve (or amend) package identity, blocking implementation sub-decisions, batch boundaries, and the complete file inventory **before** any scaffold file is created.

## 2. Baseline Commits

| Commit | Milestone |
|---|---|
| `23f43676abf6e032a5e96c4077d230cc2283f9b6` | Phase 0G sign-off — "Approve Phase 0G controlled baseline" |
| `a142da371c766bbc3489d7d9ae26e6421527c6c9` | Phase 1A architecture planning — "Document Phase 1A software foundation architecture" |
| `52c2139708a2582d0cbc04067be67fc3051a526b` | Phase 1B author decisions — "Document Phase 1B author decisions" |

## 3. Approved Decision Dependencies

This plan depends on, and does not re-decide, the following author-approved items from `PHASE_1B_AUTHOR_DECISION_REGISTER.md`:

- **Group 1:** Python 3.12; uv; `pyproject.toml`; committed `uv.lock`; Pydantic v2; pytest; mypy; Ruff formatter; Ruff linter.
- **Group 2:** Parquet (bulk tabular) + JSONL (append-only) role separation; no initial database; append-only/immutable/partitioned/provider-traceable raw storage; no migration system until a database is approved.
- **Group 3:** canonical UTC time zone; canonical symbols/timeframes with explicit provider-mapping fields; YAML + versioned manifest configuration with a three-level precedence order; environment-variable secrets with dev-only `.env`.
- **Group 4:** UUIDv7 record identity; SHA-256 `content_fingerprint` (separate from identity); MAJOR.MINOR.PATCH versioning; the approved lineage field set; JSONL append-only audit.
- **Group 5:** stdlib structured-JSON logging (separate from audit); GitHub Actions with Ruff/mypy/pytest checks; `pyproject.toml`/`uv.lock` discipline; deferred containerization.
- **Group 6:** `candle_completeness_status`, `duplicate_classification`, `gap_status` enums (policy level).
- **Group 7:** provider-neutral `MarketDataSourcePort`, `INTERFACE_ONLY` scaffold position, `OFFLINE_FILE`-only early retrieval.
- **Group 8:** canonical UTF-8 JSON manifest format; rule/schema manifest field sets; `BACKWARD_COMPATIBLE`/`BREAKING`/`DOCUMENTATION_ONLY` compatibility classes.

None of these approvals is re-opened here. This document only maps them onto exact file paths and identifies what remains blocking before those paths can be created.

## 4. Explicit Implementation Non-Scope

This document does not, and no companion edit made alongside it does:

- Create the repository scaffold, any source directory, test directory, configuration directory, manifest directory, CI directory, fixture directory, data directory, migration directory, or adapter
- Create `pyproject.toml`, `uv.lock`, `.python-version`, any Python file, any YAML file, any JSON manifest, any JSON Schema file, any GitHub Actions workflow, or any `.env` file
- Install Python, uv, or any package
- Stage, commit, or push anything

## 5. Scaffold Design Principles

- **Conservative by default.** The initial scaffold contains only what an approved decision or an approved batch (Part 14/Section 14) actually requires — never a module "because it may be useful later."
- **No speculative placeholders.** No empty directory is proposed for POI detectors, BTMM detectors, AI, Telegram, MT4, MT5, execution, risk, web platform, or live provider adapters. None of these has an approved implementation decision yet.
- **No `.gitkeep`.** A directory is proposed for creation only when at least one reviewed file will exist inside it at creation time.
- **Every file traces to an approved decision.** Section 9's inventory cross-references the Technology Register item, Decision Group, or Data Contract letter that authorizes each file's existence.
- **Batches are independently reviewable and independently committable.** A later batch never silently depends on an unapproved earlier-batch detail.
- **Approval is not implementation.** Nothing in this document, once author-approved, is thereby implemented — a separate, explicit instruction is required to create each batch's files.

## 6. Blocking Implementation Sub-Decisions

| # | Decision | Recommended option | Alternatives | Reason | Risk | Blocking scaffold | Evidence status | Author approval required | Batch affected |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Exact Python 3.12 patch version | `Python 3.12.13` — **RESOLVED** | `3.12.10` (rejected — older patch, superseded on security fixes) | Determinism (Phase 1A SS13) requires one exact pinned version; 3.12.13 is the current 3.12 security release, installed via uv rather than a python.org Windows installer | None significant now that the installation source (uv-managed) is also resolved | NO — RESOLVED | AUTHOR-APPROVED (origin: ENGINEERING-RECOMMENDED) | YES — APPROVED | 1B-A |
| 2 | Distribution/project name | `btmm-ai-scanner` — **RESOLVED** | `btmm-scanner`, `btmm-ai` (rejected) | Matches the existing GitHub repository name | None significant; the existing repo name was not itself treated as sufficient approval — this decision was separately, explicitly author-approved (Section 7) | NO — RESOLVED | AUTHOR-APPROVED (origin: ENGINEERING-RECOMMENDED) | YES — APPROVED | 1B-A |
| 3 | Python import-package name | `btmm_ai_scanner` — **RESOLVED** | `btmm_scanner` (SUPERSEDED — previously used illustratively in `REPOSITORY_SCAFFOLD_PLAN.md` Section 2; that document's illustrative tree has now been renamed throughout to `btmm_ai_scanner` per this approval) | Mirrors the distribution name closely, avoids future ambiguity | The rename across `REPOSITORY_SCAFFOLD_PLAN.md` has now been applied (Part 2 of this correction) | NO — RESOLVED | AUTHOR-APPROVED (origin: ENGINEERING-RECOMMENDED) | YES — APPROVED | 1B-A |
| 4 | src-layout vs. flat-layout | src-layout (`src/<package>/`) — **RESOLVED** | Flat layout (`<package>/` at repo root) (rejected) | Prevents accidental import of an uninstalled package; matches the existing illustrative tree | None significant | NO — RESOLVED | AUTHOR-APPROVED (origin: ENGINEERING-RECOMMENDED) | YES — APPROVED | 1B-A |
| 5 | Build backend | `uv_build` — **RESOLVED**, constrained as `uv_build>=0.11.30,<0.12` | `hatchling`, `setuptools` (rejected) | uv (already approved) does not itself mandate a PEP 517 backend, but the author has now selected uv's own backend, version-bound to match the approved `uv==0.11.30` family | None significant — both the backend choice and its version constraint are now resolved | NO — RESOLVED | AUTHOR-APPROVED (origin: ENGINEERING-RECOMMENDED) | YES — APPROVED | 1B-A |
| 6 | Whether to create `.python-version` | Yes — **RESOLVED, REQUIRED in Batch 1B-A**; exact content = `3.12.13` — **RESOLVED** (via item #1) | Rely on `pyproject.toml`'s `requires-python` only (rejected) | Pins the exact local/dev interpreter for uv | None significant — both inclusion and content are now resolved | NO — RESOLVED | AUTHOR-APPROVED | YES — APPROVED | 1B-A |
| 7 | Exact runtime dependency versions | **Batch 1B-A: NONE required — RESOLVED.** Batch 1B-B: Pydantic v2 constraint remains `Not chosen here` | Loosely-pinned ranges vs. exact pins (still open for 1B-B) | Batch 1B-A's `config/enums.py`/`config/loader.py` are standard-library-only (see Section 17's Batch 1B-A Dependency Baseline note below); no runtime dependency is needed until Batch 1B-B's executable contracts | Batch 1B-A: none (no dependency exists to pin). Batch 1B-B: unpinned/loose versions would reduce determinism | NO for 1B-A (nothing to resolve); YES for 1B-B (Pydantic constraint still open) | AUTHOR-APPROVED (Batch 1B-A = NONE) / REQUIRES AUTHOR DECISION (Batch 1B-B Pydantic constraint) | YES | 1B-A (resolved: none) / 1B-B (still open) |
| 8 | Exact development dependency versions | **RESOLVED:** `pytest>=9.1.1,<10`; `mypy>=2.3.0,<3`; `ruff>=0.15.22,<0.16` | Exact pins vs. bounded ranges (bounded ranges chosen) | Bounded ranges express intent while `uv.lock` supplies the exact reproducible resolution | None significant | NO — RESOLVED | AUTHOR-APPROVED (origin: ENGINEERING-RECOMMENDED) | YES — APPROVED | 1B-A / 1B-F |
| 9 | `pyproject.toml` metadata fields | **RESOLVED — full field set decided:** `name = "btmm-ai-scanner"`; `version = "0.1.0"`; `description = "Deterministic software foundation for the BTMM and POI AI scanner."`; `authors = [{ name = "Bellforex" }]`; `requires-python = ">=3.12,<3.13"`; `dependencies = []`; `license = "LicenseRef-Proprietary"` (see item 11); `classifiers` = the six approved values (Decision Group 3). **Explicitly omitted from Batch 1B-A, by decision, not by gap:** `readme`, `license-files`, `maintainers`, `keywords`, `project.urls`, `project.scripts`, entry points, `dynamic`, `optional-dependencies`, GUI metadata, CLI metadata | A larger metadata set (urls, keywords, readme, maintainers, etc.) — rejected for Batch 1B-A | Minimum viable metadata for a private, pre-release package; every omitted field is a deliberate decision, not an oversight | None significant — every field's fate (populate or omit) is now decided | NO — RESOLVED | AUTHOR-APPROVED (origin: ENGINEERING-RECOMMENDED) | YES — APPROVED | 1B-A |
| 10 | Minimum supported operating systems | **RESOLVED:** Windows 11 x64 is the validated initial development environment. **Package compatibility claim: NOT YET ESTABLISHED** — Batch 1B-A uses only portable Python standard-library functionality (no Windows-only filesystem assumptions, no registry access, no COM integration, no shell-specific implementation), but OS-independence is not *claimed* until actually tested. No operating-system classifier is included. Linux and macOS validation remain deferred | Restrict to Windows only (rejected — would falsely claim a restriction that isn't technically required); claim full cross-platform support now (rejected — untested) | Affects CI matrix and classifiers; avoids an unverified compatibility claim | None significant — the *position* (what to claim and what not to claim) is now decided, even though the underlying multi-OS testing itself remains future work | NO — RESOLVED (position); the actual cross-platform validation itself remains a future CI/testing task, not a scaffold blocker | AUTHOR-APPROVED (origin: ENGINEERING-RECOMMENDED) | YES — APPROVED | 1B-A / 1B-F |
| 11 | License-field position | **RESOLVED:** `license = "LicenseRef-Proprietary"` — a PEP 639 license-expression string in `[project]`, with **no `LICENSE`/`LICENCE` file** created in Batch 1B-A (the file itself is an explicit omission, per Decision Group 3) | SPDX expression + a `LICENSE` file (rejected for Batch 1B-A — no licence file is created); license text embedded only in `pyproject.toml` (closer to what was chosen, minus a file) | Matches the project's proprietary, non-public-release status without requiring a separate licence-file artifact this batch | None significant | NO — RESOLVED | AUTHOR-APPROVED (origin: ENGINEERING-RECOMMENDED) | YES — APPROVED | 1B-A |
| 12 | Initial package version | `0.1.0` — **RESOLVED** | `0.0.1` (rejected) | SemVer pre-1.0 convention for unreleased software | None significant | NO — RESOLVED | AUTHOR-APPROVED (origin: ENGINEERING-RECOMMENDED) | YES — APPROVED | 1B-A |
| 13 | Exact YAML configuration filenames | Not chosen here | `config/default.yaml` + `config/<environment>.yaml`, vs. one monolithic file | Must match the approved three-level precedence | Filename collision with the `config/` **code** module if unresolved | YES | REQUIRES AUTHOR DECISION | YES | Deferred beyond 1B-A–1B-F (see Part 17) |
| 14 | Exact configuration-directory placement | Not chosen here | Top-level `config/` (currently a protected path for this task) vs. package-internal `src/<package>/config/defaults/` | Top-level `config/` is explicitly protected/off-limits during this planning task | Choosing wrong risks colliding with the protected-path list | YES | REQUIRES AUTHOR DECISION | YES | Deferred |
| 15 | Exact manifest filenames | Not chosen here | `<rule_family>_v<version>.json` pattern vs. other | Must be reviewed before any manifest is created (Part 11 rule) | Wrong naming requires a rename after real content exists | YES | REQUIRES AUTHOR DECISION | YES | Deferred (post-1B-F) |
| 16 | Exact manifest-directory behavior | Create only when the first real, reviewed manifest exists — no empty directory, no `.gitkeep` | Pre-create empty `manifests/rules/` and `manifests/schemas/` now | Matches Section 5's design principle and Part 11's explicit rule | Pre-creating empty dirs invites unreviewed placeholder content later | NO (principle already settled) | ENGINEERING-RECOMMENDED (already effectively approved via Section 5) | YES (already covered) | Deferred |
| 17 | Avoid empty manifest directories until real manifest exists | Yes | No | Same as #16 | Same as #16 | NO | ENGINEERING-RECOMMENDED | YES (already covered) | Deferred |
| 18 | Exact validation-status enum | Not chosen here | `PASS \| FAIL \| QUARANTINED` vs. a finer-grained set | `PROVENANCE_VALIDATION_AND_AUDIT_PLAN.md` SS9a left this `REQUIRES AUTHOR DECISION` | Wrong enum granularity requires a breaking schema change later | YES | REQUIRES AUTHOR DECISION | YES | 1B-C |
| 19 | Exact contract module boundaries | One module per contract letter under `contracts/` | One monolithic `contracts.py` | Matches the 18-contract structure in `DATA_CONTRACTS_AND_SCHEMA_PLAN.md` | A monolithic file becomes unwieldy as contracts grow | YES | ENGINEERING-RECOMMENDED / REQUIRES AUTHOR DECISION | YES | 1B-B |
| 20 | Exact canonical JSON serialization approach | Not chosen here | Sorted-key, separator-normalized `json.dumps` vs. a canonical-JSON library | Needed for deterministic `content_fingerprint` computation | Wrong approach breaks fingerprint reproducibility across environments | YES | REQUIRES AUTHOR DECISION | YES | 1B-B / deferred (manifests) |
| 21 | Exact SHA-256 fingerprint field sets | Not chosen here | Fingerprint the full record vs. a defined subset per contract | `DATA_CONTRACTS_AND_SCHEMA_PLAN.md` SS4 left this deferred | Wrong field set breaks duplicate/integrity detection later | YES | REQUIRES AUTHOR DECISION | YES | 1B-B |
| 22 | Exact raw provider-payload encoding | Not chosen here | Store raw bytes vs. a normalized intermediate | `PHASE_1A...` Part 7 item 11 left the physical encoding open | Wrong encoding may lose provider-native fidelity | YES | REQUIRES AUTHOR DECISION | YES | Deferred beyond 1B-A–1B-F |
| 23 | Exact partition naming | Not chosen here | `symbol/timeframe/date` vs. other schemes | Affects raw-storage layout | Wrong naming complicates later retrieval/replay | YES | REQUIRES AUTHOR DECISION | YES | Deferred |
| 24 | Retention policy | Not chosen here | Indefinite retention (current default principle) vs. a defined limit | `PROVENANCE_VALIDATION_AND_AUDIT_PLAN.md` SS23 left this open | No retention enforcement exists in any of Batches 1B-A–1B-F | NO for these batches; YES before real ingestion | REQUIRES AUTHOR DECISION | YES | Deferred |
| 25 | Exact candle-close timestamp convention | Not chosen here | Inclusive vs. exclusive boundary | `DATA_CONTRACTS_AND_SCHEMA_PLAN.md` Contracts A/B leave this `REQUIRES AUTHOR DECISION` | Wrong convention causes off-by-one boundary errors | NO for the contract *shape* (field can exist, marked provisional); YES for correctness | ENGINEERING-PROVISIONAL / REQUIRES AUTHOR DECISION | YES | 1B-B (shape only) |
| 26 | Provider completion-evidence representation | Not chosen here | Provider-supplied "closed" flag vs. wall-clock inference (already rejected as sole proof) | Group 6 approved the enum, not the detection mechanism | Wrong mechanism could misclassify incomplete candles as complete | NO for the enum shape; YES for real detection | REQUIRES AUTHOR DECISION | YES | 1B-C (shape only) |
| 27 | Exact canonical candle key | Not chosen here | `(provider, provider_symbol, provider_timeframe, candle_open_time)` (already named in Group 6) vs. an expanded key | Duplicate-key final field set was explicitly left deferred | Wrong key causes false-positive or false-negative duplicate detection | NO for the shape; YES for the final field set | REQUIRES AUTHOR DECISION | YES | 1B-C (shape only) |
| 28 | CI trigger policy | Not chosen here | On push to `main` + on pull request, vs. push-only | Determines when Batch 1B-F's workflow runs | Overly broad triggers waste CI minutes; overly narrow triggers miss regressions | YES | REQUIRES AUTHOR DECISION | YES | 1B-F |
| 29 | Branch-protection policy | Not chosen here | Require passing CI before merge vs. no protection | This is a GitHub repository **setting**, not a file this scaffold creates | Not enforcing it allows an unreviewed merge to bypass CI | YES (as a policy), but **not a file-creation matter** | REQUIRES AUTHOR DECISION | YES | Outside file scope (repository setting) |
| 30 | Mandatory pull-request policy | Not chosen here | Require PR review before merge vs. direct pushes allowed | Same nature as #29 | Same as #29 | YES (as a policy), but **not a file-creation matter** | REQUIRES AUTHOR DECISION | YES | Outside file scope (repository setting) |

**No item above is silently decided.** Items 16–17 are marked `Blocking scaffold: NO` only because they restate a design principle (Section 5) already implicit in the author-approved "no empty placeholder directories" instruction — they are not new technology or naming decisions.

**Recalculated totals, following the Phase 1B-A Decision Group 3 — Metadata, Configuration Contract, Tests and Git Hygiene decision (supersedes the prior Phase 1B-A Runtime and Dependency Baseline recalculation above it in session history):**

| Category | Count | Items |
|---|---|---|
| Total blocking-decision rows | 30 | 1–30 |
| Fully author-approved decisions (no residual blocking sub-item) | 11 | 1 (exact Python patch), 2 (distribution name), 3 (import package), 4 (layout), 5 (build backend + exact constraint), 6 (`.python-version` inclusion + content), 8 (development dependency versions), 9 (full metadata field set, including explicit omissions), 10 (minimum-OS position), 11 (license-field position), 12 (initial version) |
| Remaining unresolved-and-blocking decisions | 11 | 7 (Batch 1B-A resolved to NONE, but Batch 1B-B's exact Pydantic constraint remains open), 18, 19, 20, 21, 25, 26, 27, 28, 29, 30 |
| Deferred decisions (scope beyond the current batches, not yet blocking) | 8 | 13, 14, 15, 16, 17, 22, 23, 24 |

**11 + 11 + 8 = 30.** This matches the expected result stated in the governing instruction exactly — actual counting confirms it rather than assuming it. Item 7 remains **partially** resolved — Batch 1B-A's own portion is settled (zero runtime dependencies), but Batch 1B-B's exact Pydantic version constraint remains genuinely open, so item 7 stays in the "unresolved-and-blocking" bucket rather than "fully author-approved," to avoid ever presenting Pydantic's constraint as resolved. **This correction newly resolved only items 9, 10, and 11** (previously unresolved or partially unresolved); items 1, 2, 3, 4, 5, 6, 8, and 12 were already resolved by prior decisions and are unchanged here.

**Following Phase 1B-A Decision Group 4 (Interfaces, Tool Configuration, Execution Sequence and Rollback): the 30-row table above and this recalculation remain unchanged — no row was newly resolved, because none of Decision Group 4's items exactly corresponds to any existing numbered row.** Exact function signatures, exact exception classes, exact `__init__.py` contents, exact Ruff configuration, exact mypy configuration, exact pytest configuration, the installation sequence, and the rollback sequence are now resolved — but as a **separate, implementation-specific approval accounting** (Section 6a), not as changes to items 7, 13–17, 20, or 28 above. **Still not silently resolved by any correction to date:** Pydantic's version constraint (item 7, Batch 1B-B), YAML library/filename/placement decisions (items 13–14), manifest filenames/directory behavior (items 15–17), canonical JSON serialization (item 20), raw-payload encoding/partition naming/retention (items 22–24), candle-close/completion/duplicate-key semantics (items 25–27), and CI trigger/branch-protection/PR policy (items 28–30).

**Additional decisions approved by this correction, not modeled as their own numbered row in the table above** (per the "do not change the total row count" instruction, these are recorded here in prose instead of as new rows 31+):
- **Whether uv-managed Python is permitted:** `AUTHOR-APPROVED` — uv-managed Python is permitted and preferred. The existing local Python 3.14.6 (discovered by the Phase 1B-A Runtime and Dependency Environment Audit) must not be removed, modified, or used as the project runtime. A system-installed interpreter is acceptable only when it exactly satisfies the approved runtime (`3.12.13`) and is provenance-recorded. Project execution must use `uv run` or the project virtual environment.
- **Exact uv version policy:** `AUTHOR-APPROVED` — `uv == 0.11.30`, recorded as `[tool.uv] required-version = "==0.11.30"` in the future `pyproject.toml`.
- **Direct-dependency constraint policy (as a policy, distinct from any specific package's version number):** `AUTHOR-APPROVED` — direct dependencies use reviewed bounded ranges in `pyproject.toml`; exact direct and transitive versions are resolved in a committed `uv.lock`; dependency upgrades require review and testing; no fully floating dependency ranges are permitted.
- **Whether configuration modules remain in Batch 1B-A:** `AUTHOR-APPROVED` — yes, `config/__init__.py`, `config/enums.py`, and `config/loader.py` remain in Batch 1B-A, scoped to standard-library-only content (foundational enums, plain mappings, environment-variable reading, deterministic precedence merging, secret-value exclusion) — no YAML parsing, no Pydantic models, no `pydantic-settings`, no filesystem configuration loading, no production/provider-specific configuration, and no credential persistence in this batch.
- **Whether the nine-file Batch 1B-A scope remains unchanged:** **SUPERSEDED by the Phase 1B-A Decision Group 3 revision below** — Batch 1B-A now contains ten changed paths (nine new files plus one modified existing file, `.gitignore`). See Section 14's revised batch row and Section 9's new `.gitignore` inventory row.

**Phase 1B-A Decision Group 3 — Metadata, Configuration Contract, Tests and Git Hygiene (`AUTHOR-APPROVED`):**

- **Project metadata:** the full `[project]` field set is now resolved (Section 6 items 9, 10, 11) — see the pyproject.toml plan following Section 9's inventory table for the exact TOML content.
- **Configuration enums (`src/btmm_ai_scanner/config/enums.py`):** exactly two `StrEnum` classes are approved — `InternalSymbol` (`XAUUSD`, `EURUSD`, `GBPUSD`) and `Timeframe` (`M1`, `M5`, `M15`, `H1`, `H3`, `H4`, `D1`, `W1`), both using uppercase string values. No POI-type, lifecycle-state, BTMM-state, or validation-state enum is introduced; no provider-specific symbol or unsupported timeframe is added; no resampling logic exists. See Section 17 for the full boundary.
- **Configuration loader (`src/btmm_ai_scanner/config/loader.py`):** approved responsibilities are reading approved non-secret runtime environment variables (prefix `BTMM_CONFIG_`, e.g. `BTMM_CONFIG_LOG_LEVEL` → `log_level`), normalizing keys, merging three precedence layers deterministically (project defaults → environment-specific non-secret overrides → runtime environment overrides, later overrides earlier), returning a new mapping without mutating caller-owned input, using the standard library only, and a **shallow top-level merge only** (no nested/deep merge). See Section 17 for the full boundary.
- **Secret boundary:** the loader must reject keys containing indicators such as `password`, `secret`, `token`, `credential`, `api_key`, `private_key`; it must not read `.env`, return credential values, log credential names or values, or create credential defaults; actual secret retrieval remains a separate, future, unbuilt boundary.
- **Test boundaries:** `tests/test_import_smoke.py` and `tests/test_config_precedence.py` — exact approved/prohibited coverage recorded in Section 23.
- **Git hygiene (`.gitignore`):** joins Batch 1B-A as a **modified existing file** (not a new file). Preserves `references/private/*`; adds `.venv/`, `.env`/`.env.*`/`!.env.example`, `__pycache__/`/`*.py[cod]`, `.pytest_cache/`/`.mypy_cache/`/`.ruff_cache/`, `.coverage`/`coverage.xml`/`htmlcov/`, `build/`/`dist/`/`*.egg-info/`. Does **not** ignore `pyproject.toml`, `uv.lock`, `.python-version`, source code, tests, reviewed configuration, reviewed manifests, or documentation. **`.gitignore` is not modified by this documentation task** — see Section 9's new inventory row.
- **Revised Batch 1B-A scope:** ten changed paths total (nine new implementation files + one modified existing file, `.gitignore`). Batch 1B-A remains `NOT YET AUTHOR-APPROVED FOR EXECUTION`.

All items above (both this Decision Group 3 block and the five items preceding it) carry: Recommendation origin `ENGINEERING-RECOMMENDED`; Current author-decision status `AUTHOR-APPROVED`; Implementation status `NOT YET IMPLEMENTED`; Production status `NOT PRODUCTION-APPROVED`.

### 6a. Decision Group 4 — Interfaces, Tool Configuration, Execution Sequence and Rollback (`AUTHOR-APPROVED`)

**None of these items corresponds to an existing row in the 30-row blocking-decision table above — the table's totals (Section 6's recalculation) are therefore left unchanged by this decision round.** They are recorded here as a separate, implementation-specific approval-status accounting, exactly as instructed.

| Item | Detail location | Recommendation origin | Author-decision status | Implementation status | Production status |
|---|---|---|---|---|---|
| Package-root content (`src/btmm_ai_scanner/__init__.py`) | Section 9b | ENGINEERING-RECOMMENDED | AUTHOR-APPROVED | NOT YET IMPLEMENTED | NOT PRODUCTION-APPROVED |
| Configuration package exports (`config/__init__.py`) | Section 17d | ENGINEERING-RECOMMENDED | AUTHOR-APPROVED | NOT YET IMPLEMENTED | NOT PRODUCTION-APPROVED |
| Enum implementation (exact code) | Section 17a | ENGINEERING-RECOMMENDED | AUTHOR-APPROVED | NOT YET IMPLEMENTED | NOT PRODUCTION-APPROVED |
| Exception hierarchy | Section 17e | ENGINEERING-RECOMMENDED | AUTHOR-APPROVED | NOT YET IMPLEMENTED | NOT PRODUCTION-APPROVED |
| Loader function interface and behavior | Section 17b | ENGINEERING-RECOMMENDED | AUTHOR-APPROVED | NOT YET IMPLEMENTED | NOT PRODUCTION-APPROVED |
| Private implementation boundary | Section 17f | ENGINEERING-RECOMMENDED | AUTHOR-APPROVED | NOT YET IMPLEMENTED | NOT PRODUCTION-APPROVED |
| Ruff configuration | Section 9a | ENGINEERING-RECOMMENDED | AUTHOR-APPROVED | NOT YET IMPLEMENTED | NOT PRODUCTION-APPROVED |
| mypy configuration | Section 9a | ENGINEERING-RECOMMENDED | AUTHOR-APPROVED | NOT YET IMPLEMENTED | NOT PRODUCTION-APPROVED |
| pytest configuration | Section 9a | ENGINEERING-RECOMMENDED | AUTHOR-APPROVED | NOT YET IMPLEMENTED | NOT PRODUCTION-APPROVED |
| Development dependency group | Section 9a | ENGINEERING-RECOMMENDED | AUTHOR-APPROVED | NOT YET IMPLEMENTED | NOT PRODUCTION-APPROVED |
| Exact test-function scope | Section 23a | ENGINEERING-RECOMMENDED | AUTHOR-APPROVED | NOT YET IMPLEMENTED | NOT PRODUCTION-APPROVED |
| Installation/execution sequence (Stages A–J) | Section 16a | ENGINEERING-RECOMMENDED | AUTHOR-APPROVED | NOT YET IMPLEMENTED | NOT PRODUCTION-APPROVED |
| Rollback procedure | Section 27 | ENGINEERING-RECOMMENDED | AUTHOR-APPROVED | NOT YET IMPLEMENTED | NOT PRODUCTION-APPROVED |
| Acceptance criteria | Section 28 | ENGINEERING-RECOMMENDED | AUTHOR-APPROVED | NOT YET IMPLEMENTED | NOT PRODUCTION-APPROVED |

**Batch 1B-A is not thereby represented as execution-authorized.** Every item above is a documented, approved *specification* — none has been implemented, and none carries any production status beyond `NOT PRODUCTION-APPROVED`. Batch 1B-A remains `NOT YET AUTHOR-APPROVED FOR EXECUTION` (Section 14) until a separate, explicit instruction authorizes Stage A of Section 16a.

## 7. Proposed Package and Distribution Identity

**Status: `AUTHOR-APPROVED` (Phase 1B-0 Package Identity and Layout decision). Recommendation origin: `ENGINEERING-RECOMMENDED`. Implementation status: `NOT YET IMPLEMENTED`. Production status: `NOT PRODUCTION-APPROVED`.**

| Field | Author-approved value | Status |
|---|---|---|
| `distribution_name` | `btmm-ai-scanner` | `AUTHOR-APPROVED` |
| `import_package` | `btmm_ai_scanner` | `AUTHOR-APPROVED` |
| `source_package_path` | `src/btmm_ai_scanner/` | `AUTHOR-APPROVED` |
| `layout` | `src` (src-layout, not flat-layout) | `AUTHOR-APPROVED` |
| `build_backend` | `uv_build` | `AUTHOR-APPROVED` |
| `initial_project_version` | `0.1.0` | `AUTHOR-APPROVED` |
| `python_version_file` | `.python-version` — **REQUIRED IN BATCH 1B-A** | `AUTHOR-APPROVED` (inclusion); exact patch content `REQUIRES AUTHOR DECISION` (Blocking Decision #1) |

**Distribution identity and import identity are intentionally different representations, and remain distinct concepts** — the hyphenated distribution name (`btmm-ai-scanner`, used in `pyproject.toml`'s `[project].name`, on PyPI if ever published, and in `uv`/`pip` commands) installs the underscore-based Python import package (`btmm_ai_scanner`, used in `import btmm_ai_scanner` and under `src/`); a distribution name may contain hyphens, but the import package name must be a valid Python identifier, which does not permit hyphens.

**The `src` layout separates importable code from the repository root** — the package lives at `src/btmm_ai_scanner/`, not at a flat `btmm_ai_scanner/` beside `pyproject.toml`, preventing accidental import of an uninstalled package.

**No package directory exists yet.** `src/`, `src/btmm_ai_scanner/`, and every file beneath them remain proposals only — this correction records an approved *decision*, not an implemented artifact.

**No build configuration exists yet.** `uv_build` is the author-approved future build backend; no `pyproject.toml` `[build-system]` table has been written.

**No package has been installed.** Neither Python, uv, nor any dependency has been installed by this or any prior task.

**No version has been published.** `0.1.0` is the author-approved *initial* version value for a future `pyproject.toml`; no package has been built, tagged, or published under that version.

**The existing GitHub repository name (`btmm-ai-scanner`) was not, by itself, treated as sufficient approval of the distribution name.** The distribution name above is now approved via the author's explicit Phase 1B-0 Package Identity and Layout decision, recorded in this section — not merely inferred from the repository's hosting label.

**Renaming impact (resolved):** `docs/architecture/REPOSITORY_SCAFFOLD_PLAN.md` Section 2's proposed tree previously used `src/btmm_scanner/` (without `_ai_`) illustratively. Per this approval, that tree — and every other proposed package-path reference in that document — has now been corrected throughout to `src/btmm_ai_scanner/` (see that document's Section 9). No stale active reference to `src/btmm_scanner/` remains in either document.

## 8. Proposed Exact Directory Tree

**This tree reflects only the batches defined in Section 14 (1B-A through 1B-F) — a deliberately narrow subset of the full 16-layer architecture.** Directories for `measurements/`, `domain/`, `poi/`, `lifecycle/`, `btmm/`, `annotations/`, `replay/`, `scripts/`, and `migrations/` are **not** included below, because no batch in Section 14 places a reviewed file inside them yet (Section 5 principle). `manifests/rules/` and `manifests/schemas/` are likewise excluded until their first real manifest exists (Part 11 rule).

```
btmm-ai-scanner/
├── docs/                                  (existing — unchanged)
├── knowledge/                             (existing — unchanged)
├── references/                            (existing — unchanged)
├── .gitignore                             (existing — proposed MODIFICATION, Batch 1B-A, Decision Group 3 — creation order 0, precedes all other Batch 1B-A files)
├── pyproject.toml                         (proposed, Batch 1B-A — file scope, exact contents in Part 8/Section 17)
├── uv.lock                                (proposed, Batch 1B-A — created only after pyproject.toml and dependency set are approved)
├── .python-version                        (proposed, Batch 1B-A — REQUIRED, inclusion AUTHOR-APPROVED; exact patch content still pending Blocking Decision #1)
├── src/
│   └── btmm_ai_scanner/                    (proposed import package — name AUTHOR-APPROVED, Section 7)
│       ├── __init__.py                    (Batch 1B-A)
│       ├── config/
│       │   ├── __init__.py                (Batch 1B-A)
│       │   ├── enums.py                   (Batch 1B-A — canonical symbol/provider/timeframe enums)
│       │   └── loader.py                  (Batch 1B-A — YAML + precedence loader; no YAML *data* file created — see Blocking Decisions #13–#14)
│       ├── contracts/
│       │   ├── __init__.py                (Batch 1B-B)
│       │   ├── types.py                   (Batch 1B-B — identity/fingerprint/version value types)
│       │   ├── raw_candle.py              (Batch 1B-B — Contract A)
│       │   ├── normalized_candle.py       (Batch 1B-B — Contract B)
│       │   ├── validation_result.py       (Batch 1B-B — Contract N)
│       │   ├── provenance_record.py       (Batch 1B-B — Contract M)
│       │   ├── rule_version_manifest.py   (Batch 1B-B — Contract Q, shape only)
│       │   └── schema_version_manifest.py (Batch 1B-B — Contract R, shape only)
│       ├── validation/
│       │   ├── __init__.py                (Batch 1B-C)
│       │   ├── completeness.py            (Batch 1B-C)
│       │   ├── duplicates.py              (Batch 1B-C)
│       │   ├── gaps.py                    (Batch 1B-C)
│       │   └── eligibility.py             (Batch 1B-C)
│       ├── audit/
│       │   ├── __init__.py                (Batch 1B-D)
│       │   ├── events.py                  (Batch 1B-D — Contract O)
│       │   └── writer.py                  (Batch 1B-D — append-only writer interface)
│       ├── observability/
│       │   ├── __init__.py                (Batch 1B-D)
│       │   └── logging_config.py          (Batch 1B-D)
│       └── ingestion/
│           ├── __init__.py                (Batch 1B-E)
│           ├── port.py                    (Batch 1B-E — MarketDataSourcePort)
│           ├── requests.py                (Batch 1B-E)
│           ├── results.py                 (Batch 1B-E)
│           └── offline_file_source.py     (Batch 1B-E — OFFLINE_FILE stub)
├── tests/
│   ├── test_import_smoke.py               (Batch 1B-A)
│   ├── test_config_precedence.py          (Batch 1B-A)
│   └── unit/
│       ├── test_identity_and_fingerprint.py        (Batch 1B-B)
│       ├── test_semver.py                          (Batch 1B-B)
│       ├── test_raw_candle_contract.py             (Batch 1B-B)
│       ├── test_normalized_candle_contract.py      (Batch 1B-B)
│       ├── test_manifest_compatibility_classes.py  (Batch 1B-B)
│       ├── test_validation_result.py               (Batch 1B-B — Decision Group 4)
│       ├── test_provenance_record.py                (Batch 1B-B — Decision Group 4)
│       ├── test_candle_completeness.py             (Batch 1B-C)
│       ├── test_exact_duplicate.py                 (Batch 1B-C)
│       ├── test_conflicting_duplicate_quarantine.py (Batch 1B-C)
│       ├── test_missing_gap.py                     (Batch 1B-C)
│       ├── test_no_synthetic_fill.py               (Batch 1B-C)
│       ├── test_validation_eligibility.py          (Batch 1B-C)
│       ├── test_processing_version_preservation.py (Batch 1B-C)
│       ├── test_audit_log_separation.py            (Batch 1B-D)
│       ├── test_secret_redaction.py                (Batch 1B-D)
│       ├── test_ingestion_port_contract.py         (Batch 1B-E)
│       └── test_offline_file_stub.py               (Batch 1B-E)
└── .github/
    └── workflows/
        └── ci.yml                          (Batch 1B-F)
```

**No directory or file in this tree is created or modified by this document — including `.gitignore`, which remains untouched by this documentation task despite being a proposed Batch 1B-A modification.** `measurements/`, `domain/`, `poi/`, `lifecycle/`, `btmm/`, `annotations/`, `replay/`, `scripts/`, `migrations/`, `manifests/rules/`, and `manifests/schemas/` remain correctly absent, matching Section 5's conservative-scaffold principle.

## 9. Exact File Inventory

Every proposed file, one row each. **Creation order** matches Section 15. **Independent commit eligible** reflects whether the file may be committed alone or only as part of its whole batch (Section 14 requires whole-batch review, so no file below is independently committable ahead of its batch — the column records intra-batch groupings only).

| Batch | Exact path | New/Modified | File type | Purpose | Approved decision supported | Required content | Prohibited content | Direct dependencies | Tests required | Blocking unresolved decision | Creation order | Scope status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1B-A | `.gitignore` | **Modified** (existing file) | Ignore-pattern text | Excludes Python/uv/test/build artifacts from version control | Decision Group 3 (Git Hygiene) | Preserve `references/private/*`; add `.venv/`, `.env`/`.env.*`/`!.env.example`, `__pycache__/`/`*.py[cod]`, `.pytest_cache/`/`.mypy_cache/`/`.ruff_cache/`, `.coverage`/`coverage.xml`/`htmlcov/`, `build/`/`dist/`/`*.egg-info/` | Ignoring `pyproject.toml`, `uv.lock`, `.python-version`, source code, tests, reviewed configuration, reviewed manifests, or documentation | None | N/A (no test exercises `.gitignore` directly) | None — fully resolved | 0 (precedes all other Batch 1B-A files) | **IMPLEMENTED** — modified in commit `47cfd699bb7f4893774579f1693abbbb57b91607`; not modified by this documentation task |
| 1B-A | `pyproject.toml` | New | TOML manifest | Project/build metadata, dependency declarations | Register #1–#8, #12–#13; Phase 1B-0 Package Identity and Layout decision; Phase 1B-A Runtime and Dependency Baseline; Phase 1B-A Decision Group 3 (full metadata field set) | `[project]` table: `name = "btmm-ai-scanner"`, `version = "0.1.0"`, `description = "Deterministic software foundation for the BTMM and POI AI scanner."`, `authors = [{ name = "Bellforex" }]`, `requires-python = ">=3.12,<3.13"`, `dependencies = []`, `license = "LicenseRef-Proprietary"`, `classifiers` (six approved values, Section 6 item 9); `[build-system]` table (`uv_build>=0.11.30,<0.12`); `[tool.uv]` version requirement; `src`-layout package discovery pointing at `btmm_ai_scanner`; a development-dependency group with the three bounded ranges (`pytest`, `mypy`, `ruff`) | Secrets, entry/risk fields, any runtime dependency (Batch 1B-A has none); `readme`, `license-files`, `maintainers`, `keywords`, `project.urls`, `project.scripts`, entry points, `dynamic`, `optional-dependencies` (all explicitly omitted, Section 6 item 9) | None | Import-smoke test passes | None — metadata fully resolved (Section 6 items 9, 10, 11) | 1 | **IMPLEMENTED** — committed in `47cfd699bb7f4893774579f1693abbbb57b91607` |
| 1B-A | `uv.lock` | New | Lockfile | Reproducible dependency resolution | Register #3; Phase 1B-A Runtime and Dependency Baseline | Resolved dependency graph for the dev group only (`pytest`, `mypy`, `ruff` and their transitive deps) — no runtime dependency to resolve | Secrets | `pyproject.toml` | N/A (generated) | None — `uv.lock` cannot exist until `uv` and Python 3.12.13 are installed during an authorized implementation task | 2 | **IMPLEMENTED** — committed in `47cfd699bb7f4893774579f1693abbbb57b91607`; generated via `uv lock`, verified via `uv lock --check` |
| 1B-A | `.python-version` | New | Text | Local/dev interpreter pin | Register #1–#2; Phase 1B-0 Package Identity and Layout decision (inclusion); Phase 1B-A Runtime and Dependency Baseline (exact content) | Exact content: `3.12.13` | Nothing else | `pyproject.toml`'s `requires-python` | N/A | None — both inclusion and content are resolved | 3 | **IMPLEMENTED** — committed in `47cfd699bb7f4893774579f1693abbbb57b91607` |
| 1B-A | `src/btmm_ai_scanner/__init__.py` | New | Python | Package root docstring only | Register #1, #3, #12; Decision Group 4 (Section 9b) | Module docstring only: `"""Deterministic software foundation for the BTMM and POI AI scanner."""` | `__version__` constant; configuration re-exports; filesystem access; environment-variable access; logging setup; network activity; any import-time initialization; business logic | None | Import smoke test | None — fully resolved | 4 | **IMPLEMENTED** — committed in `47cfd699bb7f4893774579f1693abbbb57b91607` |
| 1B-A | `src/btmm_ai_scanner/config/__init__.py` | New | Python | Re-exports the approved public configuration API | Layer: config; Decision Group 4 (Section 17d) | `from .enums import InternalSymbol, Timeframe`; `from .loader import ENV_PREFIX, ConfigurationError, InvalidConfigurationKeyError, SecretConfigurationKeyError, load_configuration`; `__all__` listing all six names | Business logic; any export beyond the six named symbols | `config/enums.py`, `config/loader.py` | Import smoke test (`test_config_public_exports_import`) | None — fully resolved | 5 | **IMPLEMENTED** — committed in `47cfd699bb7f4893774579f1693abbbb57b91607` |
| 1B-A | `src/btmm_ai_scanner/config/enums.py` | New | Python (standard-library only) | Canonical internal-symbol/timeframe enums | Phase 0G `AUTHOR-APPROVED` lists (`docs/PROJECT_SCOPE.md`); Group 3; Decision Group 3/4 (Section 17a) | Exactly two `StrEnum` classes: `InternalSymbol` (`XAUUSD`/`EURUSD`/`GBPUSD`), `Timeframe` (`M1`/`M5`/`M15`/`H1`/`H3`/`H4`/`D1`/`W1`) — no separate provider enum | Any un-approved symbol/timeframe; a separate `ProviderEnum`; POI/lifecycle/BTMM/validation-state enums; Pydantic; any third-party import | None | `test_config_public_exports_import` (import), Config-precedence test (indirect usage) | None — fully resolved | 6 | **IMPLEMENTED** — committed in `47cfd699bb7f4893774579f1693abbbb57b91607` |
| 1B-A | `src/btmm_ai_scanner/config/loader.py` | New | Python (standard-library only) | Config precedence resolution (defaults → environment → env vars) | Group 3 (configuration precedence); Decision Group 4 (Section 17b/17e/17f — exact signature, exceptions, private helpers) | `ENV_PREFIX`; `load_configuration(project_defaults, environment_overrides, *, environ=None) -> dict`; `ConfigurationError`/`InvalidConfigurationKeyError`/`SecretConfigurationKeyError`; private `_`-prefixed helpers for key validation, secret detection, runtime extraction, shallow merge | Any hard-coded secret; any actual YAML file; YAML parsing; Pydantic models; `pydantic-settings`; filesystem configuration loading; production/provider-specific configuration; credential persistence; recursive/nested merge; a global configuration singleton | `config/enums.py` | `test_config_precedence.py` (11 named tests, Section 23a) | #13, #14 (no YAML file created until resolved) | 7 | **IMPLEMENTED** — committed in `47cfd699bb7f4893774579f1693abbbb57b91607` (code only — no YAML data file, no third-party dependency) |
| 1B-A | `tests/test_import_smoke.py` | New | Python test | Confirms the package imports cleanly, with no side effects | N/A (scaffold integrity); Decision Group 4 (Section 23a); requires only `pytest>=9.1.1,<10` | Exactly three tests: `test_package_imports`, `test_config_public_exports_import`, `test_import_has_no_filesystem_or_network_side_effect` | Any domain logic; POI/BTMM/ingestion/AI/signal/trading/profitability/database/live-provider scope | package root | Self | None — fully resolved | 8 | **IMPLEMENTED** — committed in `47cfd699bb7f4893774579f1693abbbb57b91607`; 3 tests passing |
| 1B-A | `tests/test_config_precedence.py` | New | Python test | Confirms precedence order, normalization, and secret rejection | Group 3; Decision Group 4 (Section 23a); requires only `pytest>=9.1.1,<10` | Exactly 11 named tests (Section 23a) using controlled mappings/monkeypatched environ | Real secrets; any third-party runtime dependency; real machine configuration | `config/loader.py` | Self | #13, #14 | 9 | **IMPLEMENTED** — committed in `47cfd699bb7f4893774579f1693abbbb57b91607`; 31 collected pytest cases from the 11 defined test functions (2 of which are parametrized), all passing |
| 1B-B | `src/btmm_ai_scanner/contracts/__init__.py` | New | Python | Marks `contracts` as a package; re-exports the final approved public contract API | Layer: contracts; Phase 1B-B Decision Group 5 (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §23M) | **Final exact 17-name `__all__` order (§23M):** `ContractModel`, `SHA256Fingerprint`, `SemVer`, `UUIDv7`, `CandleCompleteness`, `CandleVolumeKind`, `RawCandle`, `NormalizedCandle`, `AnalyticalEligibility`, `ValidationResult`, `ValidationStatus`, `EvidenceClassification`, `ProvenanceRecord`, `ProvenanceSourceReference`, `CompatibilityClass`, `RuleVersionManifest`, `SchemaVersionManifest`. No wildcard exports; no private validator, UUID generator, fingerprint-generation function, persistence helper, or loading helper exported; no additional public name enters Batch 1B-B without author review | Business logic | package root | Import smoke test | #19 | 10 | **IMPLEMENTED** — committed in `1b8602a5dcc97a89be51ba5ee65ab4940751567a` |
| 1B-B | `src/btmm_ai_scanner/contracts/types.py` | New | Python (Pydantic v2) | **Defines** `ContractModel` (shared frozen/strict/forbid-extra base); `UUIDv7` validation-only annotated type; `SHA256Fingerprint` validation-only annotated type; project-owned `SemVer` `RootModel` with full SemVer 2.0.0 grammar and `compare_precedence()`/`same_precedence_as()` API | Group 4 (identity, fingerprint, versioning); Phase 1B-B Decision Group 1 (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §19); Phase 1B-B Decision Group 2 (§20A–§20F) | Value-type wrappers and shared base model only — **Pydantic v2 required, dataclass placeholder rejected**; exact `ContractModel` config (`extra="forbid"`, `frozen=True`, `strict=True`, `validate_default=True`, `revalidate_instances="always"`, `allow_inf_nan=False`, `str_strip_whitespace=False`, `use_enum_values=False`); UUIDv7 validation-only (no generation); `SHA256Fingerprint` validation-only (64-char lowercase hex, no generation); `SemVer` full grammar/parsing/precedence resolved (§20D/§20E) | Any entry/risk field; **no UUID generator**; **no fingerprint calculation**; **no rich SemVer ordering methods** (`__lt__`/`__le__`/`__gt__`/`__ge__`); **no canonical-JSON functionality**; `validate_assignment`, `arbitrary_types_allowed`, `from_attributes`, `populate_by_name`, alias generators/field aliases, custom `json_encoders` | `pydantic>=2.13.4,<2.14` (approved, not yet added) | `test_identity_and_fingerprint.py` (17 functions), `test_semver.py` (15 functions) | #20, #21 (RESOLVED FOR BATCH 1B-B SCOPE, per §19C/§19D — canonical serialization and fingerprint field sets are explicitly out of this batch); SemVer grammar/parsing/comparison — **RESOLVED FOR BATCH 1B-B SCOPE by Decision Group 2** | 11 | **IMPLEMENTED** — committed in `1b8602a5dcc97a89be51ba5ee65ab4940751567a` |
| 1B-B | `src/btmm_ai_scanner/contracts/raw_candle.py` | New | Python (Pydantic v2) | Contract A (Raw Candle Record) — **exact 23-field `RawCandle` contract**, plus `CandleCompleteness`/`CandleVolumeKind` enums | Data Contracts Plan Contract A; Group 3/6 fields; Phase 1B-B Decision Group 3 (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §21A–§21E, §21H–§21K) | Exact 23 fields per §21D; `Decimal` numeric policy (no int/float/str coercion); OHLC invariants; volume rules (§21C); UTC timestamp normalization + original-timezone preservation (§21H); provider-neutral source-string strictness (§21E); `rule_version`/`contract_version`/`schema_version`/`provenance_id` | Normalization logic; entry/risk fields; any provider-specific normalization; any ingestion code; any fingerprint calculation | `contracts/types.py`, `config/enums.py` | `test_raw_candle_contract.py` (19 functions) | #18 (validation-status enum, still open); #25 (candle-close timestamp convention — §21H/§21I resolve the RawCandle/NormalizedCandle-specific contract only; the global timestamp policy remains open, BB-7); #26, #27 (provider completion/duplicate-key mechanisms, still open) | 12 | **IMPLEMENTED** — committed in `1b8602a5dcc97a89be51ba5ee65ab4940751567a` |
| 1B-B | `src/btmm_ai_scanner/contracts/normalized_candle.py` | New | Python (Pydantic v2) | Contract B (Normalized Candle Record) — **exact 26-field `NormalizedCandle` contract** | Data Contracts Plan Contract B; Phase 1B-B Decision Group 3 (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §21F–§21K) | Exact 26 fields per §21F (RawCandle's 23 + `raw_candle_id`, `symbol: InternalSymbol`, `timeframe: Timeframe`); `raw_candle_id` parent reference with `record_id` required to differ (§21G); same Decimal/OHLC/volume/timestamp policies as RawCandle | Measurement formulas; POI logic; `RawCandle.to_normalized()`/`NormalizedCandle.from_raw()`; provider-symbol or timeframe mapping; resampling; synthetic candle construction; gap filling; duplicate resolution; filesystem loading; network ingestion; any fingerprint calculation | `contracts/raw_candle.py` | `test_normalized_candle_contract.py` (19 functions) | #25 (same RawCandle/NormalizedCandle-specific resolution and open global-policy caveat as above, BB-7) | 13 | **IMPLEMENTED** — committed in `1b8602a5dcc97a89be51ba5ee65ab4940751567a` |
| 1B-B | `src/btmm_ai_scanner/contracts/validation_result.py` | New | Python | Contract N (Validation Result) shape | Data Contracts Plan Contract N | Fields per Contract N | POI/BTMM validity fields | `contracts/types.py` | `test_validation_eligibility.py` (Batch C) | #18 (status enum) | 14 | **IMPLEMENTED** — committed in `1b8602a5dcc97a89be51ba5ee65ab4940751567a` |
| 1B-B | `src/btmm_ai_scanner/contracts/provenance_record.py` | New | Python | Contract M (Provenance Record) shape | Data Contracts Plan Contract M | Fields per Contract M | Business logic | `contracts/types.py` | Covered by processing-version test | None | 15 | **IMPLEMENTED** — committed in `1b8602a5dcc97a89be51ba5ee65ab4940751567a` |
| 1B-B | `src/btmm_ai_scanner/contracts/rule_version_manifest.py` | New | Python (Pydantic v2) | Contract Q — **exact 12-field `RuleVersionManifest` contract**, plus `CompatibilityClass` enum (**in-memory contract shape only** — no manifest file I/O, no persistence, no manifest directory) | Data Contracts Plan Contract Q; Group 8; Phase 1B-B Decision Group 1 §19G; Phase 1B-B Decision Group 5 (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §23A–§23F, §23H–§23J) | Exact 5-value `CompatibilityClass` enum (§23A: `FULLY_COMPATIBLE`/`BACKWARD_COMPATIBLE`/`FORWARD_COMPATIBLE`/`INCOMPATIBLE`/`UNKNOWN`); exact 12 fields per §23B; initial-manifest consistency (§23D: no previous version ⇒ no `supersedes_manifest_id`, `compatibility_with_previous == UNKNOWN`); successor-manifest consistency (§23E: both predecessor references present together, higher SemVer precedence required, downgrades/equal-precedence/build-only-difference successors rejected); local `supersedes_manifest_id` behavior (§23F: self-supersession rejected, one direct predecessor only, no cross-record lookup) | Actual manifest content/file I/O; manifest loading; manifest persistence; manifest supersession mechanism beyond the local reference; any `manifests/` directory creation; any cross-record existence check | `contracts/types.py` | `test_manifest_compatibility_classes.py` | #15, #20 (RESOLVED FOR BATCH 1B-B SCOPE — canonical JSON serialization deferred beyond this batch, §19D/§19F) | 16 | **IMPLEMENTED** — committed in `1b8602a5dcc97a89be51ba5ee65ab4940751567a` (shape only) |
| 1B-B | `src/btmm_ai_scanner/contracts/schema_version_manifest.py` | New | Python (Pydantic v2) | Contract R — **exact 14-field `SchemaVersionManifest` contract** (**in-memory contract shape only** — no manifest file I/O, no persistence, no manifest directory, no JSON Schema file export) | Data Contracts Plan Contract R; Group 8; Phase 1B-B Decision Group 1 §19F/§19G; Phase 1B-B Decision Group 5 (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §23A, §23C–§23F, §23H–§23J) | Exact 14 fields per §23C, including `target_contract_name`/`target_contract_version` (no dynamic contract loading, no target-contract inspection); `CompatibilityClass` enum shared with Contract Q (§23A); initial-manifest consistency (§23D); successor-manifest consistency (§23E: higher SemVer precedence required, downgrades/equal-precedence/build-only-difference successors rejected); local `supersedes_manifest_id` behavior (§23F) | Actual manifest content/file I/O; manifest loading; manifest persistence; manifest supersession mechanism beyond the local reference; any `manifests/` directory creation; any JSON Schema file, schema-export script, or schema directory; any dynamic import of the target contract | `contracts/types.py` | `test_manifest_compatibility_classes.py` | #15, #20 (RESOLVED FOR BATCH 1B-B SCOPE, same as above) | 17 | **IMPLEMENTED** — committed in `1b8602a5dcc97a89be51ba5ee65ab4940751567a` (shape only) |
| 1B-B | `tests/unit/test_identity_and_fingerprint.py` | New | Python test | `ContractModel`/UUIDv7/SHA-256 behavior and distinctness | Group 4; Phase 1B-B Decision Group 2 §20G | **Exactly 17 approved test-function names** (§20G): `test_contract_model_is_frozen`, `test_contract_model_forbids_extra_fields`, `test_contract_model_rejects_type_coercion`, `test_contract_model_validates_default_values`, `test_contract_model_revalidates_nested_instances`, `test_contract_model_rejects_nan_and_infinity`, `test_uuidv7_accepts_canonical_string_and_uuid_instance`, `test_uuidv7_rejects_invalid_text`, `test_uuidv7_rejects_nil_and_non_version_seven_values`, `test_uuidv7_rejects_non_rfc_variant`, `test_uuidv7_rejects_noncanonical_text`, `test_uuidv7_serialization_modes`, `test_sha256_fingerprint_accepts_exact_lowercase_hex`, `test_sha256_fingerprint_rejects_invalid_values`, `test_sha256_fingerprint_serializes_without_normalization`, `test_identity_and_fingerprint_are_not_interchangeable`, `test_core_value_types_round_trip_through_json`. Parameterization permitted; fixed UUID values only; no UUID generation | Real production data; any UUID generation; any additional function name without review | `contracts/types.py` | Self | #21 (RESOLVED FOR BATCH 1B-B SCOPE — canonical fingerprint field-set still deferred beyond this batch) | 18 | **IMPLEMENTED** — committed in `1b8602a5dcc97a89be51ba5ee65ab4940751567a` |
| 1B-B | `tests/unit/test_semver.py` | New | Python test | Full SemVer 2.0.0 grammar and precedence behavior | Group 4; Phase 1B-B Decision Group 2 §20H | **Exactly 15 approved test-function names** (§20H): `test_semver_accepts_valid_semver_2_0_0_values`, `test_semver_rejects_invalid_values`, `test_semver_rejects_leading_zeroes`, `test_semver_preserves_exact_text`, `test_semver_parse_returns_semver`, `test_semver_is_frozen`, `test_semver_serializes_as_json_string`, `test_semver_compares_core_versions`, `test_semver_orders_prerelease_before_release`, `test_semver_compares_prerelease_identifiers`, `test_semver_ignores_build_metadata_for_precedence`, `test_semver_exact_equality_includes_build_metadata`, `test_semver_same_precedence_ignores_build_metadata`, `test_semver_does_not_define_rich_ordering`, `test_semver_round_trips_through_json`. Official SemVer 2.0.0 precedence-chain coverage required; no complete Pydantic error-prose assertions; no additional function name without review | N/A | `contracts/types.py` | Self | None — fully resolved by Decision Group 2 | 19 | **IMPLEMENTED** — committed in `1b8602a5dcc97a89be51ba5ee65ab4940751567a` |
| 1B-B | `tests/unit/test_raw_candle_contract.py` | New | Python test | `RawCandle` contract validation | Contract A; Phase 1B-B Decision Group 3 §21M | **Exactly 19 approved test-function names** (§21M): `test_raw_candle_accepts_valid_contract`, `test_raw_candle_requires_exact_field_set`, `test_raw_candle_is_frozen`, `test_raw_candle_rejects_extra_fields`, `test_raw_candle_requires_strict_decimal_inputs`, `test_raw_candle_rejects_nonpositive_prices`, `test_raw_candle_enforces_ohlc_bounds`, `test_raw_candle_validates_volume_kind_and_value`, `test_raw_candle_validates_completeness_values`, `test_raw_candle_rejects_naive_timestamps`, `test_raw_candle_normalizes_aware_canonical_times_to_utc`, `test_raw_candle_preserves_original_timestamp_offsets`, `test_raw_candle_requires_original_and_utc_instants_to_match`, `test_raw_candle_requires_event_before_availability`, `test_raw_candle_requires_processing_not_before_event`, `test_raw_candle_rejects_complete_status_before_availability`, `test_raw_candle_rejects_blank_or_padded_source_text`, `test_raw_candle_requires_version_and_provenance_types`, `test_raw_candle_round_trips_through_json`. Fixed UUIDv7 values only; no fingerprint calculation | Live market data; any additional function name without review | `contracts/raw_candle.py` | Self | #18, #25–#27 (same open items as the source file) | 20 | **IMPLEMENTED** — committed in `1b8602a5dcc97a89be51ba5ee65ab4940751567a` |
| 1B-B | `tests/unit/test_normalized_candle_contract.py` | New | Python test | `NormalizedCandle` contract validation | Contract B; Phase 1B-B Decision Group 3 §21N | **Exactly 19 approved test-function names** (§21N): `test_normalized_candle_accepts_valid_contract`, `test_normalized_candle_requires_exact_field_set`, `test_normalized_candle_is_frozen`, `test_normalized_candle_rejects_extra_fields`, `test_normalized_candle_requires_distinct_raw_and_normalized_identities`, `test_normalized_candle_requires_internal_symbol_and_timeframe_enums`, `test_normalized_candle_preserves_source_traceability_fields`, `test_normalized_candle_requires_strict_decimal_inputs`, `test_normalized_candle_rejects_nonpositive_prices`, `test_normalized_candle_enforces_ohlc_bounds`, `test_normalized_candle_validates_volume_kind_and_value`, `test_normalized_candle_validates_completeness_values`, `test_normalized_candle_rejects_naive_timestamps`, `test_normalized_candle_normalizes_aware_canonical_times_to_utc`, `test_normalized_candle_preserves_original_timestamp_offsets`, `test_normalized_candle_requires_original_and_utc_instants_to_match`, `test_normalized_candle_enforces_timestamp_ordering`, `test_normalized_candle_requires_version_and_provenance_types`, `test_normalized_candle_round_trips_through_json`. No provider API call; no market-data loading; no symbol/timeframe mapping; no synthetic candle creation | Live market data; any additional function name without review | `contracts/normalized_candle.py` | Self | #25 (same open items) | 21 | **IMPLEMENTED** — committed in `1b8602a5dcc97a89be51ba5ee65ab4940751567a` |
| 1B-B | `tests/unit/test_manifest_compatibility_classes.py` | New | Python test | `RuleVersionManifest`/`SchemaVersionManifest` contract and `CompatibilityClass` validation | Group 8; Phase 1B-B Decision Group 5 (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §23L) | **Exactly 29 approved test-function names** (§23L — count correction: 29, not 27): `test_compatibility_class_values_are_exact`, `test_rule_version_manifest_accepts_initial_manifest`, `test_rule_version_manifest_accepts_successor_manifest`, `test_rule_version_manifest_requires_exact_field_set`, `test_rule_version_manifest_is_frozen`, `test_rule_version_manifest_rejects_extra_fields`, `test_rule_version_manifest_rejects_blank_or_padded_rule_set_name`, `test_rule_version_manifest_enforces_initial_reference_consistency`, `test_rule_version_manifest_enforces_successor_reference_consistency`, `test_rule_version_manifest_requires_increasing_rule_version`, `test_rule_version_manifest_rejects_equal_precedence_successor`, `test_rule_version_manifest_rejects_self_supersession`, `test_rule_version_manifest_normalizes_effective_at_to_utc`, `test_rule_version_manifest_requires_version_evidence_and_provenance_types`, `test_rule_version_manifest_round_trips_through_json`, `test_schema_version_manifest_accepts_initial_manifest`, `test_schema_version_manifest_accepts_successor_manifest`, `test_schema_version_manifest_requires_exact_field_set`, `test_schema_version_manifest_is_frozen`, `test_schema_version_manifest_rejects_extra_fields`, `test_schema_version_manifest_rejects_blank_or_padded_names`, `test_schema_version_manifest_enforces_initial_reference_consistency`, `test_schema_version_manifest_enforces_successor_reference_consistency`, `test_schema_version_manifest_requires_increasing_schema_version`, `test_schema_version_manifest_rejects_equal_precedence_successor`, `test_schema_version_manifest_rejects_self_supersession`, `test_schema_version_manifest_normalizes_effective_at_to_utc`, `test_schema_version_manifest_requires_target_version_evidence_and_provenance_types`, `test_schema_version_manifest_round_trips_through_json`. Fixed UUIDv7 values only; no fingerprint calculation; no manifest loading; no persistence | Real manifest files; any additional function name without review | `contracts/rule_version_manifest.py`, `contracts/schema_version_manifest.py` | Self | #15, #20 | 22 | **IMPLEMENTED** — committed in `1b8602a5dcc97a89be51ba5ee65ab4940751567a` |
| 1B-B | `tests/unit/test_validation_result.py` | New | Python test | `ValidationResult` contract validation | Contract N; Phase 1B-B Decision Group 4 (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §22M) | **Exactly 16 approved test-function names** (§22M): `test_validation_result_accepts_valid_contract`, `test_validation_result_requires_exact_field_set`, `test_validation_result_is_frozen`, `test_validation_result_rejects_extra_fields`, `test_validation_result_requires_distinct_record_and_subject_ids`, `test_validation_result_rejects_blank_or_padded_profile`, `test_validation_result_validates_status_values`, `test_validation_result_validates_eligibility_values`, `test_validation_result_enforces_status_eligibility_consistency`, `test_validation_result_validates_reason_code_format`, `test_validation_result_rejects_duplicate_reason_codes`, `test_validation_result_requires_reasons_for_noneligible_or_nonvalid_results`, `test_validation_result_rejects_naive_evaluated_at`, `test_validation_result_normalizes_evaluated_at_to_utc`, `test_validation_result_requires_version_and_provenance_types`, `test_validation_result_round_trips_through_json`. Fixed UUIDv7 values only; no fingerprint calculation | Live market data; any additional function name without review | `contracts/validation_result.py` | Self | #18 (validation-status enum finality — this test file exercises the approved shape only) | 23 | **IMPLEMENTED** — committed in `1b8602a5dcc97a89be51ba5ee65ab4940751567a` |
| 1B-B | `tests/unit/test_provenance_record.py` | New | Python test | `ProvenanceRecord`/`ProvenanceSourceReference` contract validation | Contract M; Phase 1B-B Decision Group 4 (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §22N) | **Exactly 17 approved test-function names** (§22N): `test_provenance_source_reference_accepts_valid_values`, `test_provenance_source_reference_requires_exact_field_set`, `test_provenance_source_reference_rejects_blank_or_padded_reference`, `test_provenance_record_accepts_valid_contract`, `test_provenance_record_requires_exact_field_set`, `test_provenance_record_is_frozen`, `test_provenance_record_rejects_extra_fields`, `test_provenance_record_requires_distinct_record_and_subject_ids`, `test_provenance_record_requires_nonempty_sources`, `test_provenance_record_rejects_duplicate_sources`, `test_provenance_record_rejects_self_source_references`, `test_provenance_record_validates_evidence_classification`, `test_provenance_record_rejects_naive_created_at`, `test_provenance_record_normalizes_created_at_to_utc`, `test_provenance_record_validates_parent_provenance_ids`, `test_provenance_record_requires_version_types`, `test_provenance_record_round_trips_through_json`. No source loading; no network access; fixed UUIDv7 values only; no fingerprint calculation | Live market data; any additional function name without review | `contracts/provenance_record.py` | Self | None — fully resolved by Decision Group 4 | 24 | **IMPLEMENTED** — committed in `1b8602a5dcc97a89be51ba5ee65ab4940751567a` |
| 1B-C | `src/btmm_ai_scanner/validation/__init__.py` | New | Python | Marks `validation` as a package | Layer: validation | Empty/minimal | Business logic | package root | Import smoke test | None | 25 | Required in later foundation batch |
| 1B-C | `src/btmm_ai_scanner/validation/completeness.py` | New | Python | `candle_completeness_status` classification logic (shape/interface only) | Group 6 | Enum + classification function signature | Provider-specific detection logic | `contracts/raw_candle.py` | `test_candle_completeness.py` | #26 | 26 | Required in later foundation batch |
| 1B-C | `src/btmm_ai_scanner/validation/duplicates.py` | New | Python | `duplicate_classification` logic (shape/interface only) | Group 6 | Enum + candidate-key function signature | Automatic conflict-winner logic | `contracts/raw_candle.py` | `test_exact_duplicate.py`, `test_conflicting_duplicate_quarantine.py` | #27 | 27 | Required in later foundation batch |
| 1B-C | `src/btmm_ai_scanner/validation/gaps.py` | New | Python | `gap_status` logic (shape/interface only) | Group 6 | Enum + gap-detection function signature | Synthetic-fill logic of any kind | `contracts/raw_candle.py` | `test_missing_gap.py`, `test_no_synthetic_fill.py` | #26 | 28 | Required in later foundation batch |
| 1B-C | `src/btmm_ai_scanner/validation/eligibility.py` | New | Python | 8-step normalization/measurement eligibility gate | `PROVENANCE_VALIDATION_AND_AUDIT_PLAN.md` SS9a | Eligibility-gating function per the approved 8-step sequence | POI/BTMM validity decisions | `validation/completeness.py`, `duplicates.py`, `gaps.py`, `contracts/validation_result.py` | `test_validation_eligibility.py` | #18 | 29 | Required in later foundation batch |
| 1B-C | `tests/unit/test_candle_completeness.py` | New | Python test | Completeness classification correctness | Group 6 | Positive/negative/boundary cases | Live data | `validation/completeness.py` | Self | #26 | 30 | Required in later foundation batch |
| 1B-C | `tests/unit/test_exact_duplicate.py` | New | Python test | Exact-duplicate handling | Group 6 | Preserve-both-records assertions | Live data | `validation/duplicates.py` | Self | #27 | 31 | Required in later foundation batch |
| 1B-C | `tests/unit/test_conflicting_duplicate_quarantine.py` | New | Python test | Conflicting-duplicate quarantine behavior | Group 6 | No-silent-winner assertions | Automatic resolution logic | `validation/duplicates.py` | Self | #27 | 32 | Required in later foundation batch |
| 1B-C | `tests/unit/test_missing_gap.py` | New | Python test | Gap-status transitions | Group 6 | State-transition assertions | Live data | `validation/gaps.py` | Self | #26 | 33 | Required in later foundation batch |
| 1B-C | `tests/unit/test_no_synthetic_fill.py` | New | Python test | No forward/back fill ever occurs | Group 6 | Negative assertions against every fill technique | Any fill implementation | `validation/gaps.py` | Self | None | 34 | Required in later foundation batch |
| 1B-C | `tests/unit/test_validation_eligibility.py` | New | Python test | 8-step gating sequence correctness | SS9a | Step-by-step gating assertions | POI/BTMM logic | `validation/eligibility.py` | Self | #18 | 35 | Required in later foundation batch |
| 1B-C | `tests/unit/test_processing_version_preservation.py` | New | Python test | `processing_version` never silently overwritten | Group 4 | Reprocessing-creates-new-record assertions | Live data | `contracts/types.py` | Self | None | 36 | Required in later foundation batch |
| 1B-D | `src/btmm_ai_scanner/audit/__init__.py` | New | Python | Marks `audit` as a package | Layer: audit | Empty/minimal | Business logic | package root | Import smoke test | None | 37 | Required in later foundation batch |
| 1B-D | `src/btmm_ai_scanner/audit/events.py` | New | Python | Contract O (Audit Event) shape | Contract O | Fields per Contract O | Passwords/tokens/API keys/private-book text | `contracts/types.py` | `test_secret_redaction.py` | None | 38 | Required in later foundation batch |
| 1B-D | `src/btmm_ai_scanner/audit/writer.py` | New | Python | Append-only audit-writer interface (JSONL) | Group 4/5 | Interface only, no live file I/O target chosen | Any mutation of a written event | `audit/events.py` | `test_audit_log_separation.py` | #22 (physical storage encoding, deferred) | 39 | Required in later foundation batch (interface only) |
| 1B-D | `src/btmm_ai_scanner/observability/__init__.py` | New | Python | Marks `observability` as a package | Layer: cross-cutting | Empty/minimal | Business logic | package root | Import smoke test | None | 40 | Required in later foundation batch |
| 1B-D | `src/btmm_ai_scanner/observability/logging_config.py` | New | Python | Structured JSON operational-logging configuration | Group 5 | stdlib `logging` configuration, JSON formatter | Audit-event writing (kept separate) | None | `test_audit_log_separation.py`, `test_secret_redaction.py` | None | 41 | Required in later foundation batch |
| 1B-D | `tests/unit/test_audit_log_separation.py` | New | Python test | Audit and operational logs never conflated | Group 4/5 | Separate-store assertions | N/A | `audit/writer.py`, `observability/logging_config.py` | Self | None | 42 | Required in later foundation batch |
| 1B-D | `tests/unit/test_secret_redaction.py` | New | Python test | No secret ever appears in a log or audit event | Group 4 audit exclusions | Redaction assertions for passwords/tokens/API keys/`.env` content | Any real secret value (only synthetic test doubles) | `audit/events.py`, `observability/logging_config.py` | Self | None | 43 | Required in later foundation batch |
| 1B-E | `src/btmm_ai_scanner/ingestion/__init__.py` | New | Python | Marks `ingestion` as a package | Layer: ingestion | Empty/minimal | Provider-specific code | package root | Import smoke test | None | 44 | **AUTHOR-APPROVED SCOPE** (§31N); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§31O — commit `0a9814eddd1cdeda59cf95dbde8a806f30800b44`), **NOT PRODUCTION-APPROVED** |
| 1B-E | `src/btmm_ai_scanner/ingestion/port.py` | New | Python | `MarketDataSourcePort` interface | Group 7 | Abstract interface only | Any concrete provider implementation | `ingestion/requests.py`, `ingestion/results.py`, `typing.Protocol` (corrected — see Section 36 / register §31, resolving the prior stale `contracts/raw_candle.py` dependency, since `RawCandle` is explicitly prohibited from the port signature) | `test_ingestion_port_contract.py` | None | 45 | **AUTHOR-APPROVED SCOPE** (§31N); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§31O — commit `0a9814eddd1cdeda59cf95dbde8a806f30800b44`), **NOT PRODUCTION-APPROVED** |
| 1B-E | `src/btmm_ai_scanner/ingestion/requests.py` | New | Python | Provider-neutral ingestion request shape | Group 7 | Provider-neutral fields only | Provider-specific fields, aliases | `contracts/types.py` | `test_ingestion_port_contract.py` | None | 46 | **AUTHOR-APPROVED SCOPE** (§31N); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§31O — commit `0a9814eddd1cdeda59cf95dbde8a806f30800b44`), **NOT PRODUCTION-APPROVED** |
| 1B-E | `src/btmm_ai_scanner/ingestion/results.py` | New | Python | Provider-neutral ingestion result shape | Group 7 | Version references (`adapter_version`, etc.) | Signals, trades, measurements | `contracts/types.py` (for `ContractModel`/validation) and `market_data/source_input.py` (for `SourceCandleInput`, corrected — see Section 36 / register §31) | `test_ingestion_port_contract.py` | None | 47 | **AUTHOR-APPROVED SCOPE** (§31N); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§31O — commit `0a9814eddd1cdeda59cf95dbde8a806f30800b44`), **NOT PRODUCTION-APPROVED** |
| 1B-E | `src/btmm_ai_scanner/ingestion/offline_file_source.py` | New | Python | `OFFLINE_FILE` deterministic stub adapter — receives caller-supplied immutable `SourceCandleInput` fixtures, no real file I/O in this first batch (corrected wording, see Section 36 / register §31F) | Group 7 | Deterministic lookup against caller-supplied fixtures only | Any network call, any credential, any `open()`/filesystem access, any real parsing of a file format | `ingestion/port.py` (implements `MarketDataSourcePort`), `ingestion/requests.py` (`SourceAcquisitionRequest`, the catalogue key), `ingestion/results.py` (`SourceAcquisitionResult`, `acquire()`'s return type), `market_data/source_input.py` (`SourceCandleInput`, the catalogue value), `types.MappingProxyType` and `collections.abc.Mapping` (stdlib — internal catalogue wrapping and constructor input typing) — completed, see Section 36 / register §31C | `test_offline_file_stub.py` | None | 48 | **AUTHOR-APPROVED SCOPE** (§31N); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§31O — commit `0a9814eddd1cdeda59cf95dbde8a806f30800b44`), **NOT PRODUCTION-APPROVED** |
| 1B-E | `tests/unit/test_ingestion_port_contract.py` | New | Python test | Port stays provider-neutral | Group 7 | Provider-neutral-only assertions | Provider-specific test doubles beyond `OFFLINE_FILE` | `ingestion/port.py` | Self | None | 49 | **AUTHOR-APPROVED SCOPE** (§31N); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§31O — commit `0a9814eddd1cdeda59cf95dbde8a806f30800b44`), **NOT PRODUCTION-APPROVED** |
| 1B-E | `tests/unit/test_offline_file_stub.py` | New | Python test | Deterministic offline behavior, no network | Group 7 | No-network-access assertion | Live network call | `ingestion/offline_file_source.py` | Self | None | 50 | **AUTHOR-APPROVED SCOPE** (§31N); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§31O — commit `0a9814eddd1cdeda59cf95dbde8a806f30800b44`), **NOT PRODUCTION-APPROVED** |
| 1B-F | `.github/workflows/ci.yml` | New | YAML (CI workflow) | Runs Ruff format check, Ruff lint, mypy, pytest | Group 5 | Offline-safe job steps only | Credentials, deployment steps, live connections | `pyproject.toml`, `uv.lock` | N/A (the workflow itself is verification) | #28 (trigger policy), #29–#30 (repo settings, outside file scope) | 51 | Required in later foundation batch |
| 1B-C-MD | `src/btmm_ai_scanner/market_data/source_input.py` | New | Python (Pydantic v2) | `SourceCandleInput` — 23-field, `RawCandle`-mirroring, caller-supplied input contract with nullable availability fields | Phase 1B-C Decision Group 2 §29B (corrected) | Exact 23 fields per §29B, no default on any field; structural/type validation owns required-key validation, strict field types, datetime parsing, timezone-awareness, UTC normalization, offset preservation, and Decimal/string structural rules — **including full awareness/format validation of both nullable availability fields (naive/malformed values fail construction with `ValidationError`, never reach a builder)**; only the cross-field pairing and instant-match business rules are deferred to §29E | Any `RawCandle`/`NormalizedCandle` construction; any provider networking or payload parsing; any UUID/fingerprint generation | `contracts/types.py`, `contracts/raw_candle.py` (shared helpers) | `test_source_input_and_results.py` (8) | None — resolved and author-approved by Decision Group 2 (§29O) | 52 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`), **NOT PRODUCTION-APPROVED** |
| 1B-C-MD | `src/btmm_ai_scanner/market_data/results.py` | New | Python (Pydantic v2) | `IngestionOutcome` (5-value enum) and `IngestionResult` (5-field, outcome-matrix-validated) shared pipeline result vocabulary | Phase 1B-C Decision Group 2 §29C (corrected) | Exact 5-value enum; exact 5-field model; exact enforceable outcome-to-field matrix per §29C (model-validator-enforced, not calling-convention-only); exact eight-code closed reason-code vocabulary (including `CONFLICTING_REVISION_DETECTED`) | Any `NEW_RECORD`/`IdempotencyOutcome` duplicate enum; any "everything optional" field design; any reason code outside the eight-code list | `contracts/types.py` | `test_source_input_and_results.py` (8, shared with `source_input.py`) | None — resolved and author-approved by Decision Group 2 (§29O) | 53 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`), **NOT PRODUCTION-APPROVED** |
| 1B-C-MD | `tests/unit/test_source_input_and_results.py` | New | Python test | `SourceCandleInput` structural validation and `IngestionOutcome`/`IngestionResult` exactness | Phase 1B-C Decision Group 2 §29K | Exactly 8 approved test-function names (§29K) | Any pipeline execution (builder/normalization/idempotency) — model-level tests only | `source_input.py`, `results.py` | Self | None — resolved and author-approved by Decision Group 2 (§29O) | 54 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`), **NOT PRODUCTION-APPROVED** |
| 1B-C-MD | `src/btmm_ai_scanner/market_data/source_mapping.py` | New | Python | FXCM provider-symbol/provider-timeframe → `InternalSymbol`/`Timeframe` mapping registry and resolver functions | Phase 1B-C Decision Group 2 §29D | `FXCM_PROVIDER` constant; 3 exception classes; `resolve_internal_symbol`/`resolve_timeframe`; exact-match, case-sensitive, no-trim lookup | Any TradingView-reference lookup function; any network/dynamic-config/runtime-mutable registry; any synthetic symbol/timeframe | `config/enums.py` | `test_source_mapping.py` (7) | None — resolved and author-approved by Decision Group 2 (§29O) | 55 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`), **NOT PRODUCTION-APPROVED** |
| 1B-C-MD | `tests/unit/test_source_mapping.py` | New | Python test | FXCM mapping correctness, unsupported-input rejection, case sensitivity, TradingView-lookup absence | Phase 1B-C Decision Group 2 §29K | Exactly 7 approved test-function names (§29K) | Live provider connection | `source_mapping.py` | Self | None — resolved and author-approved by Decision Group 2 (§29O) | 56 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`), **NOT PRODUCTION-APPROVED** |
| 1B-C-MD | `src/btmm_ai_scanner/market_data/raw_candle_builder.py` | New | Python | `build_historical_raw_candle`/`build_live_raw_candle` — stateless `SourceCandleInput → IngestionResult` construction, implementing only the 4-case AT-1 availability decision matrix reachable after `SourceCandleInput`'s own structural validation already passed | Phase 1B-C Decision Group 2 §29E (corrected) | Exact 2 public functions sharing one private helper; exact 4-case outcome mapping per §29E (both-`None`/one-`None`/instant-mismatch/consistent); no wall-clock call; no UUID/fingerprint/provenance/version generation | Any `datetime.now()`/wall-clock call; any provider payload parsing; any exception leakage for an expected validation failure; any handling of malformed/naive availability values (impossible — rejected earlier at `SourceCandleInput` construction) | `source_input.py`, `results.py`, `contracts/raw_candle.py` | `test_raw_candle_builder.py` (8) | None — resolved and author-approved by Decision Group 2 (§29O) | 57 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`), **NOT PRODUCTION-APPROVED** |
| 1B-C-MD | `src/btmm_ai_scanner/market_data/normalization.py` | New | Python | `normalize_raw_candle` — `RawCandle → NormalizedCandle`, all six normalized-only values (`record_id`/`content_fingerprint`/`rule_version`/`contract_version`/`schema_version`/`provenance_id`) independently caller-supplied, none inherited from `RawCandle`, none generated | Phase 1B-C Decision Group 2 §29F (corrected) | Exact function signature (six required keyword-only parameters: `normalized_record_id`, `normalized_content_fingerprint`, `normalized_rule_version`, `normalized_contract_version`, `normalized_schema_version`, `normalized_provenance_id`); preserves `raw_candle_id` lineage; exact rejection reason codes | Any UUID/fingerprint/version generation; any silent copying of version/provenance fields from `RawCandle`; any resampling or synthetic candle construction | `source_mapping.py`, `results.py`, `contracts/normalized_candle.py` | `test_normalization.py` (7) | None — resolved and author-approved by Decision Group 2 (§29O) | 58 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`), **NOT PRODUCTION-APPROVED** |
| 1B-C-MD | `tests/unit/test_raw_candle_builder.py` | New | Python test | End-to-end availability-decision-matrix coverage via the real builder functions | Phase 1B-C Decision Group 2 §29K | Exactly 8 approved test-function names (§29K) | Any provider payload parsing | `raw_candle_builder.py` | Self | None — resolved and author-approved by Decision Group 2 (§29O) | 59 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`), **NOT PRODUCTION-APPROVED** |
| 1B-C-MD | `tests/unit/test_normalization.py` | New | Python test | Normalization lineage, unsupported-mapping rejection, caller-supplied identity reuse | Phase 1B-C Decision Group 2 §29K | Exactly 7 approved test-function names (§29K) | Live market data | `normalization.py` | Self | None — resolved and author-approved by Decision Group 2 (§29O) | 60 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`), **NOT PRODUCTION-APPROVED** |
| 1B-C-MD | `src/btmm_ai_scanner/market_data/idempotency.py` | New | Python | `evaluate_idempotency` — stateless duplicate/conflict detection against existing `RawCandle` records; resolves the `NEW_RECORD`→`ACCEPTED` mapping | Phase 1B-C Decision Group 2 §29G | `SourceCandleIdentity` type alias (private); exact function signature; exact algorithm and edge-case resolutions per §29G | Any persistence, mutation of `existing_raw_candles`, automatic revision winner, or hidden global state | `results.py`, `contracts/raw_candle.py`, `contracts/normalized_candle.py` | `test_idempotency.py` (8) | None — resolved and author-approved by Decision Group 2 (§29O) | 61 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`), **NOT PRODUCTION-APPROVED** |
| 1B-C-MD | `src/btmm_ai_scanner/market_data/gap_observation.py` | New | Python | `GapClassification` (`POTENTIAL_GAP` only), `GapObservation` model, `observe_potential_gap` function | Phase 1B-C Decision Group 2 §29H (corrected) | Exact 10-field model per §29H; exact per-`Timeframe` expected-interval table; exact 7-step validation order (cross-symbol, cross-timeframe, out-of-order/same-time, exact-interval no-gap, shorter-than-expected, irregular non-integer-multiple alignment, exact-multiple gap); `missing_interval_count` computed via exact integer floor-division only after divisibility validation, never truncation of an unvalidated remainder | Any `CONFIRMED_GAP`/`EXPECTED_MARKET_CLOSURE` classification; any candle fabrication or interpolation; any calendar/session logic; any silent truncation of a non-integer-multiple interval | `contracts/normalized_candle.py`, `config/enums.py` | `test_gap_observation.py` (7) | None — resolved and author-approved by Decision Group 2 (§29O) | 62 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`), **NOT PRODUCTION-APPROVED** |
| 1B-C-MD | `tests/unit/test_idempotency.py` | New | Python test | Exact-duplicate, conflicting-revision, new-record, and multi-match edge-case coverage | Phase 1B-C Decision Group 2 §29K | Exactly 8 approved test-function names (§29K) | Any persistence/database access | `idempotency.py` | Self | None — resolved and author-approved by Decision Group 2 (§29O) | 63 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`), **NOT PRODUCTION-APPROVED** |
| 1B-C-MD | `tests/unit/test_gap_observation.py` | New | Python test | Potential-gap creation, no-gap case, cross-symbol/timeframe and out-of-order protection | Phase 1B-C Decision Group 2 §29K | Exactly 7 approved test-function names (§29K) | Live market data | `gap_observation.py` | Self | None — resolved and author-approved by Decision Group 2 (§29O) | 64 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`), **NOT PRODUCTION-APPROVED** |
| 1B-C-MD | `src/btmm_ai_scanner/market_data/ports.py` | New | Python | 4 storage/replay `typing.Protocol` definitions: `RawCandleSink`, `NormalizedCandleSink`, `CandleReadRepository`, `HistoricalReplaySource` | Phase 1B-C Decision Group 2 §29I (corrected) | Exact method signatures per §29I; not `@runtime_checkable`; `HistoricalReplaySource` explicitly performs no wall-clock waiting, sleeping, or live-stream timing | Any production implementation of any protocol; any network/database code; any wall-clock wait inside `HistoricalReplaySource.replay()` | `contracts/raw_candle.py`, `contracts/normalized_candle.py` | `test_ingestion_ports.py` (6) | None — resolved and author-approved by Decision Group 2 (§29O) | 65 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`), **NOT PRODUCTION-APPROVED** |
| 1B-C-MD | `src/btmm_ai_scanner/market_data/__init__.py` | New | Python | Marks `market_data` as a package; re-exports the exact 25-name approved public API (20 original + 5 appended by Batch `1B-G-REPLAY`) | Phase 1B-C Decision Group 2 §29J; extended by register §32J | Exact 20-name `__all__` list per §29J, exact order, plus the 5 names appended per §32J (order preserved, nothing removed) | Any private helper export; any export beyond the 25 named symbols | All 8 original sibling source files, plus `raw_candle_repository.py`, `normalized_candle_repository.py`, `historical_replay.py` | Import smoke test (`test_ingestion_ports.py`/`test_historical_live_separation_and_no_synthetic_fabrication.py` exercise imports indirectly); `test_market_data_repository_and_replay_exports_import_successfully` (§32K) verifies the full 25-name list | None — resolved and author-approved by Decision Group 2 (§29O). **The controlled append-only extension to 25 names (register §32J) is now implemented and committed — see Section 37 / register §32P.** | 66 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`; export extension §32P — commit `5a1d8f30ee0eb67d27417fda9fb7407d9a5e8a85`), **NOT PRODUCTION-APPROVED** |
| 1B-C-MD | `tests/unit/test_ingestion_ports.py` | New | Python test | Port protocol conformance via local in-memory test doubles; read-only `CandleReadRepository` enforcement | Phase 1B-C Decision Group 2 §29K | Exactly 6 approved test-function names (§29K) | Any production repository/sink implementation | `ports.py` | Self | None — resolved and author-approved by Decision Group 2 (§29O) | 67 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`), **NOT PRODUCTION-APPROVED** |
| 1B-C-MD | `tests/unit/test_historical_live_separation_and_no_synthetic_fabrication.py` | New | Python test | Historical/live builder state separation, immutability, deterministic replay, no synthetic fabrication | Phase 1B-C Decision Group 2 §29K | Exactly 6 approved test-function names (§29K) | Any network/database/parser implementation (verified structurally, not by a dedicated negative test — §29K) | `raw_candle_builder.py`, `ports.py` | Self | None — resolved and author-approved by Decision Group 2 (§29O) | 68 | **AUTHOR-APPROVED SCOPE** (§28); **IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED** (§30 — commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`), **NOT PRODUCTION-APPROVED** |
| 1B-G-REPLAY | `src/btmm_ai_scanner/market_data/raw_candle_repository.py` | New | Python | `InMemoryRawCandleRepository`, `RecordIdentityConflictError`, `InvalidTimeRangeError` — implements `RawCandleSink`+`CandleReadRepository`, keyed by `record_id`, conflicting revisions observable, no automatic winner; owns the shared repository exception vocabulary | Register §32D/§32I | Exact design per §32D: no-silent-overwrite (5-case duplicate/revision policy), `None`-unbounded half-open range queries, `(event_time_utc, record_id)` ordering | Any database, filesystem, or network I/O; any automatic revision-winner selection; a separate `repository_errors.py` file | `ports.py`, `contracts/raw_candle.py` | `test_raw_candle_repository.py` (8) | None — implemented and closed per §32P | 69 | **AUTHOR-APPROVED**; **IMPLEMENTED, VERIFIED, ARCHITECTURALLY AUDITED, COMMITTED, PUSHED, CLOSED** (§32P — commit `5a1d8f30ee0eb67d27417fda9fb7407d9a5e8a85`), **NOT PRODUCTION-APPROVED** |
| 1B-G-REPLAY | `src/btmm_ai_scanner/market_data/normalized_candle_repository.py` | New | Python | `InMemoryNormalizedCandleRepository` — implements `NormalizedCandleSink` only (does not satisfy `CandleReadRepository`, a discovered structural mismatch — §32A); imports its exceptions from `raw_candle_repository.py` | Register §32E | Exact design per §32E: no-silent-overwrite (5-case duplicate/revision policy), `None`-unbounded half-open range queries by `InternalSymbol`/`Timeframe`, `(event_time_utc, record_id)` ordering | Any database, filesystem, or network I/O; any synthetic candle, interpolation, or automatic gap repair; a duplicate exception-class definition | `ports.py`, `contracts/normalized_candle.py`, `config/enums.py`, `raw_candle_repository.py` (exception imports) | `test_normalized_candle_repository.py` (8) | None — implemented and closed per §32P | 70 | **AUTHOR-APPROVED**; **IMPLEMENTED, VERIFIED, ARCHITECTURALLY AUDITED, COMMITTED, PUSHED, CLOSED** (§32P — commit `5a1d8f30ee0eb67d27417fda9fb7407d9a5e8a85`), **NOT PRODUCTION-APPROVED** |
| 1B-G-REPLAY | `src/btmm_ai_scanner/market_data/historical_replay.py` | New | Python | `InMemoryHistoricalReplaySource` — implements `HistoricalReplaySource`, snapshot-based deterministic cursor sequencer, availability-driven ordering, atomic equal-availability group release | Register §32F/§32G/§32H | Exact design per §32F: immutable sorted snapshot, `.position`/`.is_exhausted`/`advance_next_availability_group()`/`.reset()`, availability-governed no-look-ahead ordering | Any repository query performed directly by the replay source; any `RawCandle` construction, normalization, or idempotency evaluation; any wall-clock call; any single-candle `advance_one()` method | `ports.py`, `contracts/normalized_candle.py` | `test_historical_replay.py` (8), `test_no_look_ahead.py` (7) | None — implemented and closed per §32P | 71 | **AUTHOR-APPROVED**; **IMPLEMENTED, VERIFIED, ARCHITECTURALLY AUDITED, COMMITTED, PUSHED, CLOSED** (§32P — commit `5a1d8f30ee0eb67d27417fda9fb7407d9a5e8a85`), **NOT PRODUCTION-APPROVED** |
| 1B-G-REPLAY | `tests/unit/test_raw_candle_repository.py` | New | Python test | Storage/query, conflicting-revision observability, no-silent-overwrite, exception vocabulary, range boundary/invalid-range rejection, ordering, mutation isolation, Protocol conformance | Register §32K | Exactly 8 approved test-function names (§32K) | Any real database/filesystem/network access | `raw_candle_repository.py` | Self | None — implemented and closed per §32P | 72 | **AUTHOR-APPROVED**; **IMPLEMENTED, VERIFIED, ARCHITECTURALLY AUDITED, COMMITTED, PUSHED, CLOSED** (§32P — commit `5a1d8f30ee0eb67d27417fda9fb7407d9a5e8a85`), **NOT PRODUCTION-APPROVED** |
| 1B-G-REPLAY | `tests/unit/test_normalized_candle_repository.py` | New | Python test | Storage/query, conflicting-revision observability, no-silent-overwrite, exception vocabulary, range boundary/invalid-range rejection, ordering, mutation isolation, `NormalizedCandleSink`-only conformance | Register §32K | Exactly 8 approved test-function names (§32K) | Any real database/filesystem/network access | `normalized_candle_repository.py` | Self | None — implemented and closed per §32P | 73 | **AUTHOR-APPROVED**; **IMPLEMENTED, VERIFIED, ARCHITECTURALLY AUDITED, COMMITTED, PUSHED, CLOSED** (§32P — commit `5a1d8f30ee0eb67d27417fda9fb7407d9a5e8a85`), **NOT PRODUCTION-APPROVED** |
| 1B-G-REPLAY | `tests/unit/test_historical_replay.py` | New | Python test | Ordering, atomic availability-group advancement, end-of-stream, repeated-run/reset reproducibility, empty replay, Protocol conformance, package-export verification (5 new/25 total) | Register §32K | Exactly 8 approved test-function names (§32K) | Any real database/filesystem/network access | `historical_replay.py` | Self | None — implemented and closed per §32P | 74 | **AUTHOR-APPROVED**; **IMPLEMENTED, VERIFIED, ARCHITECTURALLY AUDITED, COMMITTED, PUSHED, CLOSED** (§32P — commit `5a1d8f30ee0eb67d27417fda9fb7407d9a5e8a85`), **NOT PRODUCTION-APPROVED** |
| 1B-G-REPLAY | `tests/unit/test_no_look_ahead.py` | New | Python test | Availability-governed visibility, atomic equal-availability tie-breaking, event-time/processing-time non-substitution, unavailable-availability structural policy | Register §32G/§32K | Exactly 7 approved test-function names (§32K) | Any real database/filesystem/network access | `historical_replay.py`, `contracts/normalized_candle.py` | Self | None — implemented and closed per §32P | 75 | **AUTHOR-APPROVED**; **IMPLEMENTED, VERIFIED, ARCHITECTURALLY AUDITED, COMMITTED, PUSHED, CLOSED** (§32P — commit `5a1d8f30ee0eb67d27417fda9fb7407d9a5e8a85`), **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `src/btmm_ai_scanner/measurements/__init__.py` | New | Python | Marks `measurements` as a package; exports 10 shared candle/ATR primitives | Register §33U/§33W | Exact 10-name `__all__` list per §33W | Any threshold decision itself; any POI/BTMM logic | `contracts`, `config` | Import smoke test (§33V test_domain_exports.py) | None — the author approved this milestone in full (§33AB) | 76 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `src/btmm_ai_scanner/measurements/candle_metrics.py` | New | Python | Total Range, Body, wick shares, close positions, size ratio, range context ratio | Register §33 (Candle Measurement/Small Candle/Volume-Momentum Standards, unmodified) | Exact formulas per `knowledge/MEASUREMENT_STANDARDS.md`, unmodified; `Decimal` only | Any new numeric threshold; any float arithmetic | `contracts/normalized_candle.py` | Exercised indirectly via `domain/` detector tests | None — the author approved this milestone in full (§33AB) | 77 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `src/btmm_ai_scanner/measurements/atr.py` | New | Python | Wilder-smoothed ATR(14) series | Register §33C item 1 / §33L | Exact Wilder True Range/smoothing formula; per symbol/timeframe only, no cross-sequence state | Any alternative ATR method invented without the §33C label; any wall-clock call | `contracts/normalized_candle.py` | Exercised indirectly via `domain/` detector tests | None — the author approved this milestone in full (§33AB) | 78 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `src/btmm_ai_scanner/measurements/legs.py` | New | Python | Shared leg-measurement primitives; FAST/STRONG_FAST/SLOW_OR_UNCLEAR classification | Register §33J/§33N (Market Speed and Displacement Standard §6–§7, unmodified) | Exact leg formulas per the cited standard; zero-denominator protection | Any FVG-displacement or BTMM-anchor-specific logic (POI/BTMM scope) | `measurements/candle_metrics.py`, `measurements/atr.py` | Exercised indirectly via `test_support_resistance.py` | None — the author approved this milestone in full (§33AB) | 79 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `src/btmm_ai_scanner/domain/__init__.py` | New | Python | Marks `domain` as a package; exports 23 enums/contracts/config/errors/protocol/API names | Register §33U/§33W | Exact 23-name `__all__` list per §33W | Any private helper, cursor, or internal-state export | All 8 sibling `domain/` files | `test_domain_exports.py` | None — the author approved this milestone in full (§33AB) | 80 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `src/btmm_ai_scanner/domain/configuration.py` | New | Python (Pydantic v2) | `MarketMeasurementConfiguration` — one immutable rule set for every threshold this milestone implements | Register §33M | Exact field list/defaults per §33M, each traceable to a cited `MEASUREMENT_STANDARDS.md` section; single required `minimum_price_tick: Decimal` field, no invented default | Any threshold not traceable to a cited section or §33C gap-fill; any float field | `contracts/types.py`, `config/enums.py` | `test_market_measurement_configuration.py` (6) | None — the author approved this milestone in full (§33AB) | 81 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `src/btmm_ai_scanner/domain/enums.py` | New | Python | All new closed-vocabulary enums shared across `domain/` contracts (one file, not one per enum) | Register §33I–§33O | Exact enum members per each cited standard's approved states only — no unapproved state (e.g. no `SWEPT`/`BROKEN`, no `BOS`/`CHoCH` *value*) | Any structure-direction/BOS/CHoCH **value** enum (§33B, `P0G-B003`) — **narrowed by author-approved `1B-I-STRUCTURE` (register §34K/§34Q/§34Y, `AUTHOR-APPROVED`, `APPROVED FOR CONTROLLED IMPLEMENTATION`):** that milestone adds exactly 3 new `DerivedOutputType` *routing* members (`SWING_RELATIONSHIP`, `STRUCTURE_TRANSITION`, `CURRENT_STRUCTURE_STATE`) to this file's existing `DerivedOutputType` enum — identity-provider discriminant tags only, not BOS/CHoCH/direction *value* enums, which remain confined to the new `structure/enums.py` package. Author-approved; not yet implemented, no member has been added yet. **A second, narrow exception is author-approved for `1B-J-POI` (register §35F/§35AH/§35AP, `AUTHOR-APPROVED`, `APPROVED FOR CONTROLLED IMPLEMENTATION`):** that milestone adds exactly 3 further new `DerivedOutputType` *routing* members (`POI_OBSERVATION`, `POI_LIFECYCLE_TRANSITION`, `CURRENT_POI_STATE`) to this same existing `DerivedOutputType` enum — identity-provider discriminant tags only, not POI-type/direction/lifecycle *value* enums, which remain confined to the new `poi/enums.py` package. Author-approved; not yet implemented, no member has been added yet. | `config/enums.py` | Exercised indirectly via `domain/` detector tests | None — the author approved this milestone in full (§33AB); a narrow, disclosed exception is author-approved for `1B-I-STRUCTURE` (register §34Q/§34Y) and a second, narrow, disclosed exception is author-approved for `1B-J-POI` (register §35F/§35AP) | 82 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `src/btmm_ai_scanner/domain/swings.py` | New | Python (Pydantic v2) | `ConfirmedSwing` contract and Meaningful Swing High/Low detection | Register §33I (Ambiguity 10, unmodified) | Five-candle pivot window, Pivot Tie Tolerance plateau handling, Meaningful Reversal Threshold confirmation, alternating sequence, non-repainting availability, exactly as approved | Any HH/HL/LH/LL/BOS/CHoCH label; any "materially breached" invented threshold | `measurements/candle_metrics.py`, `measurements/atr.py`, `domain/configuration.py`, `domain/enums.py`, `contracts/types.py` | `test_confirmed_swings.py` (10) | None — the author approved this milestone in full (§33AB) | 83 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `src/btmm_ai_scanner/domain/displacement.py` | New | Python (Pydantic v2) | `DisplacementObservation` contract and single-candle Range Speed Ratio classification | Register §33J (Ambiguity 7 §2, unmodified) | NORMAL/FAST/VERY_FAST classification only; candle-level, never labeled "institutional" | Any FVG-displacement or BTMM-leg-specific logic (POI/BTMM scope) | `measurements/candle_metrics.py`, `domain/configuration.py`, `domain/enums.py` | `test_displacement.py` (6) | None — the author approved this milestone in full (§33AB) | 84 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `src/btmm_ai_scanner/domain/equal_levels.py` | New | Python (Pydantic v2) | `EqualLevelCluster` contract (liquidity exposed as computed properties, no separate `LiquidityReference` object) and the fully-specified deterministic non-overlapping cluster-formation algorithm | Register §33K (Ambiguity 5, unmodified, plus §33C's fully-specified cluster-formation procedure) | Exact tolerance formula; exact 9-step deterministic non-overlapping cluster-formation algorithm (no swing reuse, no retroactive reopening of a closed cluster) | Any `SWEPT`/`BROKEN` state (unapproved); any cross-timeframe cluster; any separate `LiquidityReference` public contract | `domain/swings.py`, `domain/configuration.py`, `domain/enums.py` | `test_equal_levels.py` (9) | None — the author approved this milestone in full (§33AB) | 85 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `src/btmm_ai_scanner/domain/support_resistance.py` | New | Python (Pydantic v2) | `SupportResistanceZone` contract, origin/touch/reaction gating — confirmed-only output, no public `DRAFT`/`STRONG`/`*_BREAK_CANDIDATE` status | Register §33N (Ambiguity 12, unmodified; origin-reaction reuses Ambiguity 9 generically) | Exact origin-eligibility, fixed-boundary, touch/pierce, reaction-gating formulas; emits only zones that reach `CONFIRMED` | The full Ambiguity-15 reclaim/invalidation lifecycle (explicitly deferred, §33B); any public `DRAFT`/`STRONG`/`*_BREAK_CANDIDATE` status | `domain/swings.py`, `measurements/legs.py`, `domain/configuration.py`, `domain/enums.py` | `test_support_resistance.py` (10) | None — the author approved this milestone in full (§33AB) | 86 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `src/btmm_ai_scanner/domain/trendlines.py` | New | Python (Pydantic v2) | `Trendline` contract, anchor/slope/touch detection — confirmed-only output, no public `DRAFT`/`STRONG`/`BREAK_CANDIDATE`/`BROKEN`/`INVALIDATED` status; slope is explicitly price-per-candle-index | Register §33O (Ambiguity 11, unmodified) | Exact anchor-eligibility, slope-equation, steepness, touch/pierce formulas; `Decimal` slope; emits only trendlines that reach a third qualifying touch | Any final trendline invalidation/retest/reclaim/false-break/sweep (§33B, `P0G-B005`); any trendline ranking score; any public lifecycle status | `domain/swings.py`, `measurements/atr.py`, `domain/configuration.py`, `domain/enums.py` | `test_trendlines.py` (10) | None — the author approved this milestone in full (§33AB) | 87 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `src/btmm_ai_scanner/domain/analyzer.py` | New | Python (Pydantic v2) | `DerivedOutputIdentityProvider` (pure semantic-key identity contract), analyzer-computed canonical `content_fingerprint`, 7 exceptions, `MarketMeasurementAnalysis` aggregate, `analyze_market_measurements` public API | Register §33H, §33P–§33Q | Exact API signature, exact aggregate field order, exact 7-exception vocabulary, exact semantic-key definitions per output type, exact canonical fingerprint serialization | Any random UUID/fingerprint generation; any `market_data` Protocol modification; any stateful call-order-dependent identity mechanism | `domain/swings.py`, `domain/displacement.py`, `domain/equal_levels.py`, `domain/support_resistance.py`, `domain/trendlines.py`, `domain/configuration.py` | `test_analyzer_api.py` (8), `test_batch_replay_equivalence.py` (6) | None — the author approved this milestone in full (§33AB) | 88 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `tests/unit/test_market_measurement_configuration.py` | New | Python test | Configuration validation and immutability | Register §33V | Exactly 6 approved test-function names (§33V) | Any real database/filesystem/network access | `domain/configuration.py` | Self | None — the author approved this milestone in full (§33AB) | 89 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `tests/unit/test_confirmed_swings.py` | New | Python test | Meaningful swing confirmation, plateau handling, supersession, no-look-ahead, strength, insufficient history | Register §33V | Exactly 10 approved test-function names (§33V) | Any real database/filesystem/network access | `domain/swings.py` | Self | None — the author approved this milestone in full (§33AB) | 90 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `tests/unit/test_displacement.py` | New | Python test | Range Speed Ratio classification, baseline exclusion, availability | Register §33V | Exactly 6 approved test-function names (§33V) | Any real database/filesystem/network access | `domain/displacement.py` | Self | None — the author approved this milestone in full (§33AB) | 91 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `tests/unit/test_equal_levels.py` | New | Python test | Equal-high/low confirmation, strength tiers, cluster merging, distinct-swing requirement, liquidity status | Register §33V | Exactly 9 approved test-function names (§33V) | Any real database/filesystem/network access | `domain/equal_levels.py` | Self | None — the author approved this milestone in full (§33AB) | 92 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `tests/unit/test_support_resistance.py` | New | Python test | Zone boundaries, origin-reaction gating, distinct touches, DRAFT/CONFIRMED/STRONG, break-candidate, non-repainting, multi-zone preservation | Register §33V | Exactly 10 approved test-function names (§33V) | Any real database/filesystem/network access | `domain/support_resistance.py` | Self | None — the author approved this milestone in full (§33AB) | 93 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `tests/unit/test_trendlines.py` | New | Python test | Anchor eligibility, spacing, slope classification, pierce integrity, touch progression, break-candidate, multi-candidate preservation | Register §33V | Exactly 10 approved test-function names (§33V) | Any real database/filesystem/network access | `domain/trendlines.py` | Self | None — the author approved this milestone in full (§33AB) | 94 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `tests/unit/test_analyzer_api.py` | New | Python test | Mixed-symbol/timeframe/unsorted/duplicate rejection, missing-metadata rejection, empty input, determinism, aggregate field order | Register §33V | Exactly 8 approved test-function names (§33V) | Any real database/filesystem/network access | `domain/analyzer.py` | Self | None — the author approved this milestone in full (§33AB) | 95 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `tests/unit/test_batch_replay_equivalence.py` | New | Python test | Batch/replay equivalence for swings/trendlines/zones, simultaneous-availability grouping | Register §33V | Exactly 6 approved test-function names (§33V) | Any real database/filesystem/network access | `domain/analyzer.py`, `market_data.InMemoryHistoricalReplaySource` | Self | None — the author approved this milestone in full (§33AB) | 96 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-H-MEASUREMENTS | `tests/unit/test_domain_exports.py` | New | Python test | Package-export verification (33 new names), no-POI/BTMM/structure-transition-field structural assertions | Register §33V | Exactly 5 approved test-function names (§33V) | Any real database/filesystem/network access | `measurements/__init__.py`, `domain/__init__.py` | Self | None — the author approved this milestone in full (§33AB) | 97 | **AUTHOR-APPROVED**, **IMPLEMENTED**, **VERIFIED**, **ARCHITECTURALLY AUDITED**, **COMMITTED**, **PUSHED**, **CLOSED**, **NOT PRODUCTION-APPROVED** |
| 1B-I-STRUCTURE | `src/btmm_ai_scanner/structure/__init__.py` | New | Python | Marks `structure` as a package; exports 12 structure-owned names, no `domain` re-exports | Register §34P/§34S | Exact 12-name `__all__` list per §34S | Any POI/BTMM export; any re-export of `domain`-owned names | `structure/enums.py`, `structure/configuration.py`, `structure/relationships.py`, `structure/transitions.py`, `structure/current_state.py`, `structure/analyzer.py` | `test_structure_exports.py` | None — the author approved this milestone in full (§34Y) | 98 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-I-STRUCTURE | `src/btmm_ai_scanner/structure/enums.py` | New | Python | `StructureDirection`, `SwingRelationshipLabel`, `StructureTransitionType` | Register §34D/§34E/§34I | Exactly the enum members listed in §34D/§34E/§34I; no unapproved state | Any BOS/CHoCH *value* not listed in §34I; any structure-state enum beyond direction (§34E) | `config/enums.py` | Exercised indirectly via `structure/` detector tests | None — the author approved this milestone in full (§34Y) | 99 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-I-STRUCTURE | `src/btmm_ai_scanner/structure/configuration.py` | New | Python (Pydantic v2) | `StructureConfiguration` — one immutable, all-default rule set | Register §34K | Exact field list per §34K; `swing_relationship_equal_tolerance_atr_multiplier` reuses the 1B-H `0.10` value verbatim, no new threshold invented | Any new numeric threshold not already approved elsewhere | `contracts/types.py`, `contracts/provenance_record.py` | `test_structure_configuration.py` (5) | None — the author approved this milestone in full (§34Y) | 100 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-I-STRUCTURE | `src/btmm_ai_scanner/structure/relationships.py` | New | Python (Pydantic v2) | `SwingRelationship` contract and HH/LH/HL/LL/EQUAL_HIGH/EQUAL_LOW classification | Register §34D | Same-type-predecessor comparison only; equal relationships emitted explicitly and treated as reference-only for bootstrap/BOS/CHoCH; no color-based inference | Any candle-color-based inference; any comparison against a non-same-type predecessor | `domain/swings.py`, `domain/enums.py`, `structure/configuration.py`, `structure/enums.py` | `test_swing_relationships.py` (8) | None — the author approved this milestone in full (§34Y) | 101 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-I-STRUCTURE | `src/btmm_ai_scanner/structure/transitions.py` | New | Python (Pydantic v2) | `StructureTransition` contract, break detection, BOS/CHoCH classification, protected/weak swing derivation | Register §34F/§34G/§34H/§34I/§34J | Close-based strict-inequality break rule only; exact BOS/CHoCH prerequisite/effect rules; protected/weak re-derived fresh every call; CHoCH-before-BOS same-candle ordering | Any wick-only break; any invalidation/retest lifecycle; any transitional/pending CHoCH state; any overshoot/break-magnitude field | `domain/swings.py`, `structure/relationships.py`, `structure/configuration.py`, `structure/enums.py` | `test_break_and_transitions.py` (12) | None — the author approved this milestone in full (§34Y) | 102 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-I-STRUCTURE | `src/btmm_ai_scanner/structure/current_state.py` | New | Python (Pydantic v2) | `CurrentStructureState` contract — one identity-bearing current snapshot per analysis call | Register §34I | Exact field list per §34I; singleton semantic key per §34K | Any lifecycle-history field (recoverable from `structure_transitions` instead) | `structure/enums.py`, `contracts/types.py` | Exercised indirectly via `test_break_and_transitions.py`, `test_structure_batch_replay_equivalence.py` | None — the author approved this milestone in full (§34Y) | 103 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-I-STRUCTURE | `src/btmm_ai_scanner/structure/analyzer.py` | New | Python (Pydantic v2) | 9-error vocabulary (6 reused unmodified + 3 new), locally-duplicated canonical-fingerprint/identity-resolver helpers, `StructureAnalysis` aggregate, `analyze_structure_state` public API | Register §34K/§34L/§34M/§34N | Exact API signature; exact 7-field aggregate order; exact semantic-key definitions per output type; exact canonical fingerprint serialization (identical algorithm to 1B-H, independently implemented) | Any random UUID/fingerprint generation; any `market_data`/`domain` Protocol modification; any import of 1B-H's private (`_`-prefixed) helpers | `structure/relationships.py`, `structure/transitions.py`, `structure/current_state.py`, `structure/configuration.py`, `domain/enums.py` | `test_structure_analyzer_api.py` (10), `test_structure_batch_replay_equivalence.py` (6) | None — the author approved this milestone in full (§34Y) | 104 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-I-STRUCTURE | `tests/unit/test_structure_configuration.py` | New | Python test | Configuration validation, immutability, no-required-argument construction | Register §34R | Exactly 5 approved test-function names (§34R) | Any real database/filesystem/network access | `structure/configuration.py` | Self | None — the author approved this milestone in full (§34Y) | 105 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-I-STRUCTURE | `tests/unit/test_swing_relationships.py` | New | Python test | HH/LH/HL/LL/equal-high/equal-low classification, first-of-type behavior, no color inference | Register §34R | Exactly 8 approved test-function names (§34R) | Any real database/filesystem/network access | `structure/relationships.py` | Self | None — the author approved this milestone in full (§34Y) | 106 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-I-STRUCTURE | `tests/unit/test_structure_bootstrap.py` | New | Python test | Bootstrap direction detection across 0–4+ swing counts, contradictory/equal evidence | Register §34R | Exactly 8 approved test-function names (§34R) | Any real database/filesystem/network access | `structure/analyzer.py`, `structure/relationships.py` | Self | None — the author approved this milestone in full (§34Y) | 107 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-I-STRUCTURE | `tests/unit/test_protected_weak_swings.py` | New | Python test | Protected/weak derivation, direction-dependence, exclusivity, roll-forward behavior | Register §34R | Exactly 6 approved test-function names (§34R) | Any real database/filesystem/network access | `structure/transitions.py` | Self | None — the author approved this milestone in full (§34Y) | 108 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-I-STRUCTURE | `tests/unit/test_break_and_transitions.py` | New | Python test | Close-based break confirmation, bullish/bearish BOS/CHoCH, repeated-break suppression, same-candle ordering | Register §34R | Exactly 12 approved test-function names (§34R) | Any real database/filesystem/network access | `structure/transitions.py` | Self | None — the author approved this milestone in full (§34Y) | 109 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-I-STRUCTURE | `tests/unit/test_structure_analyzer_api.py` | New | Python test | Mixed-input/unsorted/duplicate/missing-reference rejection, empty input, determinism | Register §34R | Exactly 10 approved test-function names (§34R) | Any real database/filesystem/network access | `structure/analyzer.py` | Self | None — the author approved this milestone in full (§34Y) | 110 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-I-STRUCTURE | `tests/unit/test_structure_batch_replay_equivalence.py` | New | Python test | Batch/replay equivalence for transitions/current-state, simultaneous-availability grouping | Register §34R | Exactly 6 approved test-function names (§34R) | Any real database/filesystem/network access | `structure/analyzer.py`, `domain.analyze_market_measurements`, `market_data.InMemoryHistoricalReplaySource` | Self | None — the author approved this milestone in full (§34Y) | 111 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-I-STRUCTURE | `tests/unit/test_structure_exports.py` | New | Python test | Package-export verification (12 structure-owned names), no-POI/BTMM-field structural assertions, no forbidden imports | Register §34R | Exactly 5 approved test-function names (§34R) | Any real database/filesystem/network access | `structure/__init__.py` | Self | None — the author approved this milestone in full (§34Y) | 112 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/__init__.py` | New | Python | Marks `poi` as a package; exports exactly 23 public names, no `domain`/`structure` re-exports | Register §35AA/§35AJ | Exact 23-name `__all__` list per §35AJ | Any BTMM/entry/trade export; any re-export of `domain`-owned or `structure`-owned names; any export of `PoiValidityStatus` (removed) | `poi/enums.py`, `poi/configuration.py`, `poi/observation.py`, `poi/lifecycle.py`, `poi/current_state.py`, `poi/order_blocks.py`, `poi/fair_value_gaps.py`, `poi/reversal_candles.py`, `poi/bases.py`, `poi/pressure_wicks.py`, `poi/engulfing.py`, `poi/single_candle_reversals.py`, `poi/three_candle_stars.py`, `poi/reference_zones.py`, `poi/period_levels.py`, `poi/overlap.py`, `poi/analyzer.py` | `test_poi_exports.py` (5) | None — the author approved this milestone in full (§35AP) | 113 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/enums.py` | New | Python | `PoiFamily`, `PoiDirection`, `PoiType` (32 members), `PoiStrengthTier`, `PoiLifecycleStatus` (11 members incl. `NOT_APPLICABLE`), `PoiLifecycleTransitionType`, `PoiFreshnessStatus`, `PoiTapClassification`, `PoiOverlapRelationshipType` | Register §35D/§35V/§35X/§35Z | Exactly the enum members listed in §35D/§35V/§35X/§35Z; no unapproved state | Any `PoiValidityStatus`/`FORMING` member (removed entirely, §35D/§35U); any `EXPIRED` lifecycle value (§35W); any Trendline/Swing-High/Swing-Low `PoiType` member (deferred, §35C) | `config/enums.py` | Exercised indirectly via `poi/` detector tests and `test_poi_exports.py` | None — the author approved this milestone in full (§35AP) | 114 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/configuration.py` | New | Python (Pydantic v2) | `PoiConfiguration` — one immutable, all-default rule set (one required field, `minimum_price_tick`) | Register §35AG | Exact field list per §35AG; every threshold value reused verbatim from already-approved standards | Any new numeric threshold not already approved elsewhere; any `strong_poi_timeframes` field (removed, audit Part 11, §35AG) | `contracts/types.py`, `contracts/provenance_record.py`, `poi/enums.py` | `test_poi_configuration.py` (7) | None — the author approved this milestone in full (§35AP) | 115 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/observation.py` | New | Python (Pydantic v2) | `PoiObservation` contract — exact 23-field order per §35E | Register §35E/§35F/§35G | Exact 23-field order; zero-height point-zone rule for the 12 period-level types (`zone_top = zone_bottom = representative_price`) | Any `validity_status` field (removed, §35D/§35U); any `source_structure_record_ids` field (removed, §35B/§35S); any mutable `list` field (tuples only) | `contracts/types.py`, `poi/enums.py`, `domain/enums.py` (`DerivedOutputType`) | Exercised indirectly via every family detector test and `test_poi_batch_replay_equivalence.py` | None — the author approved this milestone in full (§35AP) | 116 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/lifecycle.py` | New | Python (Pydantic v2) | `PoiLifecycleTransition` contract; breach/reclaim/displacement/invalidation state machine reused verbatim from `POI_BOUNDARY_BREACH_RECLAIM_INVALIDATION.md`; repeated-tap classification; `FRESH`/`INTERACTED` freshness transition | Register §35V | Exact Contact/Overshoot Tolerance formulas; exact 3-bar reclaim/displacement windows (breach candle excluded from bar 1); exact forbidden-transition guard | Any `EXPIRED`/automatic-age-expiration behavior (§35W); any lifecycle transition emitted for Equal-Level or period-level types (permanently `NOT_APPLICABLE`, §35P/§35R) | `poi/enums.py`, `poi/observation.py`, `measurements/atr.py`, `measurements/candle_metrics.py` | `test_poi_lifecycle_and_freshness.py` (14) | None — the author approved this milestone in full (§35AP) | 117 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/current_state.py` | New | Python (Pydantic v2) | `CurrentPoiState` contract — exact 21-field order per §35Z (singleton per confirmed POI instance) | Register §35Z | Exact field list per §35Z; singleton semantic key per §35F | Any `validity_status` field (removed, §35D); any lifecycle-history field (recoverable from `poi_lifecycle_transitions` instead) | `poi/enums.py`, `contracts/types.py` | Exercised indirectly via `test_poi_lifecycle_and_freshness.py`, `test_poi_batch_replay_equivalence.py` | None — the author approved this milestone in full (§35AP) | 118 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/order_blocks.py` | New | Python (Pydantic v2) | Buy/Sell Order Block candidate detection and confirmation | Register §35K | Two-candle Size Ratio >= 2.0/3.0 rule (Candle Measurement Standard V1); zone = full range of smaller (origin) candle; availability = displacement candle close (RECON-D1) | Any BOS-based "origin vs. middle of move" gate (not approved, disclosed limitation §35AN); any base-formation deduplication | `poi/configuration.py`, `poi/enums.py`, `poi/observation.py`, `measurements/candle_metrics.py`, `contracts/types.py` | `test_order_blocks.py` (8) | None — the author approved this milestone in full (§35AP) | 119 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/fair_value_gaps.py` | New | Python (Pydantic v2) | Buy/Sell Fair Value Gap candidate detection and confirmation | Register §35J | Strict 3-candle gap geometry (`low(3) > high(1)` / `high(3) < low(1)`); zone spans candle-1-to-candle-3; availability = 3rd candle close (RECON-D5); rejected if gap closes before 3rd candle | Any minimum-size threshold (RECON-D3: none); any structure-context gate (§35S) | `poi/configuration.py`, `poi/enums.py`, `poi/observation.py`, `measurements/candle_metrics.py`, `contracts/types.py` | `test_fair_value_gaps.py` (8) | None — the author approved this milestone in full (§35AP) | 120 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/reversal_candles.py` | New | Python (Pydantic v2) | Buy-to-Sell/Sell-to-Buy Candle candidate detection and 3-bar reversal confirmation | Register §35M | Exact Size Ratio/Body Efficiency/Close Position thresholds (standard/strong); zone = candidate candle full range; confirmation within exactly 3 confirmed candles; availability = reversal confirmation time | Any HH/HL/BOS/CHoCH-based trend-context gate (explicitly prohibited by source standard, §35M); any window extension beyond 3 candles | `poi/configuration.py`, `poi/enums.py`, `poi/observation.py`, `measurements/candle_metrics.py`, `measurements/atr.py`, `contracts/types.py` | `test_reversal_candles.py` (8) | None — the author approved this milestone in full (§35AP) | 121 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/bases.py` | New | Python (Pydantic v2) | Base Rally/Base Drop candidate detection and confirmation | Register §35M | 2-6 compact base candle rule; exact Base Height/Midpoint Drift/Overlap Ratio thresholds; zone = `[Base Low, Base High]`; availability = departure candle close (RECON-D2) | Any base candle count outside 2-6; any departure Size Ratio below 2.0 | `poi/configuration.py`, `poi/enums.py`, `poi/observation.py`, `measurements/candle_metrics.py`, `measurements/atr.py`, `contracts/types.py` | `test_bases.py` (8) | None — the author approved this milestone in full (§35AP) | 122 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/pressure_wicks.py` | New | Python (Pydantic v2) | Bullish/Bearish Pressure Wick candidate detection and same-candle confirmation | Register §35M | Exact Wick Share/Body Efficiency/Dominance/Close Position thresholds (standard/strong); zone = rejection wick only; availability = own candle close | Any candle-colour-based direction inference; any zone including the candle body | `poi/configuration.py`, `poi/enums.py`, `poi/observation.py`, `measurements/candle_metrics.py`, `contracts/types.py` | `test_pressure_wicks.py` (6) | None — the author approved this milestone in full (§35AP) | 123 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/engulfing.py` | New | Python (Pydantic v2) | Bullish/Bearish Engulfing candidate detection and confirmation (GROUP3-D1/D2) | Register §35L | Size Ratio >= 2.0/3.0 vs. engulfed candle; zone = engulfed (first, smaller) candle full range; availability = engulfing candle's own close | Any zone including the engulfing (second, larger) candle; any BOS-based "middle of move" gate (disclosed limitation) | `poi/configuration.py`, `poi/enums.py`, `poi/observation.py`, `measurements/candle_metrics.py`, `contracts/types.py` | `test_engulfing.py` (5) | None — the author approved this milestone in full (§35AP) | 124 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/single_candle_reversals.py` | New | Python (Pydantic v2) | Hammer/Shooting Star candidate detection and same-candle confirmation (GROUP3-D3-D6) | Register §35L | Exact Wick Share/Body Efficiency/Opposite-Wick thresholds (standard/strong); zone = rejection wick only; availability = own candle close | Any confirmation-candle delay (entry-timing observation only, not a lifecycle gate, GROUP3-D6); any zone including the candle body | `poi/configuration.py`, `poi/enums.py`, `poi/observation.py`, `measurements/candle_metrics.py`, `contracts/types.py` | `test_single_candle_reversals.py` (5) | None — the author approved this milestone in full (§35AP) | 125 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/three_candle_stars.py` | New | Python (Pydantic v2) | Morning/Evening Star candidate detection and 3rd-candle confirmation (GROUP3-D7-D9) | Register §35L | Exact middle-candle Doji Body Efficiency threshold (standard/strong); zone = middle doji candle full range only; availability = 3rd candle close | Any zone including candle 1 or candle 3; any hard mandatory gate on the 3rd-candle Size Ratio beyond the doji threshold (book's "ideally" retained as descriptive only) | `poi/configuration.py`, `poi/enums.py`, `poi/observation.py`, `measurements/candle_metrics.py`, `contracts/types.py` | `test_three_candle_stars.py` (5) | None — the author approved this milestone in full (§35AP) | 126 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/reference_zones.py` | New | Python (Pydantic v2) | Support/Resistance and Equal-Highs/Equal-Lows reference POI construction — no re-detection | Register §35N/§35P | Boundaries/confirmation/availability inherited byte-for-byte from `domain.SupportResistanceZone`/`domain.EqualLevelCluster`; fixed `PoiLifecycleStatus = NOT_APPLICABLE` for Equal-Highs/Equal-Lows | Any new boundary/confirmation/availability computation (must inherit, never re-derive); any lifecycle transition for Equal-Highs/Equal-Lows; any sweep/`SWEPT`/`BROKEN` state (`P0G-B004`, out of scope) | `domain.SupportResistanceZone`, `domain.EqualLevelCluster`, `poi/configuration.py`, `poi/enums.py`, `poi/observation.py`, `contracts/types.py` | `test_reference_zones.py` (7) | None — the author approved this milestone in full (§35AP) | 127 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/period_levels.py` | New | Python (Pydantic v2) | 12 Previous/Current Day/Week/Month High/Low point-zone level construction | Register §35R | Exact UTC half-open day/ISO-week/calendar-month windows; `event_time_utc`-based window membership; empty-window skipping; content-evolving snapshot for the 6 "Current" types; fixed `PoiLifecycleStatus = NOT_APPLICABLE` | Any broker-local timezone or DST adjustment; any lifecycle transition; any sweep/break prediction (not automated, §35R) | `poi/configuration.py`, `poi/enums.py`, `poi/observation.py`, `contracts/types.py` | `test_period_levels.py` (8) | None — the author approved this milestone in full (§35AP) | 128 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/overlap.py` | New | Python (Pydantic v2) | `PoiOverlapRelationship` contract; 3-case geometric overlap/containment test; cross-timeframe merge resolution | Register §35X/§35I | Exact non-zero/non-zero, point/interval, point/point overlap rules; same-`poi_type` cross-timeframe merge only; `_TIMEFRAME_STRENGTH_RANK` total order; no transitive inference | Any same-timeframe or cross-`poi_type` merge (report-only); any boundary recomputation/averaging/widening on merge; any bullish/bearish `PoiOverlapRelationship` (same-direction required) | `poi/enums.py`, `poi/observation.py`, `poi/configuration.py`, `contracts/types.py` | `test_poi_overlap_merge_and_precedence.py` (9) | None — the author approved this milestone in full (§35AP) | 129 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `src/btmm_ai_scanner/poi/analyzer.py` | New | Python (Pydantic v2) | 10-error vocabulary (4 reused unmodified + 6 new), locally-duplicated canonical-fingerprint/identity-resolver helpers, `PoiTimeframeInput`, `PoiAnalysis` aggregate, `analyze_pois` public API | Register §35AA/§35AB/§35F/§35G/§35H/§35AE/§35AF | Exact API signature; exact 7-field aggregate order; exact semantic-key definitions per output type; exact canonical fingerprint serialization (identical algorithm to `domain`/`structure`, independently implemented); no `structure_analysis` field on `PoiTimeframeInput` | Any random UUID/fingerprint generation; any `market_data`/`domain` Protocol modification; any import of `domain`'s or `structure`'s private (`_`-prefixed) helpers; any dependency on `structure` | `poi/order_blocks.py`, `poi/fair_value_gaps.py`, `poi/reversal_candles.py`, `poi/bases.py`, `poi/pressure_wicks.py`, `poi/engulfing.py`, `poi/single_candle_reversals.py`, `poi/three_candle_stars.py`, `poi/reference_zones.py`, `poi/period_levels.py`, `poi/overlap.py`, `poi/lifecycle.py`, `poi/current_state.py`, `poi/configuration.py`, `poi/enums.py`, `domain/enums.py` | `test_poi_analyzer_api.py` (11), `test_poi_batch_replay_equivalence.py` (6) | None — the author approved this milestone in full (§35AP) | 130 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `tests/unit/test_poi_configuration.py` | New | Python test | Configuration defaults, immutability, threshold validation, Option B proxy disclosure | Register §35AI | Exactly 7 approved test-function names (§35AI) | Any real database/filesystem/network access | `poi/configuration.py` | Self | None — the author approved this milestone in full (§35AP) | 131 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `tests/unit/test_order_blocks.py` | New | Python test | Buy/Sell Order Block candidacy, zone, availability, strong classification | Register §35AI | Exactly 8 approved test-function names (§35AI) | Any real database/filesystem/network access | `poi/order_blocks.py` | Self | None — the author approved this milestone in full (§35AP) | 132 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `tests/unit/test_fair_value_gaps.py` | New | Python test | Buy/Sell FVG strict geometry, zone, availability, rejection | Register §35AI | Exactly 8 approved test-function names (§35AI) | Any real database/filesystem/network access | `poi/fair_value_gaps.py` | Self | None — the author approved this milestone in full (§35AP) | 133 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `tests/unit/test_reversal_candles.py` | New | Python test | Buy-to-Sell/Sell-to-Buy candidacy, zone, 3-bar confirmation, availability | Register §35AI | Exactly 8 approved test-function names (§35AI) | Any real database/filesystem/network access | `poi/reversal_candles.py` | Self | None — the author approved this milestone in full (§35AP) | 134 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `tests/unit/test_bases.py` | New | Python test | Base Rally/Drop candle-count, zone, departure candle, availability | Register §35AI | Exactly 8 approved test-function names (§35AI) | Any real database/filesystem/network access | `poi/bases.py` | Self | None — the author approved this milestone in full (§35AP) | 135 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `tests/unit/test_pressure_wicks.py` | New | Python test | Bullish/Bearish Pressure Wick thresholds, zone, strong classification, confirmation | Register §35AI | Exactly 6 approved test-function names (§35AI) | Any real database/filesystem/network access | `poi/pressure_wicks.py` | Self | None — the author approved this milestone in full (§35AP) | 136 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `tests/unit/test_engulfing.py` | New | Python test | Bullish/Bearish Engulfing size ratio, zone, availability | Register §35AI | Exactly 5 approved test-function names (§35AI) | Any real database/filesystem/network access | `poi/engulfing.py` | Self | None — the author approved this milestone in full (§35AP) | 137 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `tests/unit/test_single_candle_reversals.py` | New | Python test | Hammer/Shooting Star thresholds, zone, confirmation | Register §35AI | Exactly 5 approved test-function names (§35AI) | Any real database/filesystem/network access | `poi/single_candle_reversals.py` | Self | None — the author approved this milestone in full (§35AP) | 138 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `tests/unit/test_three_candle_stars.py` | New | Python test | Morning/Evening Star doji threshold, zone, availability | Register §35AI | Exactly 5 approved test-function names (§35AI) | Any real database/filesystem/network access | `poi/three_candle_stars.py` | Self | None — the author approved this milestone in full (§35AP) | 139 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `tests/unit/test_reference_zones.py` | New | Python test | Support/Resistance/Equal-Highs/Equal-Lows inheritance, direction mapping, no-lifecycle assertion | Register §35AI | Exactly 7 approved test-function names (§35AI) | Any real database/filesystem/network access | `poi/reference_zones.py` | Self | None — the author approved this milestone in full (§35AP) | 140 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `tests/unit/test_period_levels.py` | New | Python test | Exact UTC window boundaries, previous/current-period semantics, fingerprint evolution, fixed lifecycle | Register §35AI | Exactly 8 approved test-function names (§35AI) | Any real database/filesystem/network access | `poi/period_levels.py` | Self | None — the author approved this milestone in full (§35AP) | 141 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `tests/unit/test_poi_lifecycle_and_freshness.py` | New | Python test | Breach/reclaim/displacement/invalidation state machine, repeated tap, freshness, descriptive age | Register §35AI | Exactly 14 approved test-function names (§35AI) | Any real database/filesystem/network access | `poi/lifecycle.py` | Self | None — the author approved this milestone in full (§35AP) | 142 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `tests/unit/test_poi_overlap_merge_and_precedence.py` | New | Python test | Overlap/containment/boundary-touching classification, point-zone rules, cross-timeframe merge, non-transitivity | Register §35AI | Exactly 9 approved test-function names (§35AI) | Any real database/filesystem/network access | `poi/overlap.py` | Self | None — the author approved this milestone in full (§35AP) | 143 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `tests/unit/test_poi_analyzer_api.py` | New | Python test | Mixed-input/unsorted/duplicate/missing-reference rejection, empty input, candidate/confirmation boundary, determinism | Register §35AI | Exactly 11 approved test-function names (§35AI) | Any real database/filesystem/network access | `poi/analyzer.py` | Self | None — the author approved this milestone in full (§35AP) | 144 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `tests/unit/test_poi_batch_replay_equivalence.py` | New | Python test | Batch/replay equivalence for observations/transitions/current-state/merge, 3-way fingerprint equivalence | Register §35AI | Exactly 6 approved test-function names (§35AI) | Any real database/filesystem/network access | `poi/analyzer.py`, `domain.analyze_market_measurements`, `market_data.InMemoryHistoricalReplaySource` | Self | None — the author approved this milestone in full (§35AP) | 145 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |
| 1B-J-POI | `tests/unit/test_poi_exports.py` | New | Python test | Package-export verification (23 POI-owned names), no-BTMM/entry/trade/structure-source-field assertions, no forbidden imports | Register §35AI | Exactly 5 approved test-function names (§35AI) | Any real database/filesystem/network access | `poi/__init__.py` | Self | None — the author approved this milestone in full (§35AP) | 146 | **AUTHOR-APPROVED**, **APPROVED FOR CONTROLLED IMPLEMENTATION**, **NOT YET IMPLEMENTED**, **NOT PRODUCTION-APPROVED** |

**Total inventoried changed paths: 147** (68 new files + 1 modified existing file, `.gitignore`, added by the Phase 1B-A Decision Group 3 — Metadata, Configuration Contract, Tests and Git Hygiene correction, **plus 7 new rows added by the Historical Repository and Replay Foundation milestone (Section 37 / register §32), plus 22 new rows added by the Market Measurements and Reference Structures Foundation milestone (Section 38 / register §33), plus 15 new rows added by the Structure State and Transition Foundation milestone (Section 39 / register §34), plus 34 new rows added by the POI Detection and Lifecycle Foundation milestone — see Section 40 / register §35**). `.python-version`'s *inclusion and content* are both fully resolved (`3.12.13`). **`.gitignore` had no prior row in this inventory — this is a newly added row, not a reclassification of an existing one.** **None of the `1B-J-POI` rows is created or modified by this document — this is a documentation-only inventory-lock amendment.** Every row above is either "Required in initial scaffold" (Batch 1B-A, 10 changed paths: 9 new files + 1 modified `.gitignore`), "Required in later foundation batch" (Batches 1B-B through 1B-F, 42 files — Batch 1B-B 15 files, Batch 1B-C (validation/eligibility) 12 files, Batch 1B-D 7 files, Batch 1B-E 7 files, Batch 1B-F 1 file), "**`AUTHOR-APPROVED SCOPE` pending implementation-control approval**" (Batch `1B-C-MD`, the market-data pipeline, 17 changed paths — new files only, no modified file), "**`AUTHOR-APPROVED`; `IMPLEMENTED, VERIFIED, ARCHITECTURALLY AUDITED, COMMITTED, PUSHED, CLOSED`**" (Batch `1B-G-REPLAY`, the Historical Repository and Replay Foundation, 7 new-file changed paths — plus one existing file, `market_data/__init__.py`, its 5-name export extension now implemented and committed, not counted as a new row), "**`AUTHOR-APPROVED`; `IMPLEMENTED, VERIFIED, ARCHITECTURALLY AUDITED, COMMITTED, PUSHED, CLOSED`**" (Batch `1B-H-MEASUREMENTS`, the Market Measurements and Reference Structures Foundation, 22 new-file changed paths — no existing file modified), "**`AUTHOR-APPROVED`, `APPROVED FOR CONTROLLED IMPLEMENTATION`**" (Batch `1B-I-STRUCTURE`, the Structure State and Transition Foundation, 15 new-file changed paths — plus one existing file, `domain/enums.py`, an author-approved 3-name `DerivedOutputType` extension not yet implemented and not counted as a new row), "**`AUTHOR-APPROVED`; `IMPLEMENTED, VERIFIED, ARCHITECTURALLY AUDITED, COMMITTED, PUSHED, CLOSED`**" (Batch `1B-J-POI`, the POI Detection and Lifecycle Foundation, 34 new-file changed paths (18 source + 16 test) — plus the same existing `domain/enums.py` file receiving a second, disclosed, narrow `DerivedOutputType` extension, not counted as a new row), or explicitly excluded as "Deferred beyond Phase 1B" (manifests, raw-storage I/O, provider adapters — see Section 6) or "Prohibited" (any entry/risk/execution file — none appears above). **No file listed above is marked as already created or modified except the 17 Batch 1B-B paths, the 10 Batch 1B-A paths, the 7 Batch `1B-G-REPLAY` paths (plus `market_data/__init__.py`'s export extension), and the 22 Batch `1B-H-MEASUREMENTS` paths, all already implemented and closed — every Batch `1B-C-MD` row remains implemented and closed; every Batch `1B-H-MEASUREMENTS` row is now implemented, verified, architecturally audited, committed, pushed, and closed (`NOT PRODUCTION-APPROVED`); every Batch `1B-I-STRUCTURE` row is now `AUTHOR-APPROVED`, `APPROVED FOR CONTROLLED IMPLEMENTATION`, not yet implemented; every Batch `1B-J-POI` row is now `AUTHOR-APPROVED`, `IMPLEMENTED, VERIFIED, ARCHITECTURALLY AUDITED, COMMITTED, PUSHED, CLOSED` (`NOT PRODUCTION-APPROVED`), per Section 40 / register §35AQ, commit `ebc55e0bdbbef7b7f528d874107daa2d75a5628a`.**

**Inventory growth (Phase 1B-J-POI, register §35AP):** previous total 113 rows → **147 rows**, by the addition of exactly 34 new rows (18 new `poi/` source files, creation order 113–130, and 16 new `tests/unit/test_*` files, creation order 131–146), under the new batch tag **`1B-J-POI`** (the next unused batch letter after `1B-I-STRUCTURE`). No existing row (1 through 113) is added, removed, renamed, or renumbered — **except** row 82 (`domain/enums.py`), whose "Prohibited content"/"Blocking unresolved decision" cells now note a second, narrow, author-approved exception (3 further `DerivedOutputType` routing members: `POI_OBSERVATION`, `POI_LIFECYCLE_TRANSITION`, `CURRENT_POI_STATE`) for this milestone; the row's path, batch tag (`1B-H-MEASUREMENTS`), creation order (82), and status remain completely unchanged — a wording-only annotation, not a reclassification, identical in kind to how `1B-I-STRUCTURE` annotated the same row. Every new row's Scope status is **`AUTHOR-APPROVED`, `APPROVED FOR CONTROLLED IMPLEMENTATION`, `NOT YET IMPLEMENTED`, `NOT PRODUCTION-APPROVED`** — the author explicitly approved the corrected `1B-J-POI` architecture in full (register §35AP) following a focused read-only architectural audit and one consolidated documentation-only correction pass; this inventory-lock amendment authorizes only the exact 34-row addition to the master inventory and **not implementation itself**, which remains a separate, explicit authorization. This amendment creates no source file, test file, package directory, dependency, or lockfile change — `src/btmm_ai_scanner/poi/` does not exist on disk.

**Inventory growth (Phase 1B-B Decision Group 4):** previous total 50 rows → 52 rows, by the addition of exactly 2 new rows (`tests/unit/test_validation_result.py`, `tests/unit/test_provenance_record.py`). Batch 1B-B grew from 13 to **15 inventoried files**. No other row was added, removed, or reassigned to a different batch by this change.

**Inventory growth (Phase 1B-C Decision Group 2, §29N):** previous total 52 rows → **69 rows**, by the addition of exactly 17 new rows (the 9 `market_data/` source files and 8 `test_*` files named in `PHASE_1B_AUTHOR_DECISION_REGISTER.md` §29A–§29K), under the new batch tag **`1B-C-MD`** — deliberately distinct from the existing `1B-C` batch tag (Section 14's "Validation and Eligibility Foundation," 12 files), since Phase 1B-C in the decision register (market-data pipeline, §28–§29) and Batch 1B-C in this document's Section 14 (validation/eligibility) are two different scopes that happen to share the "1B-C" phase prefix. Every new row's Scope status is **`AUTHOR-APPROVED SCOPE` (architecture, §28N) with `AUTHOR-APPROVED implementation controls` (§29O)** — the author explicitly approved Decision Group 2 in full following a final read-only architectural audit verdict of `A. PASS — READY FOR AUTHOR APPROVAL` (no blocking or non-blocking finding); this approval authorizes only the exact 17-path batch and **not implementation itself**, which remains a separate, explicit authorization. No existing row (1–52) was modified, removed, renumbered, or reassigned to a different batch by this addition. `.gitignore`, `pyproject.toml`, and `uv.lock` are unaffected — none of the 17 new rows is a modified file.

### 9a. Batch 1B-A `pyproject.toml` Metadata Plan (Decision Group 3, `AUTHOR-APPROVED`)

**`pyproject.toml` is not created by this document.** The following is the exact future `[project]` metadata content, once Batch 1B-A is separately authorized for execution:

```toml
[project]
name = "btmm-ai-scanner"
version = "0.1.0"
description = "Deterministic software foundation for the BTMM and POI AI scanner."
authors = [
    { name = "Bellforex" }
]
requires-python = ">=3.12,<3.13"
dependencies = []
license = "LicenseRef-Proprietary"
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.12",
    "Private :: Do Not Upload",
]

[build-system]
requires = ["uv_build>=0.11.30,<0.12"]
build-backend = "uv_build"

[tool.uv]
required-version = "==0.11.30"
```

**Explicitly omitted from Batch 1B-A** (a decision, not a gap): `readme` field and no `README.md` file; `license-files` field and no licence file; `maintainers`; `keywords`; `project.urls`; `project.scripts`; entry points; `dynamic` metadata; `optional-dependencies`; GUI metadata; CLI metadata.

**Prohibited classifiers and claims (binding):** no deprecated `License ::` classifier; no operating-system classifier; no public-release classifier; no production-readiness claim; no trading-profitability claim; no AI-performance claim; no financial-product claim.

**Development-dependency group (`AUTHOR-APPROVED`, Decision Group 4):**

```toml
[dependency-groups]
dev = [
    "pytest>=9.1.1,<10",
    "mypy>=2.3.0,<3",
    "ruff>=0.15.22,<0.16",
]
```

**Confirmed: `[project].dependencies` remains empty (`[]`)** — Batch 1B-A has zero runtime dependencies (Section 6 item 7).

**Ruff configuration (`AUTHOR-APPROVED`, Decision Group 4):**

```toml
[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B", "RUF"]
```

Rules: no preview rules; no ignored rules; no per-file exemptions. Format verification: `ruff format --check .`. Lint verification: `ruff check .`. No automatic fixing during Batch 1B-A verification.

**mypy configuration (`AUTHOR-APPROVED`, Decision Group 4):**

```toml
[tool.mypy]
python_version = "3.12"
strict = true
warn_unused_configs = true
show_error_codes = true
pretty = true
```

Rules: no `ignore_missing_imports`; no plugin; no per-module relaxation; no suppression merely to obtain a passing result. Verification target: `mypy src tests`.

**pytest configuration (`AUTHOR-APPROVED`, Decision Group 4):**

```toml
[tool.pytest]
minversion = "9.1"
testpaths = ["tests"]
addopts = [
    "-ra",
    "--strict-config",
    "--strict-markers",
    "--import-mode=importlib",
]
```

Rules: no coverage plugin; no network plugin; no async-test plugin; no custom markers; no `pythonpath` manipulation. Tests execute against the package installed in the uv project environment.

**Not silently resolved by this plan:** any configuration section, dependency, or tool option beyond the four `[tool.*]`/`[dependency-groups]` blocks recorded above.

### 9b. Batch 1B-A Package-Root Content Plan (Decision Group 4, `AUTHOR-APPROVED`)

**`src/btmm_ai_scanner/__init__.py` is not created by this document.** Its exact future content:

```python
"""Deterministic software foundation for the BTMM and POI AI scanner."""
```

Rules: no `__version__` constant; no configuration re-exports; no filesystem access; no environment-variable access; no logging setup; no network activity; no import-time initialization of any kind. **`pyproject.toml` remains the authoritative project-version source** — the package root does not duplicate or re-derive the version.

## 10. File-by-File Responsibilities

Responsibilities are recorded per-file in Section 9's "Purpose" and "Required content"/"Prohibited content" columns. Summarized by batch:

- **1B-A (Toolchain and Package Shell):** establishes `.gitignore` hygiene, the installable package boundary, and configuration-precedence code, with zero domain contracts. Its two tests only prove the package imports and that precedence ordering works — neither touches trading logic.
- **1B-B (Core Foundation Contracts):** establishes value types (identity, fingerprint, version) and the first five data-contract shapes (A, B, M, N, plus Q/R stubs), with zero executable validation or ingestion logic.
- **1B-C (Validation and Eligibility Foundation):** establishes the Group 6 policy enums and the 8-step eligibility gate as code *interfaces*, explicitly without any provider-specific detection mechanism (Blocking Decisions #26–#27 remain open).
- **1B-D (Audit and Operational Logging Foundation):** establishes the audit-event shape, an append-only writer *interface* (no physical storage target chosen — Blocking Decision #22 remains open), and structured logging configuration, kept strictly separate from audit.
- **1B-E (Provider-Neutral Ingestion Boundary):** establishes `MarketDataSourcePort` and its request/result shapes, plus a single deterministic `OFFLINE_FILE` stub — no provider-specific adapter, no network dependency.
- **1B-F (CI Foundation):** establishes the offline-safe verification workflow that exercises Batches A–E's lint/type/test tooling — no deployment, no credentials, no live connection.

## 11. Permitted Content

- Type/shape definitions matching an already-approved Data Contract letter or Decision Group
- Pure functions and interfaces with no I/O side effects beyond the explicitly named exception (`ingestion/offline_file_source.py` reading one fixed local file)
- Deterministic, synthetic test doubles
- Cross-references (docstrings/comments) citing the specific Phase 0G/Phase 1A/Phase 1B document and section that authorizes the content

## 12. Prohibited Content

- Any entry price, stop loss, take profit, position size, risk percentage, leverage, profit target, trading instruction, composite trading score, or profitability outcome field
- Any POI, BTMM, HH/HL/LH/LL/BOS/CHoCH, or automated market-structure detection logic
- Any live network call, broker credential, FXCM/TradingView connection, or web-scraping code
- Any AI/model inference code
- Any database, migration, or container definition
- Any private-book content or reference to `references/private/` at runtime

## 13. Dependency Direction

Unchanged from `REPOSITORY_SCAFFOLD_PLAN.md` Section 4: `A --> B` means "A depends on B." Within the batches above: `config` has no dependency; `contracts` depends on `config`; `validation` depends on `contracts` and `config`; `audit`/`observability` depend on `contracts`/`config`; `ingestion` depends on `contracts` and `config`. No batch introduces a dependency running the opposite direction, and no cycle is introduced — this is a strict subset of the already-confirmed-acyclic full dependency graph.

## 14. Implementation Batches

| Batch | Name | Scope (changed paths) | Depends on | Author decisions required | Implementation risks | Tests required | Acceptance criteria | Rollback boundary | Independently committable |
|---|---|---|---|---|---|---|---|---|---|
| 1B-A | Toolchain and Package Shell | **10 changed paths** (Section 9): 9 new files + 1 modified existing file (`.gitignore`) — **IMPLEMENTED, committed as `47cfd699bb7f4893774579f1693abbbb57b91607`, pushed to `origin/main`** | None (first batch) | **Fully resolved:** #1–#6, #8–#12 (exact patch version, package identity, layout, build backend, `.python-version`, dependency versions, full metadata field set, OS position, license position, initial version). **Still required:** none for the items this batch actually needs — remaining open items (#7 Batch 1B-B Pydantic constraint; #18–#21, #25–#30) belong to later batches, not to 1B-A itself | Package-identity risk is resolved; remaining risk is limited to exact function signatures/exception classes/tool-configuration sections, none of which was chosen by documentation alone — these are now fixed by the implementation itself | `test_import_smoke.py`, `test_config_precedence.py` | **PASSED** — Package installs via `uv`; both test files pass (34 collected, 34 passed); `.gitignore` correctly excludes build/tool artifacts; no YAML/config data file exists; runtime dependencies remain empty | Revert this batch alone via `git revert 47cfd699bb7f4893774579f1693abbbb57b91607` (no later batch exists yet) | YES (it is the first batch) — **exercised** |

**Batch 1B-A status: `IMPLEMENTED`, `VERIFIED`, `COMMITTED`, `PUSHED`, `NOT PRODUCTION-APPROVED`.** Technical implementation `ACCEPTED`; procedural deviations `DISCLOSED AND EXCEPTIONALLY ACCEPTED` (see `PHASE_1B_AUTHOR_DECISION_REGISTER.md` Section 18c for full detail). The Python minor-version alias anomaly remains `ACKNOWLEDGED`, `EXTERNAL`, `FUNCTIONALLY LIMITED`, and `NON-BLOCKING FOR THIS EXACT-PATCH PROJECT` — no repair was authorized or performed (Section 18d of the same document). Batches 1B-B through 1B-F remain entirely unimplemented; no Batch 1B-B path exists in this repository.
| 1B-B | Core Foundation Contracts | **17 changed paths** (Section 9): 15 new files + 2 modified existing files (`pyproject.toml`, `uv.lock`) — **IMPLEMENTED, committed as `1b8602a5dcc97a89be51ba5ee65ab4940751567a`, pushed to `origin/main`** | 1B-A | #18–#21, #25 (enum, serialization, fingerprint, timestamp semantics — shape only); **RESOLVED FOR BATCH 1B-B SCOPE by Decision Group 1** (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §19): Pydantic dependency range, UUIDv7 validation-only boundary, SHA-256 fingerprint validation-only boundary, canonical-JSON/JSON-Schema/manifest-file deferral | Wrong contract module boundary requires refactor before later batches build on it | 7 contract/type unit tests | **PASSED** — Package installs via `uv`; Pydantic resolves to `2.13.4` within `>=2.13.4,<2.14`; full suite passes (221 collected, 221 passed, including the original 34); exactly 132 top-level test functions across the 7 new test files; Ruff format/lint pass; mypy passes; all 17 public contract names import successfully in the exact approved `__all__` order; architectural audit verdict `PASS WITH NON-BLOCKING FINDINGS` | Revert this batch alone via `git revert 1b8602a5dcc97a89be51ba5ee65ab4940751567a` (returns to the execution-captured baseline `4074a80fe53b7784d3a51a3ac15f2fe85d244104`) | YES, once 1B-A is committed — **exercised** |

**Phase 1B-B Decision Group 1 (`AUTHOR-APPROVED`, `NOT YET IMPLEMENTED`, `NOT PRODUCTION-APPROVED`, `BATCH 1B-B NOT AUTHORIZED FOR EXECUTION`):** approved future dependency `pydantic>=2.13.4,<2.14` (Pydantic v2 required; dataclass placeholders rejected; `pydantic-settings` deferred; no second validation framework permitted). **No external UUID dependency is approved. No external SemVer dependency is approved. No canonical-JSON dependency is approved.** UUIDv7 and SHA-256 fingerprint types are validation-only in Batch 1B-B (no generation of either). No JSON Schema file is generated in Batch 1B-B. Full detail: `PHASE_1B_AUTHOR_DECISION_REGISTER.md` Section 19.

**Phase 1B-B Decision Group 2 (`AUTHOR-APPROVED`, `NOT YET IMPLEMENTED`, `NOT PRODUCTION-APPROVED`, `BATCH 1B-B NOT AUTHORIZED FOR EXECUTION`):** approved exact `ContractModel` shared base configuration, `UUIDv7`/`SHA256Fingerprint` annotated-type mechanics, and the project-owned `SemVer` type's full grammar, parsing, and precedence behavior — full detail in `PHASE_1B_AUTHOR_DECISION_REGISTER.md` Section 20. **This decision adds no inventory row.** Batch 1B-B remains **13 inventoried files**. Minimum changed-path count remains **15** (13 inventoried files + `pyproject.toml` + `uv.lock`). **Final changed-path count remains unresolved**, pending the same two candidate test files noted under Decision Group 1 (`tests/unit/test_validation_result.py`, `tests/unit/test_provenance_record.py`).

**Phase 1B-B Decision Group 3 (`AUTHOR-APPROVED`, `NOT YET IMPLEMENTED`, `NOT PRODUCTION-APPROVED`, `BATCH 1B-B NOT AUTHORIZED FOR EXECUTION`):** approved the exact 23-field `RawCandle` and 26-field `NormalizedCandle` contracts, `CandleCompleteness`/`CandleVolumeKind` enums, Decimal/OHLC/volume policy, UTC timestamp normalization with original-timezone preservation, raw-to-normalized parent-identity lineage, and their 19-function test plans each — full detail in `PHASE_1B_AUTHOR_DECISION_REGISTER.md` Section 21. **BB-7 (timestamp normalization contract) is `PARTIALLY RESOLVED`** — the RawCandle/NormalizedCandle-specific timestamp contract is resolved; the global timestamp policy (candle-close convention, DST, trading-day/week/month boundaries, provider-session handling) remains unresolved. **This decision adds no inventory row.** Batch 1B-B remains **13 inventoried files**. Minimum changed-path count remains **15**. **Final changed-path count remains unresolved.**

**Phase 1B-B Decision Group 4 (`AUTHOR-APPROVED`, `NOT YET IMPLEMENTED`, `NOT PRODUCTION-APPROVED`, `BATCH 1B-B NOT AUTHORIZED FOR EXECUTION`):** approved the exact 12-field `ValidationResult` and 10-field `ProvenanceRecord` contracts, `ValidationStatus`/`AnalyticalEligibility`/`EvidenceClassification` enums, the `ProvenanceSourceReference` nested contract, status/eligibility consistency rules, reason-code format, local provenance-lineage rules, and their 16-function and 17-function test plans respectively — full detail in `PHASE_1B_AUTHOR_DECISION_REGISTER.md` Section 22. **BB-8 and BB-14 are `RESOLVED FOR BATCH 1B-B SCOPE`. BB-7 remains `PARTIALLY RESOLVED`** (timestamp policy now additionally resolved for Contracts M and N; the global timestamp policy remains unresolved). **BB-9 is `PARTIALLY RESOLVED`** (local multi-parent provenance references and self-reference/duplicate validation resolved; global lineage-graph cycle enforcement and persistence remain unresolved). **This decision adds exactly 2 inventory rows** (`tests/unit/test_validation_result.py`, `tests/unit/test_provenance_record.py`). Batch 1B-B grows from 13 to **15 inventoried files**. **Final Batch 1B-B changed-path count is now resolved at 17** (15 inventoried files + `pyproject.toml` + `uv.lock`). No additional Batch 1B-B test file may be added without another author-approved inventory decision.

**Phase 1B-B Decision Group 5 (`AUTHOR-APPROVED`, `NOT YET IMPLEMENTED`, `NOT PRODUCTION-APPROVED`, `BATCH 1B-B NOT AUTHORIZED FOR EXECUTION`):** approved the exact 5-value `CompatibilityClass` enum, the exact 12-field `RuleVersionManifest` and 14-field `SchemaVersionManifest` contracts, initial-manifest and successor-manifest consistency rules, local `supersedes_manifest_id` supersession behavior, manifest `effective_at_utc` timestamp mechanics, the initial Batch 1B-B version policy (`0.1.0` for every rule/contract/schema/manifest-contract/manifest-schema version), the corrected 29-function `test_manifest_compatibility_classes.py` test plan (not 27), and the final exact 17-name `contracts/__init__.py` export order — full detail in `PHASE_1B_AUTHOR_DECISION_REGISTER.md` Section 23. **BB-7 is now `RESOLVED FOR BATCH 1B-B SCOPE`** (timestamp mechanics resolved for every Batch 1B-B contract; the global timestamp policy beyond Batch 1B-B remains unresolved). **BB-9 remains `PARTIALLY RESOLVED`** (local manifest supersession resolved alongside the previously resolved raw-to-normalized and multi-parent provenance references; global lineage graph, cycle detection, and persistence remain unresolved). **This decision adds no inventory row.** Inventory remains **52 rows**. Batch 1B-B remains **15 inventoried files**. Final Batch 1B-B changed-path count remains **17**.

**Phase 1B-B Decision Group 6 (`AUTHOR-APPROVED`, `NOT YET IMPLEMENTED`, `NOT PRODUCTION-APPROVED`, `BATCH 1B-B IMPLEMENTATION NOT AUTHORIZED`):** approved the exact implementation control plan — the execution baseline (commit `9249c1584389993f22a3d5753f9fc37d6e00fc9c` on `main`), the exact 17-path scope, the dependency-lock procedure, the Stage A–E construction sequence, the exact 132-top-level-test-function boundary with mandatory AST-based verification, the final Ruff/mypy/pytest quality gates, mandatory stop conditions, the correction boundary, the rollback boundary, and the post-implementation closure sequence culminating in the separate authorization phrase `AUTHORIZE PHASE 1B-B IMPLEMENTATION` — full detail in `PHASE_1B_AUTHOR_DECISION_REGISTER.md` Section 24 and this document's Section 16b. **This decision adds no inventory row and authorizes no implementation, dependency installation, staging, committing, or pushing.** Inventory remains **52 rows**. Batch 1B-B remains **15 inventoried files**. Final Batch 1B-B changed-path count remains **17**. Batch 1B-B implementation remains unauthorized.

**Phase 1B-B Baseline Correction 6A (`AUTHOR-APPROVED`, `DOCUMENTATION CORRECTION ONLY`, `NOT YET IMPLEMENTED`, `NOT PRODUCTION-APPROVED`):** corrects Decision Group 6's implementation starting commit and rollback point from `9249c1584389993f22a3d5753f9fc37d6e00fc9c` to `70fde0b8e49c2ef48397ea29090f6a36af61899b`, since committing the Decision Group 6 documentation itself advanced HEAD past the value that document had named as its own clean-tree baseline. `9249c15...` is retained only as a **historical pre-Decision-Group-6 checkpoint**; it is no longer the starting commit, the rollback target, or the required clean-tree target. Every other Decision Group 6 control (Section 16b) is unchanged. The author explicitly confirmed the separate authorization phrase `AUTHORIZE PHASE 1B-B IMPLEMENTATION` during the drafting of this correction — full detail in `PHASE_1B_AUTHOR_DECISION_REGISTER.md` Section 25. **Implementation remains blocked until this correction is itself committed and pushed.** Inventory remains **52 rows**. Batch 1B-B remains **15 inventoried files**. Final Batch 1B-B changed-path count remains **17**.

**Phase 1B-B Baseline Correction 6B (`AUTHOR-APPROVED`, `DOCUMENTATION CORRECTION ONLY`, `NOT YET IMPLEMENTED`, `NOT PRODUCTION-APPROVED`):** replaces the recurring fixed-hash baseline mechanism (which went stale twice — once after Decision Group 6 was committed, again after Correction 6A was committed) with a durable **execution-captured baseline policy**: the active implementation baseline and rollback target are the clean synchronized HEAD captured immediately before the first Batch 1B-B implementation change, not a hard-coded commit. `9249c1584389993f22a3d5753f9fc37d6e00fc9c` (pre-Decision-Group-6), `70fde0b8e49c2ef48397ea29090f6a36af61899b` (pre-Correction-6A), and `cc43df0dbdc6148567cb33c71a87bf0441f0f351` (candidate baseline at the time this correction was authored) are all retained only as historical audit checkpoints — none is a permanently fixed future baseline. The captured full hash must appear in the implementation completion report; no additional documentation commit is required to record it. Correction 6A's authorization chronology (§25C) is unchanged and not revoked — full detail in `PHASE_1B_AUTHOR_DECISION_REGISTER.md` Section 26. **This decision adds no inventory row.** Inventory remains **52 rows**. Batch 1B-B remains **15 inventoried files**. Final Batch 1B-B changed-path count remains **17**. **Implementation remains blocked until this correction is itself committed and pushed.**
| 1B-C | Validation and Eligibility Foundation | 12 files | 1B-B | #18, #26–#27 (enum finality, detection mechanism explicitly still deferred) | Premature provider-specific logic could leak in if not reviewed carefully | 7 unit tests | Eligibility gate matches the approved 8-step sequence; zero synthetic-fill code exists | Revert to end of 1B-B | YES, once 1B-B is committed |
| 1B-D | Audit and Operational Logging Foundation | **7 files** (corrected from a prior mislabeling of 6 — Section 9 lists `audit/__init__.py`, `audit/events.py`, `audit/writer.py`, `observability/__init__.py`, `observability/logging_config.py`, `test_audit_log_separation.py`, `test_secret_redaction.py`) | 1B-B (contracts/types) | None new (Group 4/5 already approved) | Conflating audit and operational logs would violate an approved separation | 2 unit tests | Audit and log stores remain structurally separate; secret-redaction test passes | Revert to end of 1B-C | YES, once 1B-B is committed (does not require 1B-C) |
| 1B-E | Provider-Neutral Ingestion Boundary | **7 changed paths** (Section 9): 7 new files, no modified file — **IMPLEMENTED, committed as `0a9814eddd1cdeda59cf95dbde8a806f30800b44`, pushed to `origin/main`** | 1B-B (`contracts/`) **and** completed `1B-C-MD` (`market_data/source_input.py`'s `SourceCandleInput`) | None — Phase 1B-E Decision Group 1 (register §31) was `AUTHOR-APPROVED` in full (§31N) and is now implemented exactly as approved | None remaining — the single vacuous test assertion identified by the Stage A audit was corrected before Stage B began (§31O) | 16 unit tests across 2 files (§31H) | **PASSED** — full suite passes (297 collected, 297 passed, including the original 34); exactly 16 new top-level test functions across the 2 new test files (205 combined with the existing 189); Ruff format/lint pass; mypy passes; all 5 public exports import successfully in the exact approved order; final combined audit verdict `A. PASS — READY TO COMMIT` | Revert this batch alone via `git revert 0a9814eddd1cdeda59cf95dbde8a806f30800b44` (returns to the execution-captured baseline `ff03c1cf1b24b66261b1c4b9b389c64df1dc3f96`) | YES, once 1B-B and `1B-C-MD` are committed — **exercised** |
| 1B-F | CI Foundation | 1 file | 1B-A (pyproject/uv.lock must exist for CI to run against) | #28 (trigger policy); #29–#30 (repository settings, not files) | An overly permissive workflow could accidentally gain network/credential access | N/A (workflow itself is the verification layer) | Workflow runs Ruff/mypy/pytest against Batches A–E's code, offline-safe, on the approved trigger | Revert to end of 1B-E (or earliest batch present) | YES, once 1B-A is committed |
| 1B-C-MD | Market-Data Pipeline (First Batch) — Phase 1B-C Decision Group 2 (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §29) | **17 changed paths** (Section 9): 17 new files, no modified file — **IMPLEMENTED, committed as `d328776abb5a2c1f42e185b9bc80f0e5a371897e`, pushed to `origin/main`** | 1B-B (`contracts/`, `config/enums.py`) only — does **not** depend on 1B-C (validation/eligibility), 1B-D, 1B-E, or 1B-F | None — Phase 1B-C Decision Group 2 (§29) was `AUTHOR-APPROVED` in full (§29O) and is now implemented exactly as approved; AT-1/RM-1/GAP-1 already `AUTHOR-APPROVED` (§28M) | None remaining — the one read-only-audit-identified layering/registry-mutability/test-coverage risk was corrected before staging (§30D) | 57 unit tests across 8 files (§29K) | **PASSED** — full suite passes (281 collected, 281 passed, including the original 34); corrected targeted suite 15 passed; exactly 57 new top-level test functions across the 8 new test files (189 combined with the existing 132); Ruff format/lint pass; mypy passes; all 20 public exports import successfully in the exact approved order; final architectural audit verdict `A. PASS — READY FOR STAGING REVIEW`; staged-diff review verdict `A. PASS — READY FOR COMMIT REVIEW` | Revert this batch alone via `git revert d328776abb5a2c1f42e185b9bc80f0e5a371897e` (returns to the execution-captured baseline `1a439f3a1b4b4f6189ec4c209362f5d592910160`) | YES, once 1B-B is committed — **exercised** |
| 1B-G-REPLAY | Historical Repository and Replay Foundation — register §32 | **7 new changed paths** (Section 9), plus one existing file (`market_data/__init__.py`)'s 5-name export extension — **IMPLEMENTED, committed as `5a1d8f30ee0eb67d27417fda9fb7407d9a5e8a85`, pushed to `origin/main`** | 1B-B (`contracts/`, `config/enums.py`) and completed `1B-C-MD` (`market_data.RawCandle`/`NormalizedCandle`/`ports`) only — does not depend on 1B-C (validation/eligibility), 1B-D, 1B-E, or 1B-F | None — the author approved this milestone in full (§32O) and it is now implemented exactly as approved; the repository-membership three-axis correction (§32C), the `record_id`-keyed 5-case duplicate/revision policy and closed 2-exception vocabulary (§32D/§32E/§32I), and the atomic availability-group replay ordering (§32F/§32G/§32H) are all implemented exactly as documented | None identified — existing `RawCandleSink`/`NormalizedCandleSink`/`CandleReadRepository`/`HistoricalReplaySource` Protocols are structurally sufficient without modification (§32A); no Protocol was modified | 31 unit tests across 4 files (§32K) | **PASSED** — full suite passes (328 collected, 328 passed, including the original 34); exactly 31 new top-level test functions across the 4 new test files (236 combined with the existing 205, confirmed by AST parse); Ruff format/lint pass; mypy passes; all 25 public exports import successfully in the exact approved order; final architectural audit verdict `B. PASS WITH NON-BLOCKING FINDINGS — READY TO COMMIT` (§32P) | Revert this batch alone via `git revert 5a1d8f30ee0eb67d27417fda9fb7407d9a5e8a85` (returns to the execution-captured baseline `0d133fa070f66ceff60016d18cbc531bfff3f0af`) | YES, once 1B-B and `1B-C-MD` are committed — **exercised** |
| 1B-H-MEASUREMENTS | Market Measurements and Reference Structures Foundation — register §33 | **22 new changed paths** (Section 9): 13 new source files, 9 new test files, no modified file — **`AUTHOR-APPROVED`, `IMPLEMENTED`, `VERIFIED`, `ARCHITECTURALLY AUDITED`, `COMMITTED`, `PUSHED`, `CLOSED`, `NOT PRODUCTION-APPROVED`** | 1B-B (`contracts/`, `config/enums.py`) and completed `1B-C-MD` (`contracts.NormalizedCandle`) only — does not depend on `market_data`'s pipeline/repository/replay modules, 1B-C (validation/eligibility), 1B-D, 1B-E, or `1B-G-REPLAY` (though it may be composed with `1B-G-REPLAY`'s replay source at the caller's discretion, §33D) | None — the author approved this milestone in full (§33AB); the pure semantic-key identity provider, canonical content-fingerprint, strict pre-sorted/strictly-increasing-event-time input, fully-specified Wilder ATR(14), fully-specified equal-level clustering, confirmed-only Support/Resistance and Trendline output, removal of `STRONG_SWING` and the separate `LiquidityReference` contract, and the deferral of BOS/CHoCH/HH/HL/LH/LL (`P0G-B003`) are all approved exactly as documented | None identified — no existing `market_data`/`contracts`/`config` Protocol or file requires modification; `measurements/`/`domain/` are brand-new packages (§33S); a documented §33T dependency-direction correction to `REPOSITORY_SCAFFOLD_PLAN.md`'s Phase-1A `measurements/`/`domain/` bullets is included | 70 unit tests across 9 files (§33V) | **PASSED** — full suite 398 passed; exact 22 changed paths; exact 33 new exports; exact 70 new top-level test functions (320 combined with the existing 250, AST-verified; 398 full pytest-collected total); `uv lock --check`/`ruff format --check .`/`ruff check .`/`mypy src tests` all pass | Revert this batch alone via a single `git revert` of its implementation commit `a612d4d0cb3ef58509135edc71f459742658b5f9` (returns to the execution-captured baseline captured at that turn's start) | YES — implementation commit `a612d4d0cb3ef58509135edc71f459742658b5f9`, pushed to `origin/main` |
| 1B-I-STRUCTURE | Structure State and Transition Foundation — register §34 | **15 new changed paths** (Section 9): 7 new source files, 8 new test files, plus one existing file (`domain/enums.py`) for a 3-name `DerivedOutputType` extension (not counted as a new row) — **`AUTHOR-APPROVED`, `IMPLEMENTED`, `VERIFIED`, `ARCHITECTURALLY AUDITED`, `COMMITTED`, `PUSHED`, `CLOSED`, `NOT PRODUCTION-APPROVED`** | `domain` (`ConfirmedSwing`, `SwingType`, `DerivedOutputType`, `DerivedOutputIdentityProvider`), `measurements`, `contracts`, `config` only — does not depend on `market_data`'s pipeline/repository/replay modules, 1B-C (validation/eligibility), 1B-D, 1B-E, or `1B-G-REPLAY` (though it may be composed with `1B-G-REPLAY`'s replay source and `1B-H-MEASUREMENTS`'s `analyze_market_measurements()` at the caller's discretion, §34O) | None — the author approved this milestone in full (§34Y); all 25 items listed in register §34W are approved exactly as documented and are now implemented exactly as approved (§34Z): input contract, source-chronology/availability separation, relationship enum/tolerance, generalized bootstrap rule, direction/state model, transition-sensitive protected/weak derivation, break-confirmation policy with separate price fields, BOS/CHoCH rules, one-transition-per-candle ordering, identity/fingerprint reuse-and-duplication decision with required equivalence test, unambiguous evidence classification, public API, aggregate fields, error vocabulary, replay equivalence, package placement, file scope, test names, 12-name export surface, complexity policy, exclusions | None identified — the one authorized correction cycle fixed Ruff/mypy findings only, no architecture or contract change (§34Z) | 60 unit tests across 8 files (§34R) | **PASSED** — full suite 458 passed; exact 16 changed paths; exact 12 new exports; exact 60 new top-level test functions (380 combined with the existing 320); `uv lock --check`/`ruff format --check .`/`ruff check .`/`mypy src tests` all pass; final architectural audit verdict `A. PASS — READY TO COMMIT` | Revert this batch alone via a single `git revert` of its implementation commit `74fd50c6a44c208153769d2f1947bdfa7ff0d3cf` (returns to the execution-captured baseline captured at that turn's start) | YES — implementation commit `74fd50c6a44c208153769d2f1947bdfa7ff0d3cf`, pushed to `origin/main` |

**Total across all batches: 113 changed paths** (10 + 15 + 12 + 7 + 7 + 1 + 17 + 7 + 22 + 15 = 113), matching Section 9's corrected inventory total. **Batch `1B-C-MD` (17 paths) is `AUTHOR-APPROVED SCOPE` at the architecture level (§28N), `AUTHOR-APPROVED` at the implementation-control level (§29O), and now `IMPLEMENTED, VERIFIED, COMMITTED, PUSHED, CLOSED, NOT PRODUCTION-APPROVED` (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §30, commit `d328776abb5a2c1f42e185b9bc80f0e5a371897e`). Batch `1B-G-REPLAY` (7 new paths plus the `market_data/__init__.py` export extension) is `AUTHOR-APPROVED` (§32O) and now `IMPLEMENTED, VERIFIED, ARCHITECTURALLY AUDITED, COMMITTED, PUSHED, CLOSED, NOT PRODUCTION-APPROVED` (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §32P, commit `5a1d8f30ee0eb67d27417fda9fb7407d9a5e8a85`). Batch `1B-H-MEASUREMENTS` (22 new paths) is `AUTHOR-APPROVED` (§33AB) and now `IMPLEMENTED, VERIFIED, ARCHITECTURALLY AUDITED, COMMITTED, PUSHED, CLOSED, NOT PRODUCTION-APPROVED` (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §33AC, commit `a612d4d0cb3ef58509135edc71f459742658b5f9`). Batch `1B-I-STRUCTURE` (15 new paths) is `AUTHOR-APPROVED` (§34Y) and now `IMPLEMENTED, VERIFIED, ARCHITECTURALLY AUDITED, COMMITTED, PUSHED, CLOSED, NOT PRODUCTION-APPROVED` (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §34Z, commit `74fd50c6a44c208153769d2f1947bdfa7ff0d3cf`). Batch `1B-J-POI` (34 new paths plus the `domain/enums.py` second `DerivedOutputType` annotation) is `AUTHOR-APPROVED` (§35AP) and now `IMPLEMENTED, VERIFIED, ARCHITECTURALLY AUDITED, COMMITTED, PUSHED, CLOSED, NOT PRODUCTION-APPROVED` (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §35AQ, commit `ebc55e0bdbbef7b7f528d874107daa2d75a5628a`).**

**Batch 1B-A Runtime and Dependency Baseline (`AUTHOR-APPROVED`):**

| Aspect | Approved value |
|---|---|
| Interpreter | Python `3.12.13` |
| Supported Python range (`requires-python`) | `>=3.12,<3.13` |
| Runtime dependencies | **NONE** |
| Development dependencies | `pytest>=9.1.1,<10`; `mypy>=2.3.0,<3`; `ruff>=0.15.22,<0.16` |
| Build dependency | `uv_build>=0.11.30,<0.12` |
| Tool requirement | `uv==0.11.30` (via `[tool.uv].required-version`) |
| Configuration-module scope | `config/enums.py` and `config/loader.py` are standard-library-only — no YAML parsing, no Pydantic, no `pydantic-settings` |

**Batch 1B-A Metadata, Configuration Contract, Tests and Git Hygiene (Decision Group 3, `AUTHOR-APPROVED`):**

| Aspect | Approved value |
|---|---|
| Project metadata | Full `[project]` field set — see Section 9a |
| Classifiers | Six approved values (Section 6 item 9) |
| Minimum-OS position | Windows 11 x64 validated initial environment; cross-platform compatibility claim `NOT YET ESTABLISHED` |
| License-field position | `license = "LicenseRef-Proprietary"`; no licence file |
| Configuration enums | `InternalSymbol` (3 values), `Timeframe` (8 values) — see Section 17 |
| Configuration loader | `BTMM_CONFIG_` prefix, 3-level precedence, shallow merge, secret-key rejection — see Section 17 |
| Test boundaries | See Section 23 |
| `.gitignore` | Joins Batch 1B-A as a modified existing file — see Section 9 |

**Batch 1B-A remains `NOT YET AUTHOR-APPROVED FOR EXECUTION`.** Package identity, interpreter, build backend, dependency versions, the full metadata field set, the OS position, the license position, and the configuration/test/git-hygiene boundaries are all now resolved; exact function signatures, exact exception classes, exact `__init__.py` contents, exact Ruff/mypy/pytest configuration sections, the installation sequence, and the rollback sequence remain unresolved and are not silently decided here. **Recommendation origin: `ENGINEERING-RECOMMENDED`. Current author-decision status: `AUTHOR-APPROVED` (for the baselines recorded above). Implementation status: `NOT YET IMPLEMENTED`. Production status: `NOT PRODUCTION-APPROVED`.**

**No batch is implemented by this document.** Each requires its own separate, explicit author instruction to create.

## 15. File-Creation Order

Matches Section 9's "Creation order" column exactly: **0** (`.gitignore`, modified first, precedes every other Batch 1B-A path) → **1–9** (Batch 1B-A's nine new files) → **10–24** (Batch 1B-B, now 15 inventoried files following Decision Group 4's addition of `tests/unit/test_validation_result.py` and `tests/unit/test_provenance_record.py` at creation order 23–24) → **25–36** (Batch 1B-C) → **37–43** (Batch 1B-D, corrected to 7 files/paths) → **44–50** (Batch 1B-E) → **51** (Batch 1B-F). Within 1B-D and 1B-E, either batch may follow 1B-B directly (both depend only on 1B-B, not on each other or on 1B-C) — the numbering above reflects one valid linear order, not an additional dependency. **`.gitignore`'s order-0 position is a deliberate numbering choice** (preceding order 1 rather than renumbering all subsequent entries) — it does not imply any dependency relationship beyond "modified before the rest of Batch 1B-A." **Batch 1B-C through 1B-F creation-order values were shifted by +2 (from 23–49 to 25–51) by this task, to accommodate the two newly inventoried Decision Group 4 test rows — no batch's relative order or dependency changed, only the absolute numbering.**

## 16. Dependency-Installation Order

**No dependency is installed by this document.** The proposed future order, once approved: (1) install Python at the approved patch version; (2) install `uv`; (3) `uv` resolves and locks runtime dependencies (Pydantic v2) into `uv.lock`; (4) `uv` resolves and locks development dependencies (pytest, mypy, Ruff) into the same lockfile; (5) `uv sync` (or equivalent) materializes the environment. No step above is executed by this task.

### 16a. Batch 1B-A Approved Execution Sequence — Stages A–J (Decision Group 4, `AUTHOR-APPROVED`)

**No stage below is executed by this document.** This is the exact future execution sequence, once Batch 1B-A is separately, explicitly authorized. Each stage is a distinct checkpoint; an unexpected result at any stage stops the sequence (Section 27).

**STAGE A — Repository preflight:** (1) verify expected HEAD; (2) verify clean working tree; (3) verify `origin/main` synchronization; (4) verify no Batch 1B-A artifact exists; (5) verify private references remain ignored.

**STAGE B — Install exact uv version.** Future authorized command: `powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/0.11.30/install.ps1 | iex"`. Verification: `uv --version`. Required result: `uv 0.11.30`. **Prohibited:** `uv self update`; silent substitution of another uv version.

**STAGE C — Verify Python availability.** Inspect whether uv 0.11.30 can provide exact Python 3.12.13. **Stop when unavailable.** Do not: substitute 3.12.10; substitute another patch; upgrade uv silently; edit repository files before resolution.

**STAGE D — Install managed Python.** Future authorized command: `uv python install 3.12.13 --no-bin`. Rules: verify the managed interpreter exists; do not remove Python 3.14.6; do not replace general Python aliases; do not use `--default`; do not use `--force`; do not use `--upgrade`; do not alter global Python associations.

**STAGE E — Protect generated artifacts.** Modify `.gitignore` before any project-environment command. Preserve `references/private/*`. Add only the approved exclusions (Section 9's `.gitignore` row).

**STAGE F — Create handwritten files.** Do not run `uv init`. Create manually: `pyproject.toml`; `.python-version`; `src/btmm_ai_scanner/__init__.py`; `src/btmm_ai_scanner/config/__init__.py`; `src/btmm_ai_scanner/config/enums.py`; `src/btmm_ai_scanner/config/loader.py`; `tests/test_import_smoke.py`; `tests/test_config_precedence.py`. **`uv.lock` is generated, not handwritten** (Stage G).

**STAGE G — Generate lockfile.** Future commands: `uv lock`; `uv lock --check`.

**STAGE H — Create project environment.** Future command: `uv sync --locked`. Record: `.venv/` must remain ignored; project package and development dependencies are installed from the locked metadata.

**STAGE I — Verification.** Future commands: `uv lock --check`; `uv run --locked python --version`; `uv run --locked python -c "import btmm_ai_scanner; import btmm_ai_scanner.config"`; `uv run --locked ruff format --check .`; `uv run --locked ruff check .`; `uv run --locked mypy src tests`; `uv run --locked pytest`. Required Python result: `Python 3.12.13`. **No Ruff auto-fix is permitted.**

**STAGE J — Git-scope verification.** Confirm exactly ten changed paths: one modified tracked file (`.gitignore`); nine new files, including generated `uv.lock`; `.venv/` absent from Git status; no extra generated file; no private reference; no cache, build, or coverage artifact.

**No command in Stages A–J is executed by this documentation task.**

### 16b. Batch 1B-B Approved Execution Sequence — Stages A–E (Decision Group 6, `AUTHOR-APPROVED`)

**No stage below is executed by this document. `BATCH 1B-B IMPLEMENTATION NOT AUTHORIZED`.** This is the exact future execution sequence, once Batch 1B-B is separately, explicitly authorized via the phrase `AUTHORIZE PHASE 1B-B IMPLEMENTATION`. Full detail: `PHASE_1B_AUTHOR_DECISION_REGISTER.md` Section 24.

**Execution baseline (§24A, superseded by the execution-captured baseline policy of Baseline Correction 6B, §26):** no future implementation baseline is permanently fixed to a hard-coded commit hash. **ACTIVE IMPLEMENTATION BASELINE:** the clean synchronized HEAD observed immediately before the first Batch 1B-B implementation change, after all approved implementation-control documentation has been committed and pushed. Required capture commands: `git status --short --untracked-files=all`; `git diff --stat`; `git diff --cached --stat`; `git rev-parse HEAD`; `git rev-parse origin/main`. Implementation may begin only when: working tree clean; nothing staged; HEAD equals `origin/main`; current HEAD contains all approved implementation-control documentation; no Batch 1B-B implementation path exists; runtime dependencies empty; Python `3.12.13`; `uv` `0.11.30`; existing baseline exactly 34 passing tests; inventory 52 rows; Batch 1B-B 15 inventoried files; authorized scope 17 changed paths. The captured full hash must be included in the implementation completion report; no additional documentation commit is required merely to record it. If HEAD changes before implementation starts, the newly observed clean synchronized HEAD is captured instead. A dirty, staged, or diverged repository remains a mandatory stop condition. (`9249c1584389993f22a3d5753f9fc37d6e00fc9c` = historical pre-Decision-Group-6 checkpoint only; `70fde0b8e49c2ef48397ea29090f6a36af61899b` = historical pre-Correction-6A checkpoint only; `cc43df0dbdc6148567cb33c71a87bf0441f0f351` = current clean synchronized candidate baseline at the time Correction 6B was authored, not a permanently fixed baseline.)

**Exact 17-path scope (§24B):** 2 modified (`pyproject.toml`, `uv.lock`) + 15 new (the 8 `contracts/` files from Decision Groups 1–5 and the 7 `tests/unit/` files from Decision Groups 2–5). No eighteenth path is authorized; no documentation file may change during implementation.

**Dependency-lock procedure (§24C):** add `pydantic>=2.13.4,<2.14` via `uv add "pydantic>=2.13.4,<2.14"`; regenerate and verify via `uv lock --check`; sync via `uv sync --locked`; verify `pytest 9.1.1`/`mypy 2.3.0`/`Ruff 0.15.22` remain unchanged. No `uv self update`; no Python install/replacement; no `--upgrade`; no second runtime dependency.

**STAGE A (§24D)** — `contracts/__init__.py`, `contracts/types.py`, `test_identity_and_fingerprint.py`, `test_semver.py`: create the package boundary; implement `ContractModel`, `UUIDv7`, `SHA256Fingerprint`, `SemVer`; implement exactly 17 + 15 test functions; run Stage A tests; run targeted Ruff/mypy. Final export list not finalized until Stage E.

**STAGE B (§24E)** — `raw_candle.py`, `normalized_candle.py`, `test_raw_candle_contract.py`, `test_normalized_candle_contract.py`: implement `CandleCompleteness`, `CandleVolumeKind`, the exact 23-field `RawCandle`, the exact 26-field `NormalizedCandle`, and Decimal/OHLC/volume/timestamp invariants; implement exactly 19 + 19 test functions; run candle tests; re-run Stage A tests. Existing `InternalSymbol`/`Timeframe` enums must be reused and verified before coding — a mismatch stops the sequence; no replacement enum may be created.

**STAGE C (§24F)** — `provenance_record.py`, `validation_result.py`, `test_provenance_record.py`, `test_validation_result.py`: construction order `ProvenanceSourceReference` → `EvidenceClassification` → `ProvenanceRecord` → `ValidationStatus` → `AnalyticalEligibility` → `ValidationResult`; implement exactly 17 + 16 test functions; run both files; re-run all earlier Batch 1B-B tests.

**STAGE D (§24G)** — `rule_version_manifest.py`, `schema_version_manifest.py`, `test_manifest_compatibility_classes.py`: construction order `CompatibilityClass` → `RuleVersionManifest` → `SchemaVersionManifest`; implement exactly 29 test functions covering initial/successor consistency, higher SemVer precedence, build-metadata-only rejection, local supersession, and `effective_at_utc` normalization.

**STAGE E (§24H)** — finalize `contracts/__init__.py` with the exact approved 17-name export order (§23M); verify every export imports successfully, `__all__` contains exactly 17 entries in the approved order, and no private/generator/calculator/persistence/loader helper is exported.

**Exact test-function boundary (§24I):** exactly **132** top-level test functions (17 + 15 + 19 + 19 + 16 + 17 + 29). Parameterization may yield more collected cases; the 132 limit applies to top-level function names only. **A static AST-based function-name and count comparison is mandatory.**

**Final quality gates (§24J):** targeted `ruff format`/`ruff check` on the 15 new files during construction; repository-wide `uv lock --check`, `ruff format --check .`, `ruff check .`, `mypy src tests`, `pytest -q`; import-verification of all 17 approved names with exact `__all__` count/order. All gates must pass before architectural review.

**Mandatory stop conditions (§24K):** baseline mismatch; dirty tree; branch divergence; wrong Python/uv version; baseline-test failure or count mismatch; dependency out of range; unexpected dev-tool version change; enum mismatch; unimplementable field name/order/count; strict-validation conflict; an 18th path; documentation/private-reference change; generated schema/persisted manifest; unapproved helper; any stage-test/Ruff/mypy/suite failure; test-name/count or export mismatch. On stop: do not stage/commit/push; do not weaken rules or expand scope; preserve the diff; report the exact failure.

**Correction boundary (§24L):** syntax/import/type-annotation/validator/test/formatting/lint corrections permitted within the 17 paths; renaming/reordering/retyping fields, adding defaults/coercion/exports/tests, relaxing validation, or changing version/evidence policy all require a new author decision.

**Rollback boundary (§24M, superseded by Baseline Correction 6B, §26):** **EXECUTION ROLLBACK TARGET:** the same full HEAD hash captured as the execution baseline immediately before the first Batch 1B-B implementation change (§26B) — not a permanently fixed commit. No automatic rollback approved; `git reset --hard`, `git clean`, blanket deletion, force checkout, and history rewriting are all prohibited; an explicitly authorized future rollback may affect only `pyproject.toml`, `uv.lock`, and the 15 new files. The captured execution baseline must not change during the implementation run.

**Successful completion state (§24N, superseded by Baseline Correction 6B, §26):** HEAD remains at the execution-captured baseline (unchanged since capture); exactly 17 changed paths (2 modified, 15 new); nothing staged, committed, or pushed; the captured full hash is included in the implementation completion report; the report is submitted for architectural review before any commit instruction is issued.

**Post-implementation closure sequence (§24O):** author authorization → implementation of the 17-path scope → stop with 17 unstaged paths → architectural review → commit and push the implementation → separate documentation-only closure update → closure review → closure commit and push → Batch 1B-B marked closed but not production-approved.

**Implementation-authorization phrase (§24P):** `AUTHORIZE PHASE 1B-B IMPLEMENTATION` — a phrase separate from and later than Decision Group 6 approval. **No command in Stages A–E is executed by this documentation task. Decision Group 6 adds no inventory row; inventory remains 52 rows; Batch 1B-B remains 15 inventoried files; final changed-path count remains 17; Batch 1B-B implementation remains unauthorized.**

## 17. Configuration and Secrets Scope

**No configuration or secrets file is created by this document.**

- **Project defaults:** proposed to live in a YAML file whose name and directory placement remain `REQUIRES AUTHOR DECISION` (Blocking Decisions #13–#14) — explicitly **not** placed under the protected top-level `config/` path by default, pending that decision.
- **Environment-specific non-secret configuration:** proposed as a second, environment-named YAML file, layered on top of defaults, per the approved three-level precedence.
- **Runtime environment variables:** the third, highest-precedence layer, per Group 3 — never a file.
- **Development-only local `.env`:** git-ignored, used only for local developer convenience — not created by this document; already covered by the existing `.gitignore` entry.
- **Prohibited values in any YAML file:** passwords, tokens, API keys, broker credentials, or any other secret.
- **Precedence testing:** `tests/test_config_precedence.py` (Batch 1B-A) exercises the precedence order using synthetic test doubles, not real files or real secrets.
- **Resolved-configuration lineage:** proposed to be recorded via a `configuration_version` reference (already named among the ingestion result version references in Decision Group 7), not yet implemented.
- **Secrets exclusion:** enforced structurally by never importing or reading `.env` contents into any YAML file or any manifest, and by `test_secret_redaction.py` (Batch 1B-D) asserting no secret ever reaches a log or audit event.

No production, staging, or live-environment configuration file is proposed — there is no present need at this scaffold stage.

**Batch 1B-A Dependency Baseline note (`AUTHOR-APPROVED`, referenced from Section 6 row 7):** because Batch 1B-A creates no actual YAML file (the filename/placement decisions, #13–#14, remain deferred), `config/enums.py` and `config/loader.py` need nothing beyond the Python standard library to fulfil their Batch 1B-A scope — foundational enums (`enum.Enum`/`StrEnum`), plain dict/mapping merges, `os.environ` reads, deterministic precedence resolution, and secret-value exclusion. **No YAML parser, Pydantic model, or `pydantic-settings` is required, installed, or used in Batch 1B-A.** Pydantic remains approved for executable data contracts beginning in Batch 1B-B (Section 19); its exact version constraint remains unresolved.

### 17a. Configuration Enum Contract (Decision Group 3, `AUTHOR-APPROVED`)

| Field | Detail |
|---|---|
| Exact path | `src/btmm_ai_scanner/config/enums.py` |
| Approved classes | `InternalSymbol` (`StrEnum`); `Timeframe` (`StrEnum`) |
| `InternalSymbol` values | `XAUUSD`, `EURUSD`, `GBPUSD` (uppercase strings, matching the approved values exactly) |
| `Timeframe` values | `M1`, `M5`, `M15`, `H1`, `H3`, `H4`, `D1`, `W1` (uppercase strings, matching the approved values exactly) |
| Permitted content | Two `StrEnum` classes with the values above only |
| Prohibited content | Any POI-type enum; any lifecycle-state enum; any BTMM-state enum; any validation-state enum; any provider-specific symbol; any unsupported timeframe; any resampling logic |
| Standard-library-only requirement | Yes — `enum.StrEnum` (stdlib since Python 3.11) only, no third-party import |
| Required tests | Exercised indirectly by `tests/test_import_smoke.py` (import) and `tests/test_config_precedence.py` (usage in precedence merging) |
| Deferred behavior | Any helper/validation method beyond the enum definitions themselves is not defined here |

**Exact future implementation (`AUTHOR-APPROVED`, Decision Group 4):**

```python
from enum import StrEnum


class InternalSymbol(StrEnum):
    XAUUSD = "XAUUSD"
    EURUSD = "EURUSD"
    GBPUSD = "GBPUSD"


class Timeframe(StrEnum):
    M1 = "M1"
    M5 = "M5"
    M15 = "M15"
    H1 = "H1"
    H3 = "H3"
    H4 = "H4"
    D1 = "D1"
    W1 = "W1"
```

Rules: no aliases; no auto-generated values (`enum.auto()`); no POI-state enum; no lifecycle-state enum; no BTMM-state enum; no validation-state enum; no provider-specific value; no unsupported timeframe; no resampling behavior.

### 17b. Configuration Loader Contract (Decision Group 3, `AUTHOR-APPROVED`)

| Field | Detail |
|---|---|
| Exact path | `src/btmm_ai_scanner/config/loader.py` |
| Approved responsibilities | Read approved non-secret runtime environment variables; normalize approved environment keys; merge configuration layers deterministically; return a new mapping; preserve caller-owned input mappings; standard library only |
| Environment-variable prefix | `BTMM_CONFIG_` (example: `BTMM_CONFIG_LOG_LEVEL` → `log_level`) |
| Approved precedence (later overrides earlier) | (1) Project defaults; (2) Environment-specific non-secret overrides; (3) Runtime environment overrides |
| Approved merge behavior | **Shallow top-level merge only** — no nested/deep merge |
| Permitted content | Environment reading, key normalization, shallow deterministic merging, returning a new mapping, secret-value exclusion |
| Prohibited content | YAML parsing; filesystem configuration loading; Pydantic models; `pydantic-settings`; nested merge behavior; production configuration; provider-specific configuration; configuration persistence; logging configuration values; credential storage; trading-rule validation |
| Standard-library-only requirement | Yes |
| Required tests | `tests/test_config_precedence.py` |
| Deferred behavior | Nothing remains deferred at the interface level — the exact signature, precedence, merge, key-validation, and runtime-environment behaviors are all recorded below (Decision Group 4). The internal implementation of each private helper is not written here. |

**Exact future public interface (`AUTHOR-APPROVED`, Decision Group 4):**

```python
from collections.abc import Mapping

ENV_PREFIX = "BTMM_CONFIG_"


def load_configuration(
    project_defaults: Mapping[str, object] | None = None,
    environment_overrides: Mapping[str, object] | None = None,
    *,
    environ: Mapping[str, str] | None = None,
) -> dict[str, object]:
    ...
```

**Approved precedence:** `project_defaults` → `environment_overrides` → runtime values extracted from `environ`. Later layers replace earlier values. `None` means an empty layer. `environ=None` reads `os.environ`; a supplied `environ` mapping completely isolates the call from the machine environment. Caller-owned mappings remain unchanged; a new `dict` is returned. Merge behavior is shallow — a later nested mapping **replaces** an earlier nested mapping entirely (no recursive merge).

**Mapping-key rules:** keys must be strings; keys must be non-empty; keys must already use lowercase snake_case (conceptual accepted pattern: `[a-z][a-z0-9_]*`). Invalid keys raise `InvalidConfigurationKeyError`. Mapping keys are never silently renamed.

**Runtime-environment rules:** only keys beginning with the exact, case-sensitive prefix `BTMM_CONFIG_` are considered; the prefix is stripped; the suffix is converted to lowercase; the normalized suffix is validated against the same key rules above; an empty suffix is invalid; duplicate runtime variables that normalize to the same key are invalid; unprefixed variables are ignored; runtime values remain strings.

### 17c. Secret Boundary (Decision Group 3, `AUTHOR-APPROVED`)

The non-secret configuration loader must **reject** any key containing indicators such as: `password`, `secret`, `token`, `credential`, `api_key`, `private_key`. Rules: do not read `.env`; do not return credential values; do not log credential names or values; do not create credential defaults; do not add provider API keys. **Actual secret retrieval remains a separate, future, unbuilt boundary** — this loader only excludes secret-like keys, it does not manage secrets.

**Exact detection behavior (`AUTHOR-APPROVED`, Decision Group 4):** detection is case-insensitive; a match anywhere in the normalized key is rejected; detection applies to all three configuration layers (project defaults, environment-specific overrides, runtime environment); a match raises `SecretConfigurationKeyError`; neither the rejected key nor its value may be exposed (e.g., in an exception message or a log line).

### 17d. Configuration Package Exports (Decision Group 4, `AUTHOR-APPROVED`)

**`src/btmm_ai_scanner/config/__init__.py` is not created by this document.** Its exact future export surface:

```python
from .enums import InternalSymbol, Timeframe
from .loader import (
    ENV_PREFIX,
    ConfigurationError,
    InvalidConfigurationKeyError,
    SecretConfigurationKeyError,
    load_configuration,
)

__all__ = [
    "ENV_PREFIX",
    "ConfigurationError",
    "InternalSymbol",
    "InvalidConfigurationKeyError",
    "SecretConfigurationKeyError",
    "Timeframe",
    "load_configuration",
]
```

**No other public configuration API enters Batch 1B-A.**

### 17e. Exception Hierarchy (Decision Group 4, `AUTHOR-APPROVED`)

**Exact future hierarchy:**

```python
class ConfigurationError(ValueError):
    """Base error for invalid non-secret configuration."""


class InvalidConfigurationKeyError(ConfigurationError):
    """Raised when a configuration key has an invalid form."""


class SecretConfigurationKeyError(ConfigurationError):
    """Raised when a secret-like key enters non-secret configuration."""
```

**Rules:** no custom constructors in Batch 1B-A; no rejected key name in an exception message; no rejected value in an exception message; no provider-specific exception; no trading-domain exception.

**Approved generic messages:**
- `InvalidConfigurationKeyError`: `"Configuration keys must use lowercase snake_case."`
- `SecretConfigurationKeyError`: `"Secret-like keys are not permitted in non-secret configuration."`

### 17f. Private Implementation Boundary (Decision Group 4, `AUTHOR-APPROVED`)

`loader.py` may contain private helpers only for: key validation; secret-indicator detection; runtime-environment extraction; shallow layer merging. **Private helper names must begin with an underscore.**

**Prohibited helpers and facilities:** a YAML reader; a `.env` reader; a recursive or nested merge utility; a secret loader; a provider configuration loader; a global configuration singleton.

## 18. Rule and Schema Manifest Scope

**No manifest file or manifest directory is created by this document.** Per Part 11's rules (unchanged, reaffirmed here): `manifests/rules/` and `manifests/schemas/` are created only when their first real, reviewed manifest exists — never as empty directories, never via `.gitkeep`. No private-book text may ever enter a manifest. No manifest may approve a trading rule — it only records already-approved knowledge. Canonical JSON serialization (Blocking Decision #20) remains unresolved. **Exact first proposed filenames** (naming requires separate author approval, per Blocking Decision #15): a first rule-version manifest named illustratively `manifests/rules/measurement_standards_v1.json`, and a first schema-version manifest named illustratively `manifests/schemas/raw_candle_record_v1.json` — both illustrative only, neither created, neither approved.

## 19. Data-Contract Stub Scope

Covered file-by-file in Section 9 (Batch 1B-B rows) and Section 10. Summary: `types.py` (identity/fingerprint/version value types), `raw_candle.py` (Contract A), `normalized_candle.py` (Contract B), `validation_result.py` (Contract N), `provenance_record.py` (Contract M), `rule_version_manifest.py`/`schema_version_manifest.py` (Contracts Q/R, shape only — no manifest I/O). **Resolved by Phase 1B-B Decision Group 1** (`PHASE_1B_AUTHOR_DECISION_REGISTER.md` §19A): Pydantic v2 (`pydantic>=2.13.4,<2.14`) is required for these classes; a plain-dataclass placeholder is explicitly rejected. The exact Pydantic base-model configuration (`frozen`, `extra`, strict validation, etc.) remains unresolved and is deferred to Decision Group 2.

## 20. Validation-Stub Scope

Covered file-by-file in Section 9 (Batch 1B-C rows). Summary: `completeness.py`, `duplicates.py`, `gaps.py` implement the Group 6 enums and *interfaces* only (no provider-specific completion/gap-confirmation mechanism — Blocking Decisions #26–#27 remain open); `eligibility.py` implements the approved 8-step gating sequence. **Kept structurally separate, per the existing Phase 0G/Phase 1A validity-separation principle:** `data_quality_validity` (this batch's actual concern), `poi_validity`, `btmm_validity`, `entry_validity`, and `trade_outcome` (all out of scope for this batch and this scaffold entirely). No automatic conflicting-duplicate winner is designed — `duplicates.py`'s conflicting-duplicate path always routes to `QUARANTINED`, per the already-approved policy.

## 21. Provenance and Audit Stub Scope

Covered file-by-file in Section 9 (Batch 1B-D rows). Summary: `audit/events.py` (Contract O shape), `audit/writer.py` (append-only interface, no physical storage target chosen), `observability/logging_config.py` (structured JSON operational logging, explicitly separate from the audit writer). `provenance_record.py` itself is placed in Batch 1B-B (it is a data contract, Contract M) rather than Batch 1B-D, since every other contract in Batch 1B-B already references provenance lineage.

## 22. Ingestion-Interface Scope

Covered file-by-file in Section 9 (Batch 1B-E rows). Summary: `ingestion/port.py` (`MarketDataSourcePort`, abstract), `ingestion/requests.py`/`results.py` (provider-neutral shapes), `ingestion/offline_file_source.py` (the single permitted concrete implementation — reads one fixed local file, no network access, no credential). No FXCM adapter, no TradingView adapter, and no `HISTORICAL_BATCH`/`POLLING`/`STREAMING` retrieval mode is proposed — all remain explicitly not-yet-authorized per Decision Group 7.

## 23. Test Scope

Exact proposed test paths and names are listed in full in Section 8 (tree) and Section 9 (inventory) — 18 test files across Batches 1B-A through 1B-E: `test_import_smoke.py`, `test_config_precedence.py`, `test_identity_and_fingerprint.py`, `test_semver.py`, `test_raw_candle_contract.py`, `test_normalized_candle_contract.py`, `test_manifest_compatibility_classes.py`, `test_candle_completeness.py`, `test_exact_duplicate.py`, `test_conflicting_duplicate_quarantine.py`, `test_missing_gap.py`, `test_no_synthetic_fill.py`, `test_validation_eligibility.py`, `test_processing_version_preservation.py`, `test_audit_log_separation.py`, `test_secret_redaction.py`, `test_ingestion_port_contract.py`, `test_offline_file_stub.py`. **No fixture or test file is created by this document.**

**Phase 0G lifecycle-test restrictions remain fully binding and are unaffected by this scaffold plan:** full lifecycle tests remain limited to the 18 propagated bounded directional POIs (none of which appear in this scaffold at all — POI/lifecycle modules are entirely out of scope for Batches 1B-A–1B-F); Equal Highs/Equal Lows/both Trendlines remain limited to construction/classification/permitted-state/prohibition tests only, once their module exists (it does not, in this scope); generic bounded-lifecycle tests remain `NOT_APPLICABLE` to the 14 non-bounded reference structures. **No detector test is proposed or needed in this initial scaffold** — no detector module exists in Section 8's tree.

### 23a. Batch 1B-A Test Boundaries (Decision Group 3, `AUTHOR-APPROVED`)

**`tests/test_import_smoke.py` — approved coverage:**
- `btmm_ai_scanner` imports
- `btmm_ai_scanner.config` imports
- `InternalSymbol` imports
- `Timeframe` imports
- `config.loader` imports
- Importing performs no filesystem write
- Importing performs no network action

**`tests/test_import_smoke.py` — prohibited scope:** POIs; market structure; BTMM; ingestion; AI; signals; trading; profitability; databases; live providers.

**`tests/test_config_precedence.py` — approved coverage:**
1. Defaults remain when no override exists
2. Environment-specific values override defaults
3. Runtime environment values override both earlier layers
4. Input mappings remain unchanged
5. `BTMM_CONFIG_` is stripped
6. Environment keys are normalized
7. Secret-like keys are rejected
8. Merge results are deterministic
9. No YAML or filesystem input is required
10. Tests use controlled mappings or monkeypatched environment values
11. Tests do not depend on real machine credentials or configuration

**Confirmed for both tests:** no detector test enters Batch 1B-A; no ingestion test enters Batch 1B-A; no lifecycle test enters Batch 1B-A; no AI, signal, risk, or execution test enters Batch 1B-A. All Phase 0G testing restrictions (above) remain unchanged.

**Exact test-function inventory (Decision Group 4, `AUTHOR-APPROVED`):**

`tests/test_import_smoke.py` — exactly three test functions:
1. `test_package_imports`
2. `test_config_public_exports_import`
3. `test_import_has_no_filesystem_or_network_side_effect`

**`test_import_has_no_filesystem_or_network_side_effect` must:** avoid importing the package during test-module collection; import from inside the test function; disable bytecode writing for the isolated import; use a temporary working directory; block attempted network connections; verify the temporary directory remains unchanged; restore modified interpreter state.

`tests/test_config_precedence.py` — exactly 11 test functions:
1. `test_defaults_are_preserved`
2. `test_environment_overrides_defaults`
3. `test_runtime_environment_overrides_all_layers`
4. `test_input_mappings_are_not_mutated`
5. `test_runtime_environment_keys_are_normalized`
6. `test_shallow_merge_replaces_nested_value`
7. `test_secret_like_keys_are_rejected`
8. `test_invalid_configuration_key_is_rejected`
9. `test_empty_runtime_suffix_is_rejected`
10. `test_duplicate_normalized_runtime_keys_are_rejected`
11. `test_result_is_deterministic`

**Rules:** `test_secret_like_keys_are_rejected` may be parameterized over the six secret indicators and the three configuration layers. Tests pass controlled `environ` mappings. Tests never rely on real machine configuration or credentials.

## 24. CI Scope

Exact proposed workflow path: `.github/workflows/ci.yml` (Batch 1B-F, the only file in that batch). Planned checks: `uv lock` consistency check, Ruff format check, Ruff lint, mypy, pytest — all offline-safe. **Rules (binding, unchanged from Decision Group 5):** no private-book access; no live credentials; no provider connection; no deployment; no signals; no order execution; no write-back to repository files. **Trigger policy (Blocking Decision #28), branch-protection policy (#29), and mandatory pull-request policy (#30) remain author decisions** — the latter two are GitHub repository *settings*, not files this scaffold creates, and are explicitly out of this document's file-scope authority.

## 25. Documentation Updates

This task updates exactly two existing documents (in addition to creating this one):
- `docs/architecture/REPOSITORY_SCAFFOLD_PLAN.md` — new Section 9, "Phase 1B Exact Scope Planning" (Part 17 below).
- `docs/PROJECT_STATE.md` — new Section 23 (Part 18 below).

No other document is modified.

## 26. Verification Commands

**No command below is executed by this document.** Once Batch 1B-A is separately approved and implemented, the following command *categories* (not exact invocations, since exact dependency/version details remain Blocking Decisions) would verify it: interpreter-version check; `uv` environment sync; `uv run pytest` (import smoke + config precedence); `uv run ruff format --check`; `uv run ruff check`; `uv run mypy`. Exact commands are deferred to Part 16 (Command Plan) at implementation time, not fixed here.

## 27. Rollback and Recovery

Each batch's rollback boundary is recorded in Section 14. General principle: since batches are committed independently and in the strict order of Section 15, rolling back means reverting the single most recent batch's commit(s) — no batch's rollback requires touching an earlier batch's files, because dependencies only ever point to earlier, already-stable batches (Section 13). No batch writes to any file outside `src/btmm_ai_scanner/`, `tests/`, `.github/workflows/`, or the repository-root toolchain files (`pyproject.toml`, `uv.lock`, `.python-version`) — so rollback never risks `docs/`, `knowledge/`, or `references/`.

### 27a. Batch 1B-A Approved Rollback Procedure (Decision Group 4, `AUTHOR-APPROVED`)

**Before commit, on any unexpected result during Stages A–J (Section 16a):** (1) stop; (2) report the discrepancy; (3) do not improvise a version or dependency replacement; (4) do not stage or commit partial work.

**Approved repository rollback:** restore only `.gitignore`; delete only the nine approved new paths; delete only the generated `.venv/` directory; remove empty `src/` and `tests/` directories created solely by the failed batch; verify return to the original clean checkpoint.

**Prohibited rollback commands:** `git reset --hard`; `git clean -fd`; `git clean -fdx`; force push; deleting `references/private/`; broad deletion of unrelated untracked files.

**Installed tooling:** installed `uv` is not automatically removed; installed managed Python is not automatically removed. Removing either requires separate authorization.

**After commit:** use a reviewed `git revert <batch-commit>`. Never rewrite shared history.

## 28. Phase 1B Scaffold Acceptance Criteria

A batch is acceptable for merge only if: (1) every file it adds appears in Section 9's inventory with a matching batch label; (2) every test listed for that batch exists and passes; (3) Ruff format/lint and mypy pass with zero errors; (4) no file outside the batch's declared scope was touched; (5) no entry/risk/execution/POI/BTMM/AI content was introduced; (6) every blocking decision listed against that batch in Section 6 has been separately author-resolved before the batch is implemented; (7) the batch was reviewed and committed on its own, not bundled with a later batch.

### 28a. Batch 1B-A Exact Acceptance Criteria (Decision Group 4, `AUTHOR-APPROVED`)

Batch 1B-A passes implementation review only when: exactly ten approved paths changed; `.gitignore` preserves `references/private/*`; Python is exactly `3.12.13`; `uv` is exactly `0.11.30`; `uv.lock` exists; `uv lock --check` passes; runtime dependencies remain empty; only `pytest`, `mypy`, and Ruff are development dependencies; Ruff format check passes; Ruff lint passes; mypy passes; pytest passes; imports have no filesystem or network side effect; no YAML exists; no Pydantic exists; no provider or ingestion logic exists; no POI or BTMM logic exists; no AI, signal, risk, or execution logic exists; no secret is stored or exposed; **nothing is staged, committed, or pushed before review.**

## 29. Decisions Requiring Author Approval

All 30 items in Section 6, plus: the package/distribution identity in Section 7; whether to adopt this document's proposed batch boundaries (Section 14) as-is or amend them; whether to adopt the proposed file inventory (Section 9) as-is or amend it before Batch 1B-A begins.

## 30. Approval Status

**`ENGINEERING-RECOMMENDED`. `NOT YET AUTHOR-APPROVED`. `NOT YET IMPLEMENTED`. `NOT PRODUCTION-APPROVED`.** This document proposes a file-level scope; it does not approve itself. No batch may be implemented until the author has separately reviewed and approved: package identity (Section 7), the blocking decisions relevant to that batch (Section 6), and the batch's exact file list (Section 9/14). No file listed anywhere in this document exists as a result of this task.

**This Section 30 status statement describes the document as originally authored and remains unchanged. Batch 1B-A's actual, current implementation status is recorded separately in Section 31 below — this addition does not alter Section 30's own approval-status text.**

---

## 31. Phase 1B-A Implementation Closure

**Batch 1B-A: `IMPLEMENTED`, `VERIFIED`, `COMMITTED`, `PUSHED`. `NOT PRODUCTION-APPROVED`.**

- **Commit:** `47cfd699bb7f4893774579f1693abbbb57b91607` — "Implement Phase 1B-A software foundation"
- **Exact ten committed paths:** `.gitignore` (modified); `.python-version`, `pyproject.toml`, `src/btmm_ai_scanner/__init__.py`, `src/btmm_ai_scanner/config/__init__.py`, `src/btmm_ai_scanner/config/enums.py`, `src/btmm_ai_scanner/config/loader.py`, `tests/test_config_precedence.py`, `tests/test_import_smoke.py`, `uv.lock` (all new) — **this matches the plan in Section 9 exactly**, with no path added or removed relative to what was documented before implementation.
- **All acceptance criteria (Section 28a) passed:** exactly ten paths changed; `references/private/*` preserved; Python exactly `3.12.13`; `uv` exactly `0.11.30`; `uv.lock` exists and `uv lock --check` passes; runtime dependencies remain empty; only `pytest`/`mypy`/`ruff` are development dependencies; Ruff format check passes; Ruff lint passes; mypy passes; pytest passes (34 collected, 34 passed); imports have no filesystem or network side effect; no YAML; no Pydantic; no provider/ingestion/POI/BTMM/AI/signal/risk/execution logic; no secret stored or exposed.
- **`.venv/` exists locally but remains git-ignored** — confirmed absent from `git status` and matched by the `.gitignore:10:.venv/` rule.
- **No Batch 1B-B path exists** in the repository. **No prohibited scope entered the batch** (confirmed by dedicated forensic review).

**External toolchain state (distinct from repository implementation — none of the following is a repository file or commit):**
- `uv` `0.11.30` installed at `C:\Users\GOLD COMPUTERS\.local\bin\uv.exe` (not on PATH; PATH was not modified).
- uv-managed Python `3.12.13` installed at `C:\Users\GOLD COMPUTERS\AppData\Roaming\uv\python\cpython-3.12.13-windows-x86_64-none\` — confirmed complete and fully functional.
- Pre-existing Python `3.14.6` remains installed and untouched.
- The generic minor-version convenience alias (`cpython-3.12-windows-x86_64-none`) remains a broken/dangling directory junction — **acknowledged, external, functionally limited, non-blocking for this exact-patch project.** No repair was authorized or performed.

**Explicit author exception (governance, not a technical finding):** during the original implementation run, two genuine bugs (a Ruff `RUF100`/`F401` finding and a Windows-specific `tempfile`/`chdir` race) were fixed by directly editing the already-in-scope test file, rather than stopping and requesting separate re-authorization as the governing procedure required. This procedural deviation was disclosed in full via a dedicated forensic review and **accepted by explicit author exception** — the deviation is not hidden, minimized, or retroactively recharacterized as compliant; it is recorded here exactly as it occurred. Full deviation-by-deviation detail is preserved in `PHASE_1B_AUTHOR_DECISION_REGISTER.md` Section 18c.

**Master file inventory (Section 9) total remains 50 rows** — no row was added or removed by this closure; only the 10 Batch 1B-A rows' scope-status column was updated to `IMPLEMENTED`, and the Batch 1B-A summary row in Section 14 was updated accordingly. Batches 1B-B through 1B-F (39 remaining rows) remain exactly as previously documented — no row beyond Batch 1B-A is marked implemented.

## 32. Phase 1B-B Implementation Closure

**Batch 1B-B: `IMPLEMENTED`, `VERIFIED`, `COMMITTED`, `PUSHED`, `CLOSED`. `NOT PRODUCTION-APPROVED`.**

- **Commit:** `1b8602a5dcc97a89be51ba5ee65ab4940751567a` — "Implement Phase 1B-B contract foundation". **Execution-captured baseline and rollback target (per Baseline Correction 6B, `PHASE_1B_AUTHOR_DECISION_REGISTER.md` §26):** `4074a80fe53b7784d3a51a3ac15f2fe85d244104`.
- **Exact 17 committed paths:** `pyproject.toml`, `uv.lock` (both modified); the 15 inventoried Batch 1B-B files — 8 new source files (`contracts/__init__.py`, `types.py`, `raw_candle.py`, `normalized_candle.py`, `validation_result.py`, `provenance_record.py`, `rule_version_manifest.py`, `schema_version_manifest.py`) and 7 new test files (`test_identity_and_fingerprint.py`, `test_semver.py`, `test_raw_candle_contract.py`, `test_normalized_candle_contract.py`, `test_validation_result.py`, `test_provenance_record.py`, `test_manifest_compatibility_classes.py`) — **this matches the plan in Section 9 exactly**, with no path added or removed relative to what was documented before implementation. No eighteenth path was committed.
- **Environment/dependency results:** Python `3.12.13`; `uv` `0.11.30`; Pydantic `2.13.4` (the sole direct runtime dependency, `pydantic>=2.13.4,<2.14`); `pytest 9.1.1`/`mypy 2.3.0`/`Ruff 0.15.22` unchanged.
- **Verification results:** pre-implementation baseline 34 passed; final full suite 221 passed; original baseline tests re-run after implementation 34 passed; exact top-level test-function total 132; `uv lock --check` passed; Ruff format check passed; Ruff lint passed; mypy passed; all 17 public contract names import successfully in the exact approved order.
- **Architectural audit verdict:** `B. PASS WITH NON-BLOCKING FINDINGS — READY FOR COMMIT REVIEW`. No blocking finding remained before commit.
- **Pre-commit test correction (`POLICY-PRESERVING PRE-COMMIT TEST-COVERAGE CORRECTION`):** `tests/unit/test_validation_result.py`'s `test_validation_result_validates_reason_code_format` gained one additional invalid padded reason-code parameter, `" CODE_A"`. No production code changed; `ValidationResult`'s top-level test-function count remained 16; the grand total remained 132; the full collected test total rose from 220 to 221.
- **Procedural deviation (`DISCLOSED NON-PERSISTENT PROCEDURAL DEVIATION`):** during the implementation's AST verification, a temporary file `scratch_ast_check.py` was created, briefly introducing an eighteenth untracked path, contrary to the governing instruction's prohibition on creating a verification script and its stop-on-18th-path condition, which was not honored in the moment. The file was deleted before final verification, was never staged or committed, and does not appear in the final 17-path scope. Accepted for closure; no rollback or code correction required; full detail preserved in `PHASE_1B_AUTHOR_DECISION_REGISTER.md` §27I.
- **Inventory total remains 52 rows** — no row was added, removed, or renumbered by this closure; only the 15 Batch 1B-B rows' scope-status column and the Batch 1B-B summary row in Section 14 were updated to `IMPLEMENTED`. Batch 1B-B remains **15 inventoried files**; the final Batch 1B-B changed-path count is **17**.
- **The execution-captured-baseline policy (Baseline Correction 6B) is preserved for future implementation batches** — no future batch's baseline or rollback target is hard-coded to this commit; each future batch will capture its own clean synchronized HEAD immediately before its first implementation change.
- **No Phase 1B-C path exists** in the repository. No prohibited scope entered the batch.

## 33. Phase 1B-C Decision Group 1 — Proposed Market-Data Pipeline File Scope (Not Yet Inventoried)

**Status: `AUTHOR-APPROVED PROPOSED IMPLEMENTATION SCOPE`, `NOT YET IMPLEMENTED`, `NOT PRODUCTION-APPROVED`.** Full detail: `PHASE_1B_AUTHOR_DECISION_REGISTER.md` Section 28 (§28N author approval record).

**This section adds no inventory row.** The master file inventory (Section 9) remains exactly **52 rows**; Batch 1B-B remains **15 inventoried files**. Every path named below is an **author-approved proposed implementation scope** — approved as an architecture, but **not yet created**, and **not yet entered into the Section 9 inventory** until a separate, explicit implementation-control approval adds it. Approval of this scope does not itself authorize implementation.

**Proposed source files (9), all under `src/btmm_ai_scanner/market_data/`:** `__init__.py`; `source_input.py` (provider-neutral, immutable `SourceCandleInput` — `availability_time_utc`/`original_availability_time` are mandatory keys with explicitly nullable values, no automatic default, per the corrected AT-1 decision matrix, §28H; caller-supplied identity/version boundary; no provider networking or payload parsing); `source_mapping.py` (FXCM provider-symbol → `InternalSymbol`/`Timeframe` mapping policy only, separate from the TradingView visual-reference mapping, not a network adapter); `results.py` (`IngestionOutcome`, `IngestionResult` — no gap-relationship model); `raw_candle_builder.py` (`build_historical_raw_candle`/`build_live_raw_candle` stateless entry points accepting `SourceCandleInput` only, never an arbitrary provider dictionary; the availability-evidence decision; `RawCandle` construction; `ValidationResult` integration); `normalization.py` (`RawCandle → NormalizedCandle`); `idempotency.py` (stateless identity key, `EXACT_DUPLICATE`/`CONFLICTING_REVISION`/`NEW_RECORD`); `gap_observation.py` (`GapObservation`, `POTENTIAL_GAP` only); `ports.py` (`RawCandleSink`/`NormalizedCandleSink`/`CandleReadRepository`/`HistoricalReplaySource` protocols).

**Proposed test files (8), all under `tests/unit/`:** `test_source_input_and_results.py` (8 — covers the exact availability decision matrix); `test_source_mapping.py` (7); `test_raw_candle_builder.py` (8 — covers `SourceCandleInput`-only construction and no-provider-parser boundary); `test_normalization.py` (7); `test_idempotency.py` (8); `test_gap_observation.py` (7); `test_ingestion_ports.py` (6); `test_historical_live_separation_and_no_synthetic_fabrication.py` (6).

**Total proposed new paths: 17** (9 source + 8 test). **Recommended exact top-level test-function count: 57** (§28L breakdown, `8+7+8+7+8+7+6+6`). **No dependency change proposed. No eighteenth implementation path. No documentation file would change during that future implementation.**

**Reused without duplication:** `RawCandle`, `NormalizedCandle`, `ValidationResult`, `ProvenanceRecord`, `RuleVersionManifest`, `SchemaVersionManifest`, `InternalSymbol`, `Timeframe` — all already implemented and closed in Batch 1B-B. No duplicate candle, symbol, timeframe, validation, or provenance model is proposed.

**Provider symbols and TradingView visual references are kept separate** (§28A/§28E): the source-mapping key uses `(provider, provider_symbol, provider_timeframe)` — the TradingView ticker (`FXCM:XAUUSD` etc.) is never treated as the provider payload's raw symbol.

**First-batch input boundary (corrected):** the first batch begins at `SourceCandleInput`, not at a raw external provider payload — FXCM REST/WebSocket parsing, CSV parsing, TradingView scraping, historical-download parsing, and broker-specific payload adapters are all explicitly excluded and deferred to a future provider adapter. Corrected first-batch flow: `SourceCandleInput → structural/input validation → availability-evidence decision → RawCandle construction → ValidationResult → symbol/timeframe mapping → NormalizedCandle → idempotency decision → potential-gap observation → storage/replay port boundary`.

**Author-approved resolutions (approved before Decision Group 2, not an open question):** AT-1 (availability-time-quality representation — `SourceCandleInput` requires explicit availability fields; missing evidence yields `INDETERMINATE` before any `RawCandle` is built); RM-1 (conflicting-revision resolution policy — quarantined, no automatic winner, no silent overwrite); GAP-1 (trading-session/calendar boundary — `POTENTIAL_GAP` only in the first batch) — full detail in `PHASE_1B_AUTHOR_DECISION_REGISTER.md` §28M. **The Decision Group itself is `AUTHOR-APPROVED`, `NOT YET IMPLEMENTED`, `NOT PRODUCTION-APPROVED`** — Phase 1B-C Decision Group 1 was author-approved before Decision Group 2 (§28N); it has not been implemented and has not received production approval. Decision Group 2 (§29, also `AUTHOR-APPROVED`, `NOT YET IMPLEMENTED`, `NOT PRODUCTION-APPROVED`) does not reopen or replace Decision Group 1 — the approved `1B-C-MD` implementation scope is governed jointly by Decision Group 1's approved architecture and Decision Group 2's approved implementation controls.

**Phase 1B-C remains not implemented.** No ingestion adapter, POI logic, BTMM logic, indicator, alert, backtesting, or robot-execution code exists.

**Superseded in part — see Section 34 below.** Section 33's statement "this section adds no inventory row" and "the master file inventory (Section 9) remains exactly 52 rows" described the state through Phase 1B-C Decision Group 1 only. Phase 1B-C Decision Group 2 (§29 of the register) explicitly adds inventory rows; Section 33's own architecture content (§28A–§28M) is otherwise unchanged and not reopened.

## 34. Phase 1B-C Decision Group 2 — Market-Data Pipeline Inventory Addition

**Status: `AUTHOR-APPROVED SCOPE` (architecture, §28N) with `AUTHOR-APPROVED implementation controls` (§29O), `NOT YET IMPLEMENTED`, `NOT PRODUCTION-APPROVED`.** Full detail: `PHASE_1B_AUTHOR_DECISION_REGISTER.md` Section 29 (§29O author approval record).

**This decision group adds exactly 17 new rows to the master file inventory (Section 9),** under the new batch tag `1B-C-MD` (kept distinct from the pre-existing `1B-C` batch tag used for the unrelated validation/eligibility batch in Section 14 — see the disambiguation note in Section 9's inventory-growth paragraph). **The inventory grows from 52 rows to 69 rows.** No existing row (1 through 52) was modified, removed, renumbered, or reassigned. Creation order for the 17 new rows continues sequentially from 52 through 68, following the exact Stage A–E construction order defined in the register's §29L — this numbering is a documentation convenience only and does not imply any dependency on Batches 1B-C (validation/eligibility), 1B-D, 1B-E, or 1B-F, none of which this batch depends on (its only dependency is Batch 1B-B's `contracts/` and `config/enums.py`, per §28D/§29A).

**The 17 new rows, by path (full detail in each row, Section 9):**

| Stage | Paths |
|---|---|
| A | `market_data/source_input.py`, `market_data/results.py`, `tests/unit/test_source_input_and_results.py` |
| B | `market_data/source_mapping.py`, `tests/unit/test_source_mapping.py` |
| C | `market_data/raw_candle_builder.py`, `market_data/normalization.py`, `tests/unit/test_raw_candle_builder.py`, `tests/unit/test_normalization.py` |
| D | `market_data/idempotency.py`, `market_data/gap_observation.py`, `tests/unit/test_idempotency.py`, `tests/unit/test_gap_observation.py` |
| E | `market_data/ports.py`, `market_data/__init__.py`, `tests/unit/test_ingestion_ports.py`, `tests/unit/test_historical_live_separation_and_no_synthetic_fabrication.py` |

All 9 source-file paths are under `src/btmm_ai_scanner/market_data/`; all 8 test-file paths are under `tests/unit/`. Every row is **New** (no modified file). No `pyproject.toml`/`uv.lock` row is affected — this decision group introduces no dependency change.

**Each of the 17 new rows carries the exact same scope status:** `AUTHOR-APPROVED SCOPE` (architecture-level, §28N) and `AUTHOR-APPROVED implementation controls` (this decision group, §29O) — the author explicitly approved Decision Group 2 in full following a final read-only architectural audit verdict of `A. PASS — READY FOR AUTHOR APPROVAL` (no blocking or non-blocking finding), `NOT YET IMPLEMENTED`, `NOT PRODUCTION-APPROVED`. This approval does not itself authorize implementation; a future, separate, explicit implementation-authorization instruction (distinct from Batch 1B-B's `AUTHORIZE PHASE 1B-B IMPLEMENTATION` phrase) is still required before the exact, reviewable file list recorded here may be created.

**This decision group does not implement, stage, commit, or push any of the 17 rows.** No `market_data/` directory exists in the repository as of this decision group. No Batch 1B-A/1B-B row's status, path, or content is altered.

**Superseded in part — see Section 35 below.** The "not yet inventoried"/"proposed" framing above (and this section's own prior "does not implement" statement) described the state before implementation. The 17 rows were subsequently implemented, committed, and pushed under the `1B-C-MD` batch tag; Section 34's inventory-addition record itself (the 52→69 row count, the exact 17 paths, the exclusion from Batch 1B-A/1B-B) remains accurate and unchanged.

## 35. Phase 1B-C-MD Implementation Closure

**Batch `1B-C-MD`: `AUTHOR-APPROVED SCOPE`, `IMPLEMENTED`, `VERIFIED`, `ARCHITECTURALLY AUDITED`, `COMMITTED`, `PUSHED`, `CLOSED`, `NOT PRODUCTION-APPROVED`.**

- **Commit:** `d328776abb5a2c1f42e185b9bc80f0e5a371897e` — "Implement Phase 1B-C market-data pipeline foundation". **Execution-captured baseline and rollback target:** `1a439f3a1b4b4f6189ec4c209362f5d592910160`.
- **Exact 17 committed paths — all `New`, none modified:** the 9 source files and 8 test files named in Section 34, matching the plan exactly. No eighteenth path was committed.
- **Environment/dependency results:** Python `3.12.13`; `uv` `0.11.30`; Pydantic `2.13.4`; no dependency change (`pyproject.toml`/`uv.lock` untouched).
- **Verification results:** pre-implementation baseline 221 passed (34 original + 132 Batch 1B-B top-level functions); final full suite 281 passed; original baseline re-run 34 passed; corrected targeted suite 15 passed; exactly 57 new top-level test functions (189 combined with the existing 132); `uv lock --check` passed; Ruff format/lint passed; mypy passed; all 20 public exports import successfully in the exact approved order.
- **Architectural audit history:** initial read-only audit verdict `C. CORRECTION REQUIRED BEFORE STAGING` (three blocking findings: mutable FXCM registries, incomplete naive-timestamp coverage, missing genuine non-UTC normalization/offset-preservation coverage); all three corrected within the exact 17-path scope; final correction audit verdict `A. PASS — READY FOR STAGING REVIEW`; staged-diff review verdict `A. PASS — READY FOR COMMIT REVIEW`. No blocking or non-blocking finding remained at commit authorization.
- **No procedural deviation.** No temporary or generated verification file was created at any point.
- **Inventory total remains 69 rows** — no row was added, removed, or renumbered by this closure; only the 17 `1B-C-MD` rows' scope-status column, the Section 14 batch-summary row, and the Section 9 totals sentence were updated to reflect implementation.
- **This closure does not authorize production use, live trading, an indicator, a robot, provider networking, or a persistence backend.** No such component was implemented in this batch. Full detail: `PHASE_1B_AUTHOR_DECISION_REGISTER.md` §30.

## 36. Phase 1B-E Decision Group 1 — Reconciliation and Implementation Controls (Inventory Notes)

**Status: `AUTHOR-APPROVED SCOPE`, `IMPLEMENTED`, `VERIFIED`, `ARCHITECTURALLY AUDITED`, `COMMITTED`, `PUSHED`, `CLOSED`, `NOT PRODUCTION-APPROVED`.** Approval date: 2026-07-26. Implementation commit: `0a9814eddd1cdeda59cf95dbde8a806f30800b44`. Full detail: `PHASE_1B_AUTHOR_DECISION_REGISTER.md` §31N (approval), §31O (implementation closure).

**This section adds no inventory row and removes none.** The Batch 1B-E inventory (Section 9, rows 44–50) remains exactly 7 rows; the master inventory remains exactly 69 rows. **One existing row's description text was corrected** (row 48, `ingestion/offline_file_source.py`): the prior wording ("Reads a fixed local file only, no network call") implied genuine file I/O, which contradicts the deterministic-stub-only architecture adopted for the first Batch 1B-E implementation (register §31F). The corrected wording is shown in place above (row 48) — no path, batch tag, or creation-order value changed.

**Reconciliation summary (register §31B, Option B — Distinct Source-Adapter Control Contracts):** `ingestion/requests.py` and `ingestion/results.py` are exact, narrow, source-acquisition-only contracts (`SourceAcquisitionRequest`, `SourceAcquisitionOutcome`, `SourceAcquisitionResult`) — structurally distinct from and non-duplicative of `market_data.SourceCandleInput`/`market_data.IngestionResult`. `ingestion/port.py`'s `MarketDataSourcePort.acquire()` accepts a `SourceAcquisitionRequest` and returns a `SourceAcquisitionResult` carrying zero or more `SourceCandleInput` records — it never constructs a `RawCandle`, `NormalizedCandle`, or `market_data.IngestionResult`. Boundary flow: `Provider or deterministic source → MarketDataSourcePort → SourceAcquisitionResult → SourceCandleInput records → Phase 1B-C market-data pipeline → RawCandle/NormalizedCandle/IngestionResult`.

**Deferred, unresolved note:** the separate overlap between the older `validation/` batch (Section 14's distinct "Batch 1B-C," e.g. `validation/duplicates.py`/`validation/gaps.py`) and `market_data/idempotency.py`/`market_data/gap_observation.py` is acknowledged but **not resolved** here (register §31J). No `validation/` inventory row is changed.

**Post-audit correction pass (register §31D/§31E/§31F/§31G, resolving the first read-only architectural audit's four blocking findings):** two further row wordings were corrected — row 45 (`ingestion/port.py`)'s "Direct dependencies" column, previously `contracts/raw_candle.py` (stale, since `RawCandle` is explicitly prohibited from the port signature), now reads `ingestion/requests.py`, `ingestion/results.py`, `typing.Protocol`; row 47 (`ingestion/results.py`)'s "Direct dependencies" column now additionally names `market_data/source_input.py` (for `SourceCandleInput`), alongside `contracts/types.py`. The Section 14 batch-summary row's "Depends on" cell was likewise corrected from "1B-B (contracts)" alone to "1B-B (`contracts/`) and completed `1B-C-MD`," since `results.py` now carries `SourceCandleInput`. These are wording-only corrections — no row was added, removed, renamed, or renumbered, and paths/creation order/ownership are unchanged.

**Final consistency correction pass (register §31C/§31M, resolving the second read-only architectural audit's one blocking finding):** row 48 (`ingestion/offline_file_source.py`)'s "Direct dependencies" column — previously only `ingestion/port.py`, left incomplete when row 48's responsibility wording was first corrected — was completed to name every type `OfflineFileSource` actually depends on: `ingestion/port.py` (implements `MarketDataSourcePort`), `ingestion/requests.py` (`SourceAcquisitionRequest`, the catalogue key), `ingestion/results.py` (`SourceAcquisitionResult`, `acquire()`'s return type), `market_data/source_input.py` (`SourceCandleInput`, the catalogue value), and the stdlib `types.MappingProxyType`/`collections.abc.Mapping` (internal catalogue wrapping and constructor typing). The register's own §31C and §31M were also corrected in this pass, since they had described only the row-48 wording correction and had stated the Section 14 summary row was "unchanged" — both statements were stale after the post-audit correction pass above; §31C and §31M now accurately record all four corrected cells (rows 45, 47, 48, and the Section 14 summary row) across both passes.

**Author approval (register §31N):** the author explicitly approved Phase 1B-E Decision Group 1 exactly as documented on 2026-07-26, following a final read-only architectural audit verdict of **A. PASS — READY FOR AUTHOR APPROVAL** (no blocking finding, no non-blocking finding). This approval authorizes only the exact controlled 7-path implementation batch (5 source files, 2 test files) named above — it does not mark any row implemented, verified, committed, pushed, or closed, and it grants no production approval. **No row was added, removed, renamed, or renumbered by this approval.** The master inventory remains exactly 69 rows; Batch 1B-E remains exactly 7 rows (5 source, 2 test); creation order 44–50 is unchanged.

**No code, test, dependency, or package file is created or modified by this section.** The wording corrections to rows 45, 47, and 48, and to the Section 14 summary row, are the only inventory-affecting changes; the total changed-path count (69) and the Batch 1B-E file count (7) are unchanged.

**Implementation closure (register §31O):** the exact approved 7-path batch (5 source files under `src/btmm_ai_scanner/ingestion/`, 2 test files under `tests/unit/`) was implemented, verified, and committed as `0a9814eddd1cdeda59cf95dbde8a806f30800b44`, pushed to `origin/main`. All 7 rows above (44–50) and the Section 14 batch-summary row are updated to record this closure. **No row was added, removed, renamed, or renumbered by this closure.** The master inventory remains exactly 69 rows; Batch 1B-E remains exactly 7 rows (5 source, 2 test); creation order 44–50 is unchanged. This closure does not authorize production use, live trading, an indicator, a robot, provider networking, or a persistence backend.

## 37. Historical Repository and Replay Foundation — Inventory Addition (Implemented, Closed)

**Status: `AUTHOR-APPROVED`, `IMPLEMENTED`, `VERIFIED`, `ARCHITECTURALLY AUDITED`, `COMMITTED`, `PUSHED`, `CLOSED`, `NOT PRODUCTION-APPROVED`.** Full detail: `PHASE_1B_AUTHOR_DECISION_REGISTER.md` §32N/§32O/§32P.

**This section adds exactly 7 new rows to the master file inventory (Section 9),** under the new batch tag `1B-G-REPLAY` (the next unused batch letter after 1B-A through 1B-F, kept distinct from the special `1B-C-MD` market-data tag). **The inventory grows from 69 rows to 76 rows.** No existing row (1 through 69) is modified, removed, renumbered, or reassigned, **except** row 66 (`market_data/__init__.py`)'s "Prohibited content" and "Blocking unresolved decision" cells, which now note a pending, author-decision-required, controlled 5-name export extension (register §32J) — the row's path, batch tag (`1B-C-MD`), creation order (66), and status remain completely unchanged; this is a wording-only annotation, not a reclassification.

**Post-audit correction pass (register §32C/§32D/§32E/§32F/§32I/§32J/§32K, resolving the read-only architectural audit's three blocking findings):** (1) the repository-membership claim was corrected from an overreaching "structurally impossible" statement to an explicit three-axis distinction (structural validity / ingestion-outcome gating / analytical validity-eligibility, the last of which — `ValidationResult`/`ValidationStatus`/`AnalyticalEligibility` — the repositories never receive or enforce); (2) the single-candle `advance_one()` replay method was replaced with atomic `advance_next_availability_group()`, preventing artificial causal ordering among simultaneously-available candles; (3) range-query edge cases (`start == end`, `start > end`, naive datetime, aware non-UTC datetime) and a closed 2-exception vocabulary (`RecordIdentityConflictError`, `InvalidTimeRangeError`, both defined in `raw_candle_repository.py`) were fully specified. These corrections changed the public export count from 3 to 5 (20 → 25 total for `market_data`) and the new top-level test count from 29 to 31 (236 combined with the existing 205) — **no inventory row was added, removed, renamed, or renumbered by this correction pass**; the master inventory remains exactly 76 rows; Batch `1B-G-REPLAY` remains exactly 7 rows; creation order 69–75 is unchanged.

**The 7 new rows, by path (full detail in each row, Section 9):**

| Creation order | Path |
|---|---|
| 69 | `src/btmm_ai_scanner/market_data/raw_candle_repository.py` |
| 70 | `src/btmm_ai_scanner/market_data/normalized_candle_repository.py` |
| 71 | `src/btmm_ai_scanner/market_data/historical_replay.py` |
| 72 | `tests/unit/test_raw_candle_repository.py` |
| 73 | `tests/unit/test_normalized_candle_repository.py` |
| 74 | `tests/unit/test_historical_replay.py` |
| 75 | `tests/unit/test_no_look_ahead.py` |

**Author approval (register §32O):** the author explicitly approved the corrected Historical Repository and Replay Foundation architecture exactly as documented, with no modification to any corrected element. This approval authorizes exactly one complete implementation cycle covering all 8 approved paths at once — it does not mark any row implemented, verified, committed, pushed, or closed, and it grants no production approval. **No row was added, removed, renamed, or renumbered by this approval.** The master inventory remains exactly 76 rows; Batch `1B-G-REPLAY` remains exactly 7 new rows (3 source, 4 test), plus one existing file (`market_data/__init__.py`, row 66) pending its wording-annotated export extension; creation order 69–75 is unchanged.

Creation order 69–75 continues sequentially from the highest existing value (68, the last `1B-C-MD` row) — a documentation convenience only, not a dependency implication; this batch's only dependencies are 1B-B (`contracts/`, `config/enums.py`) and completed `1B-C-MD` (`market_data.RawCandle`/`NormalizedCandle`/`ports`), per register §32.

**Existing Protocol compatibility (register §32A):** `RawCandleSink`, `NormalizedCandleSink`, `CandleReadRepository`, and `HistoricalReplaySource` (all defined in the already-implemented, unmodified `market_data/ports.py`) are structurally sufficient for this milestone without any change. `CandleReadRepository` is satisfied only by the raw-candle repository (it is `RawCandle`-typed, not `NormalizedCandle`-typed) — a genuine, discovered structural fact, not an oversight. `market_data/ports.py` itself is **not** modified.

**No code, test, dependency, or package file is created or modified by this section.** The 7 new rows and the one wording annotation to row 66 are the only inventory-affecting changes; the total changed-path count grows from 69 to 76, and no row is added, removed, renamed, or renumbered beyond the 7 new additions.

**Implementation closure (register §32P):** the exact approved 8-path batch (3 new source files under `src/btmm_ai_scanner/market_data/`, 1 modified existing file — `market_data/__init__.py`, 4 new test files under `tests/unit/`) was implemented, verified, architecturally audited, and committed as `5a1d8f30ee0eb67d27417fda9fb7407d9a5e8a85`, pushed to `origin/main`. The final architectural audit verdict was `B. PASS WITH NON-BLOCKING FINDINGS — READY TO COMMIT`; the one non-blocking finding was a documentation-wording staleness (this document's own Section 9 summary sentence and this section's status line, both corrected as part of this same closure pass) — no code, test, or architectural defect was found. All 7 rows above (69–75) and row 66's annotation, plus the Section 14 batch-summary row, are updated to record this closure. **No row was added, removed, renamed, or renumbered by this closure.** The master inventory remains exactly 76 rows; Batch `1B-G-REPLAY` remains exactly 7 rows (3 source, 4 test); creation order 69–75 is unchanged. This closure does not authorize production use, live trading, POI detection, BTMM detection, an indicator, a robot, provider networking, or a persistence backend. **Next controlled action:** begin one combined Market Measurements and Reference Structures Foundation milestone (meaningful swings, structure transitions, displacement, equal highs/lows, support/resistance, trendlines, and liquidity references) before POI and BTMM detection — not started by this closure.

## 38. Market Measurements and Reference Structures Foundation — Inventory Addition (Implemented, Closed)

**Status: `AUTHOR-APPROVED`, `IMPLEMENTED`, `VERIFIED`, `ARCHITECTURALLY AUDITED`, `COMMITTED`, `PUSHED`, `CLOSED`, `NOT PRODUCTION-APPROVED`.** Full detail: `PHASE_1B_AUTHOR_DECISION_REGISTER.md` §33AB/§33AC.

**This section adds exactly 22 new rows to the master file inventory (Section 9),** under the new batch tag `1B-H-MEASUREMENTS` (the next unused batch letter after `1B-G-REPLAY`). **The inventory grows from 76 rows to 98 rows.** No existing row (1 through 76) is modified, removed, renumbered, or reassigned by this section — unlike Batch `1B-G-REPLAY`'s addition, this milestone modifies zero existing files, so no annotation to any prior row is needed either.

**Author approval (register §33AB):** the author explicitly approved the corrected Market Measurements and Reference Structures Foundation architecture exactly as documented, with no modification to any corrected element. This approval authorizes exactly one complete implementation cycle covering all 22 approved paths at once — it does not mark any row implemented, verified, committed, pushed, or closed, and it grants no production approval. **No row was added, removed, renamed, or renumbered by this approval.** The master inventory remains exactly 98 rows; Batch `1B-H-MEASUREMENTS` remains exactly 22 new rows (13 source, 9 test); creation order 76–97 is unchanged.

**Post-audit correction pass (register §33, resolving every blocking finding from the focused architectural audit of the originally proposed `1B-H-STRUCTURE`):** renamed to `1B-H-MEASUREMENTS`/"Market Measurements and Reference Structures Foundation"; the stateful `next_record_id()`/`next_content_fingerprint()` identity provider is replaced by a pure, content-addressed `DerivedOutputIdentityProvider.identify(output_type, semantic_key)` contract with fully-specified semantic keys per output type; `content_fingerprint` is now analyzer-computed via a fully specified canonical serialization, not caller-supplied; input ordering policy is corrected to strict Policy A (canonical pre-sorted input required, `UnsortedCandleSequenceError` on violation, no insertion-order-independence claim), with a new `AmbiguousEventTimeAnalysisError` for tied `event_time_utc` values; Wilder ATR(14) is now fully specified (seed, recurrence, warm-up, `Decimal` precision); the Equal-Level cluster-formation algorithm is now fully specified step-by-step; `SwingStrength`/`STRONG_SWING` is removed; the separate `LiquidityReference` contract is removed in favor of computed properties on `EqualLevelCluster`; Support/Resistance and Trendline are narrowed to confirmed-only output (no `DRAFT`/`STRONG`/`*_BREAK_CANDIDATE`/`BROKEN`/`INVALIDATED` status). These corrections changed the public export count from 38 to 33 (10 `measurements/` + 23 `domain/`) — the new top-level test count remains 70 (320 combined with the existing 250, AST-verified; 398 full pytest-collected total) and the file scope remains exactly 22 paths (13 source + 9 test). **No inventory row was added, removed, renamed, or renumbered by this correction pass** — the master inventory remains exactly 98 rows; Batch `1B-H-MEASUREMENTS` remains exactly 22 rows; creation order 76–97 is unchanged.

**The 22 new rows, by path (full detail in each row, Section 9):**

| Creation order | Path |
|---|---|
| 76 | `src/btmm_ai_scanner/measurements/__init__.py` |
| 77 | `src/btmm_ai_scanner/measurements/candle_metrics.py` |
| 78 | `src/btmm_ai_scanner/measurements/atr.py` |
| 79 | `src/btmm_ai_scanner/measurements/legs.py` |
| 80 | `src/btmm_ai_scanner/domain/__init__.py` |
| 81 | `src/btmm_ai_scanner/domain/configuration.py` |
| 82 | `src/btmm_ai_scanner/domain/enums.py` |
| 83 | `src/btmm_ai_scanner/domain/swings.py` |
| 84 | `src/btmm_ai_scanner/domain/displacement.py` |
| 85 | `src/btmm_ai_scanner/domain/equal_levels.py` |
| 86 | `src/btmm_ai_scanner/domain/support_resistance.py` |
| 87 | `src/btmm_ai_scanner/domain/trendlines.py` |
| 88 | `src/btmm_ai_scanner/domain/analyzer.py` |
| 89 | `tests/unit/test_market_measurement_configuration.py` |
| 90 | `tests/unit/test_confirmed_swings.py` |
| 91 | `tests/unit/test_displacement.py` |
| 92 | `tests/unit/test_equal_levels.py` |
| 93 | `tests/unit/test_support_resistance.py` |
| 94 | `tests/unit/test_trendlines.py` |
| 95 | `tests/unit/test_analyzer_api.py` |
| 96 | `tests/unit/test_batch_replay_equivalence.py` |
| 97 | `tests/unit/test_domain_exports.py` |

Creation order 76–97 continues sequentially from the highest existing value (75, the last `1B-G-REPLAY` row) — a documentation convenience only, not a dependency implication; this batch's only dependencies are 1B-B (`contracts/`, `config/enums.py`) and completed `1B-C-MD` (`contracts.NormalizedCandle`), per register §33.

**Two brand-new top-level packages, zero existing-file modification:** `measurements/` and `domain/` did not previously contain any file — both were "Directory only" per `REPOSITORY_SCAFFOLD_PLAN.md` §3 since Phase 1A. This milestone creates their first content. Because neither package existed, there is no existing `__init__.py`/export list to append to (unlike Batch `1B-G-REPLAY`'s append-only extension of `market_data/__init__.py`) — both packages' `__all__` lists are created fresh, in full, by this milestone.

**Existing Protocol compatibility (register §33S):** no `market_data` Protocol (`RawCandleSink`, `NormalizedCandleSink`, `CandleReadRepository`, `HistoricalReplaySource`) is touched. This milestone consumes `NormalizedCandle` directly from `contracts/normalized_candle.py`; at a caller's discretion in tests, it may also consume `market_data.InMemoryHistoricalReplaySource`'s output — read-only, no modification. The one new Protocol this milestone introduces, `DerivedOutputIdentityProvider` (a pure, content-addressed semantic-key identity contract — corrected from the originally proposed stateful `MarketStructureIdentityProvider`, register §33H), is local to `domain/analyzer.py`, not a `market_data` Protocol.

**Dependency-direction correction (register §33T):** `REPOSITORY_SCAFFOLD_PLAN.md` §3's Phase-1A `measurements/`/`domain/` bullets are corrected in this same operation to add `contracts` to their allowed dependency direction — a disclosed drafting-gap correction (those bullets predate the `market_data`/`contracts` package split and could not otherwise reference `NormalizedCandle` or `ContractModel`), not a scope change. See `REPOSITORY_SCAFFOLD_PLAN.md` §3 and §24 for the corrected text.

**A critical audit finding, not a silent omission (register §33B):** this milestone explicitly does **not** implement Higher High/Higher Low/Lower High/Lower Low, Break of Structure, Change of Character, or protected/weak swing labels. `REPOSITORY_SCAFFOLD_PLAN.md` §3's own `domain/` documentation already formally excludes them (`P0G-B003`), and `knowledge/market_structure/MARKET_ANALYSIS_COVERAGE_MATRIX.md`'s governing rule independently confirms neither BOS nor CHoCH is adopted as a project rule. No threshold is invented for a concept the project has three times already declared out of scope.

**No code, test, dependency, or package file is created or modified by this section.** The 22 new rows are the only inventory-affecting change; the total changed-path count grows from 76 to 98, and no existing row is added, removed, renamed, or renumbered.

**Implementation closure (register §33AC):** the exact approved 22-path batch (13 new source files under `src/btmm_ai_scanner/measurements/` and `src/btmm_ai_scanner/domain/`, 9 new test files under `tests/unit/`, 0 modified existing paths) was implemented, verified, architecturally audited, and committed as `a612d4d0cb3ef58509135edc71f459742658b5f9`, pushed to `origin/main`. The final architectural audit's one authorized correction cycle fixed two genuine defects: (1) `support_resistance.py` now locks `confirmation_candle_id` to the first qualifying touch (not the most recent) so `SUPPORT_RESISTANCE_ZONE` semantic identity remains stable as later touches appear; (2) `InvalidMarketMeasurementConfigurationError` is now raised through a defensive instrument-metadata validation path in `analyzer.py` instead of remaining an unreachable declared error. No other defect was found. Full suite **398 passed**; original baseline suite **34 passed**; new top-level test functions **70** (6+10+6+9+10+10+8+6+5); existing top-level test functions **250** (AST-verified, corrected from a stale 236 preflight estimate); combined top-level test functions **320**; full pytest-collected total **398**; public exports **33** (10 `measurements/` + 23 `domain/`), exact order; `uv lock --check`, `ruff format --check .`, `ruff check .`, and `mypy src tests` all pass. All 22 rows above (76–97), the Section 14 summary row, and this section's status line are updated to record this closure. **No row was added, removed, renamed, or renumbered by this closure.** The master inventory remains exactly 98 rows; Batch `1B-H-MEASUREMENTS` remains exactly 22 rows (13 source, 9 test); creation order 76–97 is unchanged. This closure does not authorize production use, live trading, POI detection, BTMM detection, an indicator, a robot, provider networking, or a persistence backend. **Next controlled action:** define the Structure State and Transition Foundation — HH, HL, LH, LL, bullish/bearish BOS, bullish/bearish CHoCH, protected/weak high/low, structure direction and state, and deterministic no-look-ahead transition ordering — one compact architecture definition and author approval, since these rules were explicitly deferred from `1B-H-MEASUREMENTS` (`P0G-B003`). Not started by this closure.

## 39. Structure State and Transition Foundation — Inventory Addition (Implemented, Closed)

**Status: `AUTHOR-APPROVED`, `IMPLEMENTED`, `VERIFIED`, `ARCHITECTURALLY AUDITED`, `COMMITTED`, `PUSHED`, `CLOSED`, `NOT PRODUCTION-APPROVED`.** Full detail: `PHASE_1B_AUTHOR_DECISION_REGISTER.md` §34Y (approval) / §34Z (implementation, audit, and closure record). **This section resolved every blocking finding from the focused read-only architectural audit of the originally proposed architecture in one pass** — most notably a creation-order off-by-one, corrected below — the corrected architecture was approved by the author in full (register §34Y), and the approved scope has since been implemented, verified, architecturally audited, committed, and pushed exactly as approved (register §34Z).

**This section adds exactly 15 new rows to the master file inventory (Section 9),** under the new batch tag `1B-I-STRUCTURE` (the next unused batch letter after `1B-H-MEASUREMENTS`). **The inventory grows from 98 rows to 113 rows.** No existing row (1 through 98) is added, removed, renamed, or renumbered — **except** row 82 (`domain/enums.py`), whose "Prohibited content"/"Blocking unresolved decision" cells now note an author-approved, narrow exception (3 new `DerivedOutputType` routing members) for this milestone; the row's path, batch tag (`1B-H-MEASUREMENTS`), creation order (82), and status remain completely unchanged — a wording-only annotation, not a reclassification, identical in kind to how `1B-G-REPLAY` annotated row 66.

**Corrected creation-order range: 98–112, not 99–113.** Creation order is 0-indexed throughout this project's inventory; the true highest existing creation-order value after `1B-H-MEASUREMENTS` is **97** (`tests/unit/test_domain_exports.py`, verified directly against the master Section 9 table), not 98 — "98" is correctly the pre-existing row *count* (97 + 1), but the original draft incorrectly continued the new range from that count rather than from the last *used* value. The table below is corrected to continue from 97.

**Two independent architectural facts drove the package-placement decision:** `REPOSITORY_SCAFFOLD_PLAN.md` §3's `domain/` documentation explicitly states "Must not contain: HH/HL/LH/LL/BOS/CHoCH (formally deferred, `P0G-B003`)" — placing this milestone's content there would contradict that standing restriction rather than resolve it. This milestone therefore proposes a **new top-level package**, `src/btmm_ai_scanner/structure/`, not previously reserved anywhere in `REPOSITORY_SCAFFOLD_PLAN.md`'s original directory tree — exactly mirroring how `measurements/`/`domain/` themselves were brand-new packages for `1B-H-MEASUREMENTS`. Full detail: register §34P; `REPOSITORY_SCAFFOLD_PLAN.md` §25.

**The 15 new rows, by path (full detail in each row, Section 9):**

| Creation order | Path |
|---|---|
| 98 | `src/btmm_ai_scanner/structure/__init__.py` |
| 99 | `src/btmm_ai_scanner/structure/enums.py` |
| 100 | `src/btmm_ai_scanner/structure/configuration.py` |
| 101 | `src/btmm_ai_scanner/structure/relationships.py` |
| 102 | `src/btmm_ai_scanner/structure/transitions.py` |
| 103 | `src/btmm_ai_scanner/structure/current_state.py` |
| 104 | `src/btmm_ai_scanner/structure/analyzer.py` |
| 105 | `tests/unit/test_structure_configuration.py` |
| 106 | `tests/unit/test_swing_relationships.py` |
| 107 | `tests/unit/test_structure_bootstrap.py` |
| 108 | `tests/unit/test_protected_weak_swings.py` |
| 109 | `tests/unit/test_break_and_transitions.py` |
| 110 | `tests/unit/test_structure_analyzer_api.py` |
| 111 | `tests/unit/test_structure_batch_replay_equivalence.py` |
| 112 | `tests/unit/test_structure_exports.py` |

Creation order 98–112 continues sequentially from the highest existing value (97, the last `1B-H-MEASUREMENTS` row) — a documentation convenience only, not a dependency implication; this batch's only dependencies are `domain` (specifically `ConfirmedSwing`, `SwingType`, `DerivedOutputType`, `DerivedOutputIdentityProvider`), `measurements`, `contracts`, and `config`, per register §34P.

**Source/test split 7/7 new files, plus 1 modified existing file (`domain/enums.py`, row 82, +3 `DerivedOutputType` members) — total 16 changed paths, 15 new + 1 modified.** 60 new top-level test functions across the 8 new test files (5+8+8+6+12+10+6+5), combined with the existing 320 top-level test functions: **380**. **12 new public exports for the new `structure/__init__.py` (corrected from 19 — no `domain` re-exports; callers import the 6 reused errors and `DerivedOutputIdentityProvider` directly from `btmm_ai_scanner.domain`, §34S).** Full detail: register §34Q/§34R/§34S.

**Existing Protocol compatibility (register §34P):** no `market_data` or `domain` Protocol is modified. `DerivedOutputIdentityProvider` is reused structurally unmodified from `domain`. The canonical-fingerprint/identity-resolver *implementation* (not the Protocol) is deliberately duplicated locally in the new `structure/analyzer.py` rather than importing 1B-H's private helpers — an explicitly disclosed choice with a documented maintenance risk, now backed by a required cross-package byte-equivalence test, `test_structure_fingerprint_serializer_matches_market_measurement_serializer` (register §34K/§34R).

**Post-audit correction pass (register §34, resolving every blocking finding from the focused read-only architectural audit):** the inventory creation-order range is corrected from 99–113 to 98–112 (this section); swing comparisons (relationship classification, bootstrap, protected/weak derivation) now use **source chronology** `(pivot_bar_index, pivot_start_time_utc, record_id)`, decoupled from confirmation/availability ordering, which governs only output exposure timing (register §34B/§34C); bootstrap is generalized to "the latest classified HIGH and LOW relationships agree, non-equal," replacing the original fixed "2nd vs 1st swing" rule (register §34D); protected/weak-level derivation is corrected to be transition-sensitive and symmetrically unbroken-filtered, fixing a concrete defect where a broken weak level could otherwise be re-broken (register §34F); break confirmation now records two separate fields, `broken_level_price` and `break_close_price`, replacing one ambiguous field (register §34G); BOS/CHoCH post-transition rules retire the broken weak level with no automatic replacement, and a guarded CHoCH never emits an impossible partial reversal (register §34H); at most one `StructureTransition` is ever emitted per candle, with CHoCH strictly taking priority and no same-candle chaining into a fresh BOS (register §34J); the `SWING_RELATIONSHIP` semantic key now includes both the current swing's and the predecessor's `record_id`, so a relationship is never retroactively mutated — a more complete recomputation emits an additional, differently-identified record instead (register §34K); the `CURRENT_STRUCTURE_STATE` semantic key drops a redundant literal (register §34K); every output's evidence classification is corrected to store exactly `ENGINEERING_PROVISIONAL`, distinct from this register's own document-level approval-status vocabulary (register §34K); and public exports are reduced from 19 to 12, dropping all `domain` re-exports (register §34S). These corrections changed neither the file/test/path counts (still 16 changed paths, 60 new tests, 380 combined) nor the inventory row count (still 98 → 113) — only the creation-order numbering (98–112, not 99–113) and the export count (12, not 19) changed numerically. **No inventory row was added, removed, renamed, or renumbered by this correction pass** — the master inventory remains exactly 113 rows once implemented; Batch `1B-I-STRUCTURE` remains exactly 15 rows; creation order 98–112 is the corrected, final range.

**A critical scope discipline, not a silent omission:** this milestone explicitly does **not** implement POI creation, order blocks, FVGs, candlestick POIs, Equal-Level sweep lifecycle, Support/Resistance lifecycle, Trendline lifecycle, BTMM manipulation, or any trade/execution/visualization/alerting/backtesting concern. Register §34U lists the complete exclusion set. This milestone completes `P0G-B003` — the HH/HL/LH/LL/BOS/CHoCH/protected/weak deferral first recorded against `domain/` (§3) and re-confirmed by `1B-H-MEASUREMENTS` (Section 38 above) — without extending scope beyond structure state and transitions.

**No code, test, dependency, or package file is created or modified by this section.** The 15 new rows and the one wording annotation to row 82 are the only inventory-affecting changes; the total changed-path count grows from 98 to 113, and no existing row is added, removed, renamed, or renumbered beyond the 15 new additions.

**Author-approved, exactly as corrected — no modification was made to any corrected element (register §34Y).**

**Implementation closure (register §34Z):** the exact approved 16-path batch (7 new source files under `src/btmm_ai_scanner/structure/`, 8 new test files under `tests/unit/`, 1 modified existing path `src/btmm_ai_scanner/domain/enums.py`) was implemented, verified, architecturally audited, and committed as `74fd50c6a44c208153769d2f1947bdfa7ff0d3cf`, pushed to `origin/main`. The final architectural audit's one authorized correction cycle fixed Ruff and mypy findings only (an unused import in `structure/analyzer.py`, an unused import and a concatenation-style nit in two test files, and loop-variable name reuse across differently-typed loops in `structure/transitions.py`, plus one `type: ignore` code correction) — no architecture decision, contract, test name, test count, implementation path, public export, dependency, or Protocol changed in the correction cycle. No other defect was found; every approved control was audited and confirmed exactly as designed: source-chronology comparison basis for relationships/bootstrap/protected-weak derivation; strict availability-group no-look-ahead processing; the generalized latest-relationship bootstrap rule; the transition-sensitive, symmetrically unbroken-filtered protected/weak lifecycle; the strict close-based break policy with separate `broken_level_price`/`break_close_price` fields; the exact BOS/CHoCH prerequisite/effect rules including no-automatic-replacement retirement and guarded CHoCH; the one-transition-per-candle, CHoCH-priority, no-chaining policy; the corrected `SWING_RELATIONSHIP`/`STRUCTURE_TRANSITION`/`CURRENT_STRUCTURE_STATE` semantic keys; the locally-duplicated canonical-fingerprint/identity-resolver implementation verified byte-identical to `1B-H-MEASUREMENTS`'s own via a dedicated cross-package equivalence test; the single-value `ENGINEERING_PROVISIONAL` evidence policy; and the exact 12-name `structure/__init__.py` export surface with no `domain` re-exports.

Full suite **458 passed**; original baseline suite **34 passed**; new top-level test functions **60** (5+8+8+6+12+10+6+5 across the 8 new files, exact approved distribution); existing top-level test functions **320**; combined top-level test functions **380**; full pytest-collected test total **458** (398 existing + 60 new — no parametrize used by any of the 8 new files); public structure-owned exports **12**, exact approved order; `uv lock --check` passes; `ruff format --check .` passes; `ruff check .` passes; `mypy src tests` passes with no issues across 89 source files. All 15 rows above (98–112), row 82's annotation, the Section 14 summary row, and this section's status line are updated to record this closure. **No row was added, removed, renamed, or renumbered by this closure.** The master inventory remains exactly 113 rows; Batch `1B-I-STRUCTURE` remains exactly 15 rows (7 source, 8 test); creation order 98–112 is unchanged.

This closure does not authorize production use, live trading, POI detection, BTMM detection, an indicator, a robot, provider networking, or a persistence backend. **Next controlled action:** define the **POI Detection Foundation** — POI candidate identity, POI type taxonomy, order-block candidates, fair-value-gap candidates, support/resistance and trendline POI references, equal-level liquidity references, candlestick-pattern POIs, structural-context requirements, strong-timeframe precedence, overlap/merge rules, confirmation, validity, breach, reclaim, invalidation, availability time, no-look-ahead behavior, and deterministic identity/fingerprinting/replay equivalence — using the completed measurement and structure foundations. This next milestone must not yet include BTMM manipulation lifecycle, entry signals, stop loss, take profit, position sizing, visualization, Telegram alerts, broker execution, AI inference, or production approval. Not started by this closure.

## 40. POI Detection and Lifecycle Foundation — Inventory Addition (Implemented, Closed)

**Status: `AUTHOR-APPROVED`, `IMPLEMENTED`, `VERIFIED`, `ARCHITECTURALLY AUDITED`, `COMMITTED`, `PUSHED`, `CLOSED`, `NOT PRODUCTION-APPROVED`.** Full detail: `PHASE_1B_AUTHOR_DECISION_REGISTER.md` §35AP (approval) / §35AQ (implementation, final audit, and closure record). **This section records the exact approved inventory addition for `1B-J-POI` — POI Detection and Lifecycle Foundation** (the title includes "Lifecycle" because the milestone implements the already-approved POI Boundary Breach/Reclaim/Invalidation standard and the already-approved Freshness/Age standard as public `PoiLifecycleTransition` records and `CurrentPoiState` fields for **exactly 18** of the 36 POI specifications, not detection alone — corrected from an original miscount of "30"/"32"). **The approved architecture was implemented, verified, architecturally audited, committed, and pushed exactly as approved (register §35AQ), implementation commit `ebc55e0bdbbef7b7f528d874107daa2d75a5628a`.**

**This section recorded exactly 34 new rows in the master file inventory (Section 9), now locked in by the focused inventory-readiness check's amendment,** under the next unused batch letter, `1B-J-POI`, following `1B-H-MEASUREMENTS` and `1B-I-STRUCTURE`. **The inventory has grown from 113 rows to 147 rows (creation order 113–146).** No existing row (1 through 113) was modified, removed, renamed, or renumbered — **except** row 82 (`domain/enums.py`), whose "Prohibited content"/"Blocking unresolved decision" cells now carry a second, narrow, author-approved exception (3 new `DerivedOutputType` routing members: `POI_OBSERVATION`, `POI_LIFECYCLE_TRANSITION`, `CURRENT_POI_STATE`) for this milestone; the row's path, batch tag, creation order (82), and status remain completely unchanged — a wording-only annotation, not a reclassification, identical in kind to the annotation already recorded for `1B-I-STRUCTURE`.

**36-POI coverage and implementability gate (register §35C):** 36 of 36 approved book POI specifications audited; **32 IMPLEMENTABLE_WITH_AUTHOR_GAP_FILL, 4 DEFERRED (Bullish/Bearish Trendline — no approved finite zone geometry exists; Swing High/Swing Low — already fully represented by the existing, closed `domain.ConfirmedSwing` contract, not duplicated), 0 BLOCKED.** **Of the 32 implementable types, exactly 18 are lifecycle-eligible (`FULL`) and exactly 14 are permanently `PoiLifecycleStatus = NOT_APPLICABLE`** — the 18 are the 10 volume + 6 price-action types plus Support and Resistance; the 14 are Equal Highs, Equal Lows, and all 12 Previous/Current period-level types (corrected by the focused audit — earlier drafts miscounted this as 30 or 32). No volume-based POI is blocked by missing real/tick volume data — every one of the 10 volume-based specifications is fully detectable via already-approved price-action proxies (`relative_size_ratio`, `range_context_ratio`, `body_efficiency`, `directional_close_position`, **explicitly Option B**, per register §35Q), with `NormalizedCandle.volume`/`volume_kind` (already implemented, 1B-B) available as strictly secondary, non-mandatory evidence when present — these four proxy fields are computed internally only and are never stored on `PoiObservation` or exported.

**The 18 proposed new source rows, by path (`poi/` — a new top-level package, not previously reserved):**

| Creation order | Path |
|---|---|
| 113 | `src/btmm_ai_scanner/poi/__init__.py` |
| 114 | `src/btmm_ai_scanner/poi/enums.py` |
| 115 | `src/btmm_ai_scanner/poi/configuration.py` |
| 116 | `src/btmm_ai_scanner/poi/observation.py` |
| 117 | `src/btmm_ai_scanner/poi/lifecycle.py` |
| 118 | `src/btmm_ai_scanner/poi/current_state.py` |
| 119 | `src/btmm_ai_scanner/poi/order_blocks.py` |
| 120 | `src/btmm_ai_scanner/poi/fair_value_gaps.py` |
| 121 | `src/btmm_ai_scanner/poi/reversal_candles.py` |
| 122 | `src/btmm_ai_scanner/poi/bases.py` |
| 123 | `src/btmm_ai_scanner/poi/pressure_wicks.py` |
| 124 | `src/btmm_ai_scanner/poi/engulfing.py` |
| 125 | `src/btmm_ai_scanner/poi/single_candle_reversals.py` |
| 126 | `src/btmm_ai_scanner/poi/three_candle_stars.py` |
| 127 | `src/btmm_ai_scanner/poi/reference_zones.py` |
| 128 | `src/btmm_ai_scanner/poi/period_levels.py` |
| 129 | `src/btmm_ai_scanner/poi/overlap.py` |
| 130 | `src/btmm_ai_scanner/poi/analyzer.py` |

**The 16 proposed new test rows, by path (corrected per-file counts, register §35AI):**

| Creation order | Path | Test count |
|---|---|---|
| 131 | `tests/unit/test_poi_configuration.py` | 7 |
| 132 | `tests/unit/test_order_blocks.py` | 8 |
| 133 | `tests/unit/test_fair_value_gaps.py` | 8 |
| 134 | `tests/unit/test_reversal_candles.py` | 8 |
| 135 | `tests/unit/test_bases.py` | 8 |
| 136 | `tests/unit/test_pressure_wicks.py` | 6 |
| 137 | `tests/unit/test_engulfing.py` | 5 |
| 138 | `tests/unit/test_single_candle_reversals.py` | 5 |
| 139 | `tests/unit/test_three_candle_stars.py` | 5 |
| 140 | `tests/unit/test_reference_zones.py` | 7 |
| 141 | `tests/unit/test_period_levels.py` | 8 |
| 142 | `tests/unit/test_poi_lifecycle_and_freshness.py` | 14 |
| 143 | `tests/unit/test_poi_overlap_merge_and_precedence.py` | 9 |
| 144 | `tests/unit/test_poi_analyzer_api.py` | 11 |
| 145 | `tests/unit/test_poi_batch_replay_equivalence.py` | 6 |
| 146 | `tests/unit/test_poi_exports.py` | 5 |

**Corrected path-split terminology (register §35AH): both exact splits stated, never conflated.** **NEW-PATH SPLIT:** 18 new source / 16 new test (34 new paths). **AFFECTED-PATH SPLIT:** 19 source (18 new + 1 modified `domain/enums.py`) / 16 test (35 affected paths total). 120 new top-level test functions (7+8+8+8+8+6+5+5+5+7+8+14+9+11+6+5), combined with the existing 380: **500**. **23 new public exports for `poi/__init__.py`** (corrected from an internally-inconsistent original claim of 24; no `domain`/`structure` re-exports; callers import the 4 reused errors and `DerivedOutputIdentityProvider` directly from `btmm_ai_scanner.domain`). Full detail: register §35AH/§35AI/§35AJ.

**Dependency direction — corrected (register §35AH, `structure` removed):** `poi/` depends on `domain`, `measurements`, `contracts`, `config` — does **not** depend on `structure` (removed entirely; no implementable POI type's approved rule required it, register §35B/§35S) and does not depend on `market_data`'s pipeline/repository/replay modules directly. No `market_data`/`domain` Protocol is modified.

**Fingerprint strategy (register §35G): Option C** — a third disclosed, tested-equivalent duplicate of the canonical `_canonicalize`/`_compute_content_fingerprint` implementation (matching `1B-I-STRUCTURE`'s own precedent), now backed by a required **three-way** cross-package equivalence test (`test_poi_fingerprint_serializer_matches_domain_and_structure_serializers`), extending the two-way test `1B-I-STRUCTURE` already established.

**Resolved and now author-approved: the period-window gap is fully specified as one exact, `ENGINEERING-PROVISIONAL` UTC policy (register §35R).** Day = `[00:00:00 UTC, next day 00:00:00 UTC)`; ISO week = `[Monday 00:00:00 UTC, next Monday 00:00:00 UTC)`; calendar month = `[1st 00:00:00 UTC, 1st-of-next-month 00:00:00 UTC)` — no broker-local timezone, no DST adjustment, weekends/holidays simply contain no candles. **The author has explicitly ratified this exact policy (register §35AP, item 9)**; `poi/period_levels.py` may now be implemented against it, exactly as documented.

**The 34 new rows and the one wording annotation to row 82 were the only inventory-affecting changes at approval/lock time;** the total changed-path count grew from 113 to 147, and no existing row was added, removed, renamed, or renumbered beyond the 34 new additions.

**Author-approved, exactly as corrected, and now implemented, verified, architecturally audited, committed, pushed, and closed exactly as approved (register §35AQ).** Implementation commit `ebc55e0bdbbef7b7f528d874107daa2d75a5628a`, push `02750b2..ebc55e0 main -> main`. Full suite **578 passed** (458 existing + 120 new); public exports **23**; `PoiType` members **32**; lifecycle-eligible types **18**; `NOT_APPLICABLE` types **14**; one genuine pre-commit correction (a lifecycle-status-reset defect in `poi/lifecycle.py`, corrected inside the approved 35-path scope, no architecture/contract/test-count/export-count/dependency change). No production approval is implied or requested — the milestone remains `NOT PRODUCTION-APPROVED`. **Next controlled action:** define the **BTMM Manipulation Lifecycle Foundation** (proposed identifier `1B-K-BTMM`), covering POI interaction as a required boundary, manipulation lifecycle states, accumulation/distribution context, and the remaining items recorded in register §35AQ — one compact architecture definition, one focused audit, at most one consolidated correction, explicit author approval, and one complete implementation cycle, none of which is started by this record.
