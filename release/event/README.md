# RC5 v1.0 — EVENT RELEASE PACKAGE

```
EA/          -> GIVE TO CUSTOMER   RC5_EA.ex5 + INSTALL.md
Scanner/     -> GIVE TO CUSTOMER   how to get invite-only access
Docs/        -> release notes, event-day checklist
Licensing/   -> ADMIN ONLY         admin runbook + access registry
Demo/        -> presenter walkthrough
Checksums/   -> build manifest and hashes
```

## The three rules

1. **Customers receive `EA/` and `Scanner/` only.** `Licensing/` is admin.
2. **No source, ever** — not the `.mq5`, not the `.pine`.
3. **Nothing is published** until the author says PUBLISH.

## Honest status

Verified: the Python engine, the Pine token budget and compile, the EA's
execution doctrine on real Strategy Tester quotes, and the licensing layer.

Pending: Pine runtime visual acceptance, the structure label coordinate
question, FX order execution, and the EA's terminal-event close path — which
has no runtime event source yet.

No profitability claim is made anywhere in this package, and none should be
made at the event. See `Docs/RELEASE_NOTES.md`.
